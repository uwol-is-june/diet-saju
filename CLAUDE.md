@AGENTS.md

# diet-saju

생년월일시로 **사주 원국(사주팔자)** 을 계산하고, Gemini(무료 등급)로 해석문을 생성해 보여주는 웹 서비스.
Next.js App Router 단일 앱으로 구성되고 Vercel 에 배포된다.

> `@AGENTS.md` 는 `next dev` 가 자동 관리하는 Next.js 규칙 블록이다. 직접 수정하지 말 것.

## 명령어

```bash
npm run dev        # 개발 서버 (3000 사용 중이면 3001로 자동 이동)
npm run build      # 프로덕션 빌드 (배포 전 필수)
npm run typecheck  # tsc --noEmit
npm run lint       # eslint

npm test              # Vitest 전체
npm run test:watch    # 감시 모드
npm run validate:saju # 사주 계산 테스트만 (= vitest run lib/saju)
```

테스트는 `lib/**/*.test.ts` 에 코드와 나란히 둔다. `vitest.config.mts` 가 `server-only` 를
빈 모듈로 별칭 처리한다 — 이게 없으면 `pillars.ts` import 가 예외를 던진다.
설정 파일이 `.mts` 인 이유는 `.ts` 로 두면 Vite 가 CJS 로 읽어 경고가 나기 때문이다.

### 풀이 서술 품질은 테스트가 아니라 표본으로 잰다 (TASK-53)

```bash
npm run dev                                                  # 다른 터미널에서
node scripts/sample-readings.mjs --label before              # 6표본 × 공개 유형 3
node scripts/sample-readings.mjs --label after --repeat 2    # 회차 간 일치도까지
```

**CI 에 넣지 않는다** — Gemini 쿼터를 쓰고(회당 18요청) 결과가 회차마다 다르다.
프롬프트·섹션 계약·모델 설정을 건드리는 작업은 **고치기 전에 기준선이 있어야** 시작한다.

**기준선은 이미 찍혀 있다** (`docs/saju-validation.md` 3-6 절 · 2026-08-18).
**고치기 전에 `--label before` 로 다시 찍지 말고 그 표를 기준으로 삼는다** — 같은 표를 두 번
만들면 어느 쪽이 기준인지 갈린다. 고친 뒤에 `--label after --repeat 2` 로 대조한다.

**분량을 늘리는 변경은 두 번 재야 한다.** 한 번만 재면 늘어난 자리를 무엇이 채웠는지 못 본다 —
실제로 hedge 가 기준선 위로 되돌아가고 라벨 인용이 떨어진 것을 두 번째 회차에서 잡았다.

- 지표는 **보조다.** "공감되는가" 는 셀 수 없으므로 **지표만 좋아지고 읽기가 나빠지면
  지표를 버린다.** 판단은 산출물 마크다운을 직접 읽고 한다.
- 섹션 제목과 공개 유형 목록을 **소스에서 정규식으로 읽는다** — 스크립트에 베껴 두면
  계약이 바뀔 때 옛 제목을 재는 세 번째 사본이 된다.
- 표본은 **합성 생일 6건**이고 판정 공간을 훑는다. `expect` 가 매 회차 판정을 대조하므로
  `constitution.ts` 를 고쳐 표본이 다른 칸으로 밀리면 실패로 잡힌다.
- **산출물(풀이 마크다운)은 커밋하지 않는다.** OS 임시 디렉터리에 쓰고, 저장소에는
  `docs/saju-validation.md` 3-6 절의 지표 표와 판단만 남긴다.

## 아키텍처

요청 흐름은 한 방향이다.

```
app/page.tsx                  유형 선택 (서버 컴포넌트 · 정적, 입력 없음)
  └─ app/reading/[type]/page.tsx   입력 + 결과 (세그먼트 = ReadingType id)
        │
components/SajuForm.tsx  (client · readingType 은 prop)
        │  ├─ components/BirthInputProvider.tsx  입력값 (app/layout.tsx 에 얹힘)
        │  ├─ lib/form/birth-input.ts   입력값 모양·요약 (클라이언트 안전)
        │  ├─ ResultView.tsx → ReadingSections.tsx  섹션별 렌더
        │  └─ lib/reading/sections.ts   섹션 계약 + 파서 (클라이언트 안전)
        │  POST /api/saju  →  NDJSON 스트림 응답
        ▼
app/api/saju/route.ts    (server, nodejs runtime)
        ├─ lib/rate-limit.ts   IP 기준 분당 제한
        ├─ lib/saju/schema.ts  zod 재검증 (클라이언트 검증은 신뢰하지 않음)
        ├─ lib/saju/pillars.ts 만세력 계산  ← 사실(fact)은 코드가 만든다
        │    ├─ time-correction.ts  진태양시·서머타임·표준자오선 보정 (tz 기반)
        │    ├─ ganji.ts            간지 테이블·십신·왕상휴수사·시주 규칙 (순수 함수)
        │    ├─ analysis.ts         오행 강약·신강신약·대운·세운 (순수 함수)
        │    ├─ constitution.ts     체질 경향 판정 규칙표 (순수 함수)
        │    └─ yearly.ts           올해 세운 판정 규칙표 (순수 함수)
        ├─ lib/prompt.ts       프롬프트 조립
        └─ lib/gemini.ts       Gemini 호출  ← 해석(interpretation)만 LLM 이 한다
```

### 반드시 지킬 경계

1. **사주 계산은 절대 LLM 에게 맡기지 않는다.**
   간지·오행·십신은 `lib/saju/pillars.ts` 가 계산하고, LLM 은 그 결과를 "해석"만 한다.
   프롬프트에도 `계산 완료 · 수정 금지` 로 명시되어 있다. 이 원칙을 깨면 결과가 틀린다.
   **체질 판정도 같다** — `lib/saju/constitution.ts` 가 정하고 LLM 은 서술만 한다.

2. **`lib/` 의 서버 모듈은 맨 위에 `import "server-only"` 를 유지한다.**
   클라이언트 컴포넌트에서 실수로 import 하면 빌드가 깨지도록 만든 안전장치다. 제거하지 말 것.

3. **API 키는 `lib/env.ts` 와 `lib/gemini.ts` 밖에서 읽지 않는다.**
   다른 파일에서 `process.env.GEMINI_API_KEY` 를 직접 읽는 코드를 추가하지 말 것.

4. **클라이언트 컴포넌트에서 Gemini 를 직접 호출하지 않는다.** 항상 `/api/saju` 경유.

### 스트리밍 응답 규약

`/api/saju` 는 NDJSON 스트림을 돌려준다 (한 줄에 이벤트 하나, `SajuStreamEvent`).
순서는 `chart` → `delta*` → `done` 이다.

**상태 코드를 쓸 수 있는 구간은 스트림을 열기 전까지다.** 200 본문을 쓰기 시작하면
되돌릴 수 없다. 그래서 실패를 두 갈래로 나눈다.

| 시점 | 실패 | 응답 |
| --- | --- | --- |
| 스트림 전 | 레이트 리밋 / 입력 검증 / 사주 계산 / 서버 설정 | JSON + 429·400·500 |
| 스트림 후 | Gemini 호출 실패 (쿼터·안전정책 등) | 200 + `error` 이벤트 |

원국은 코드가 10ms 안에 계산하는데 Gemini 첫 조각은 약 1초 뒤에 온다. 상태 코드를
정하려고 Gemini 를 기다리면 이미 확정된 사실을 붙잡고 있게 되므로, **원국을 먼저 보내고
Gemini 실패는 이벤트로** 알린다. 원국은 LLM 실패와 무관하게 유효한 결과다.
그래서 **관측도 상태 코드만 세면 안 된다** — `error` 이벤트를 함께 집계한다 (아래 "관측").

실측(로컬, 워밍업 후): 원국 60~70ms · 첫 글자 1.0~1.3초 · 전체 4.2~4.4초.

### 두 단계 플로우 — 유형 먼저, 입력 나중 (TASK-30)

`/` 에서 유형을 고르고 `/reading/[type]` 에서 정보를 넣는다. **`/` 에는 생년월일 입력이 없고
`/reading/*` 에는 유형 선택 컨트롤이 없다.**

- **단계는 클라이언트 상태가 아니라 라우트로 나눈다.** 스텝 상태로 하면 뒤로가기·북마크·
  유형별 메타데이터를 셋 다 잃는다. 세그먼트는 기존 `ReadingType` id 를 그대로 쓴다 —
  한글 슬러그를 만들면 API 계약(`schema.ts`)과 URL 이 두 벌이 된다.
- `generateStaticParams` 로 `READING_TYPES` 를 프리렌더하고 그 밖의 값은 `notFound()`.
  카드는 버튼 + `router.push` 가 아니라 **`next/link`** 여야 새 탭·가운데 클릭·크롤러가 산다.

#### 입력값은 메모리에만 둔다 — 저장소·URL 로 옮기지 말 것

`components/BirthInputProvider.tsx` 가 `app/layout.tsx` 에 얹혀 있다. Next 문서가
"layouts preserve state, remain interactive, and do not re-render on navigation" 을 보장하므로
(`node_modules/next/dist/docs/01-app/04-glossary.md`) `/` ↔ `/reading/*` 이동에서 값이 남는다.
**`app/reading/layout.tsx` 로 내리면 안 된다** — `/` 를 거쳐 가면 언마운트되어 값이 날아간다.

- **`sessionStorage`·`localStorage`·쿼리스트링을 쓰지 않는다.** 셋 다 생년월일이 프로세스
  밖으로 나간다 — 앞의 둘은 디스크에, 쿼리스트링은 방문 기록과 **Vercel 액세스 로그**에.
  그 순간 `app/privacy/page.tsx` 의 "저장하지 않습니다" 를 **같은 커밋에서 고쳐야 한다.**
- 지금 메모리 상태는 새로고침·새 탭·탭 닫기로 사라진다 — 폼이 원래 하던 것과 같은 범주라
  처리방침을 손대지 않아도 된다. **이 조건을 벗어나면 고쳐야 한다는 뜻이기도 하다.**
- `lib/form/birth-input.test.ts` 가 소스에서 저장소·URL 사용을 막는다. 검사는 **주석을
  걷어낸 코드**를 본다 — 주석이 "쓰지 않는다" 를 설명하느라 같은 낱말을 쓰기 때문이다.
- **입력값과 함께 "완료된 풀이" 도 들고 다닌다** (TASK-60). `OtherReadingLinks` 가 유형 사이를
  오가라고 만든 동선이라 `diet` → `gain-cause` → 다시 `diet` 가 기본 경로인데, 예전에는
  돌아올 때마다 **이미 본 글을 다시 생성**했다. 아까운 것은 토큰이 아니라 **요청 수**다
  (무료 등급의 병목은 500 RPD). 키는 `lib/form/reading-cache.ts` 가 만들고 규칙은 넷이다.
  - **키는 입력 스냅샷 전체 + 유형이다.** `Object.keys` 로 훑으므로 `BirthInput` 에 필드가
    늘면 자동으로 들어간다 — **손으로 나열하지 말 것.** 나열하면 새 필드가 생겼을 때
    조용히 옛 결과가 나온다. 이름도 키에 들어간다(호칭이 본문에 박힌다).
  - **입력이 바뀌면 통째로 비운다.** 그래서 상한·LRU 가 필요 없다 — 남는 것은 유형 수만큼이다.
  - **완료된 것만 담는다** — `done` 까지 왔고 `error` 이벤트가 없었으며 중단되지 않은 경우.
    중간까지 받은 글을 담으면 다음에 **완결된 풀이인 척** 나온다. `birth-input.test.ts` 가
    소스에서 이 조건을 확인한다.
  - **캐시 적중에는 자동 스크롤을 걸지 않는다.** 그래서 스크롤 효과가 `chart` 가 아니라
    `arrivedCount`(응답으로 원국이 도착한 횟수)에 걸려 있다. `chart` 로 되돌리면 링크를 눌러
    막 들어온 사람(이미 맨 위에 있다)의 화면이 튄다.
- **캐시도 메모리에만 있다.** 위 저장소·URL 금지가 그대로 적용되고, 그래서 처리방침을
  고치지 않아도 된다. **서버 캐시는 다른 이야기다** — 거기 담기는 것은 생년월일 파생 키 +
  풀이 전문(사주팔자·이름 포함)이라 방침 3항을 같은 커밋에서 고쳐야 한다.
- **조회수는 캐시 적중이어도 센다.** 그 유형 페이지를 본 것은 맞다. `ViewBeacon` 은 그대로다.
- 값이 있으면 폼을 접고 요약 한 줄 + **연필 아이콘**(44px 원형)만 보여준다. **둘은 같은
  줄이다** (TASK-89) — 예전에는 줄이 `flex-wrap` 이라 요약이 길면 버튼이 아랫줄로 떨어져
  무엇에 걸린 버튼인지 읽히지 않았고, 모바일에서 요약은 거의 늘 두 줄이라 **그게 기본
  모습이었다.** 글 쪽 `min-w-0` + 버튼 `shrink-0` 이 그것을 지킨다.
  **접힌 채로 바로 제출할 수 있어야 한다** — 유형만 바꿔 다시 받는 것이 두 번 클릭이다. 요약에 **이름은 넣지 않는다.**
  제출할 수 없는 값이면 접지 않는다 (왜 버튼이 꺼져 있는지 볼 수 없게 된다).
- `FirstVisitNotice` 는 `/reading/*` 에 있다. "입력한 생년월일은 저장하지 않습니다" 는
  **정보를 넣기 직전에** 보여야 뜻이 있다. 그래서 `/` 에는 클라이언트 컴포넌트가 없다.

#### 폼의 보조 설명은 기본값으로 대체한다 (TASK-98)

컨트롤마다 한 줄씩 붙어 있던 **보조 설명 셋을 걷어냈다** (2026-08-20 사용자 확정).

| 지운 줄 | 무엇이 대신하는가 |
| --- | --- |
| 성별 아래 `고르면 10년 단위 흐름(대운)까지…` | 없다 — 그 사실을 미리 말하지 않기로 했다 (위 "성별을 요구하는 유형이 생겼다") |
| 시각 아래 `분까지 고를수록 정확합니다…` | 없다 — 분 드롭다운이 이미 1분 단위로 열려 있어 시키는 일이 따로 없었다 |
| 지역 아래 `고르지 않으면 서울 기준으로 계산합니다…` | **기본값 자체** — 시/도가 `서울` 로 선택된 채 열린다 |

- **판단 기준은 "그 줄이 지금 할 일을 말하는가" 다.** 셋 다 아니었다. 조용히 적용되는
  기본값을 글로 알리던 마지막 줄은 **기본값을 눈에 보이게 해서** 필요가 없어졌다 —
  화면이 이미 말하는 것을 문장으로 되풀이하지 않는다 (`/` 카드의 판단과 같은 선).
- **막는 문구 둘은 남는다.** 반쪽 시각(`시와 분을 모두 골라 주세요…` · `role="alert"`)과
  지역이 잠긴 이유(`출생시각을 모르거나 시각 보정을 끄면…`)다. **앞의 것은 왜 버튼이
  꺼져 있는지, 뒤의 것은 왜 컨트롤을 누를 수 없는지**를 말한다 — 지우면 그 자리에서
  알 길이 없어진다. `missingForType`(성별을 요구하는 유형)도 그대로다.
  `birth-input.test.ts` 가 **지운 셋과 남긴 둘을 양쪽에서** 본다.
- **출생지 기본값은 `EMPTY_BIRTH_INPUT` 이 든다** (`birthplaceSido`·`birthplaceName` 둘 다
  `서울`). 서울은 시/군이 하나뿐이라 `placesInChosenSido.length > 1` 이 거짓이고
  **두 번째 드롭다운은 그대로 안 뜬다** — `chooseSido` 가 광역시에서 만드는 상태와 같은
  모양이다. **하나뿐인 시/도 목록을 새로 적지 말 것** (`birth-input.test.ts` 가 막는다).
- **원국은 그대로다.** 달라지는 것은 경도가 요청에 **명시적으로 실린다**는 것뿐이다 —
  표의 서울(126.99)과 서버 기본값(`SEOUL_LONGITUDE` 126.9784)은 분 단위로 반올림되는
  보정에서 같은 −32분이 된다. 생일 다섯(시지 경계 01:31 · 23:45 · 00:05 포함)으로
  `calculateSajuChart` 결과가 바이트까지 같은 것을 확인했고, 회귀는
  `birth-input.test.ts` 가 `resolveBirthInstant` 두 갈래를 대조해 잡는다.
- **접힌 요약과 결과 화면에 `서울` 이 늘 붙는다** (반영되지 않는 상태는 예외).
  계산에 실제로 쓰이는 값이라 보이는 편이 맞다. 요약이 길어지는 것은 이미 있던 최장
  경우(`강원 고성군`)보다 짧아 TASK-89 의 한 줄 규격을 새로 건드리지 않는다.
- **지역 이름은 여전히 서버로 보내지 않는다** — 가는 것은 경도뿐이다. 개인정보
  처리방침은 손대지 않았다.

#### 홈으로 돌아가는 동선 (TASK-42 → 74 → 91 · 93)

**전역 헤더는 없다.** `components/SiteHeader.tsx` 를 지웠다 (TASK-91).

**TASK-74 가 TASK-42 의 "전역 헤더를 두지 않는다" 를 뒤집었던 것을 다시 되돌렸다.**
같은 자리를 두 번 뒤집었으니 **세 번째로 다시 붙이지 않도록** 이유를 남긴다 — TASK-74 는
"결과 화면에서 스트리밍 중에 시선을 뺏는다" 를 **감수했다**고 적었는데, 감수한 값이
실제로는 그것 하나가 아니었다.

- **결과 화면에서 sticky 띠가 글 위에 계속 얹혀 있었다.** 풀이는 2,000자를 통독하는
  글인데 스크롤하는 동안 화면 위 56px 이 언제나 로고였다.
- **띠가 하는 일은 링크 하나였다.** 검색도 메뉴도 없어서(위 "레퍼런스에서 받은 것" 표)
  높이 56px 을 전 페이지에서 내주고 받는 것이 `/` 로 가는 길 하나였다. 그 길은
  **`/reading/[type]` 의 `<` 버튼**(TASK-93)과 푸터 `처음으로` 가 낸다.
- **`/` 에서는 오히려 방해였다.** 머리 그라데이션 띠(TASK-86)가 콘텐츠 열 맨 위에서
  시작해야 하는데 그 위에 흰 띠가 하나 더 있었다.

**되붙이려면 위 셋을 어떻게 치를지 먼저 답할 것.** "로고가 있으면 브랜드가 산다" 는
답이 아니다 — 좌측 패널(`AppPanel`)이 데스크톱에서 그 일을 하고, 모바일에서는 `/` 의
머리 띠가 한다.

- **`/reading/[type]` 은 맨 위 왼쪽 `<` 버튼이 홈 동선이다** (TASK-93 · 97 ·
  `BackIconLink`). **히어로 사진 위에 얹혀 있다** — 자리와 `surface` variant 의 근거는
  아래 "화면 1 구조 — 히어로 사진".
  헤더가 없어졌고 유형 줄도 없앴으므로 **TASK-74 가 그 페이지의 `BackLink` 를 뺐던
  근거 둘이 둘 다 사라졌다.**
  - **`history.back()` 이 아니라 `/` 로 가는 링크다.** 이력에 기대면 클라이언트 JS 가
    필요하고, 검색·공유 링크로 바로 들어온 사람에게는 돌아갈 이력이 없다 (`BackLink` 가
    처음 생긴 이유가 그것이다). `next/link` 라야 새 탭·가운데 클릭·크롤러가 산다.
  - **이름은 `aria-label` 이 만든다.** 화살괄호는 `aria-hidden` 장식이다 (`SajuForm` 의
    `수정` 아이콘 버튼과 같은 방식).
  - **규격(44px 원형)은 `buttonClass({ size: "icon" })` 에서 온다.** `Button` 은
    `<button>` 이라 링크로 쓸 수 없어 **규격 문자열만** 가져온다 — 44px 원형을 호출부에서
    다시 스타일링하면 규격이 두 벌이 된다. 위 "컬러 토큰과 부품 규격" 절을 함께 볼 것.
    **자리(`absolute left-5 top-3`)만 호출부가 정한다** (TASK-97) — 사진을 펼치는 음수
    여백과 위치 기준이 같은 요소여야 해서다.
- **두 고지 페이지의 `<BackLink />` 는 그대로 남는다.** `/privacy` · `/disclaimer` 는
  **문서가 길어서** 나가는 길을 찾으려면 화면 맨 위까지 스크롤을 되감아야 하고, 검색·공유
  링크로 바로 들어온 사람에게는 뒤로가기도 없다. 그 이유는 헤더와 무관했다.
  **거기에 아이콘 꼴을 쓰지 않는다** — 문서 상단에서는 어디로 가는지가 글자로 읽혀야 한다.
- `components/BackLink.tsx` 가 **두 꼴을 한 파일에서** 정의한다 (`BackLink` 텍스트 ·
  `BackIconLink` 아이콘). 목적지(`/`)와 위 판단이 같은 자리에 있어야 갈라지지 않는다.
  라벨 인자는 남겨 뒀다 — 기본값이 있어 인자 없이 쓰는 쪽이 계약이다.
- **푸터 링크 이름은 목적지를 말해야 한다.** 예전 `사주 풀이` 는 어디로 가는지도, 지금
  거기 있는지도 알려주지 않아 홈에서 눌렀을 때 "안 되는 링크" 로 읽혔다. 지금은 `처음으로` 다.
- **현재 페이지면 링크를 죽이는 처리(`aria-current`)는 하지 않는다.** `usePathname()` 이
  필요해 `SiteFooter` 가 클라이언트 컴포넌트가 되고, 레이아웃에 있으므로 **`/` 에도
  클라이언트 JS 가 들어간다** — `/` 를 통째로 정적으로 두는 성질이 깨진다. 테스트가 막는다.

#### 결과 뒤 동선 (TASK-31)

결과 아래의 `OtherReadingLinks` 가 나머지 두 유형으로 보낸다. **평범한 라우트 이동이다** —
`router.replace` 로 URL 과 화면을 따로 맞추는 편법을 쓰지 않는다. 프로바이더가 입력을 들고
있으므로 옮겨 간 화면은 값이 채워진 채 접혀 있고, 제출 한 번이면 그 유형의 풀이가 나온다.
URL 과 보이는 결과가 항상 일치하는 쪽을 택한 것이다.

**스트리밍 중에는 내지 않는다** — 지금 쓰이고 있는 글을 끊게 만든다.

### 섹션 계약 — JSON 구조화 출력을 쓰지 않는 이유 (TASK-06 결정)

`responseMimeType: "application/json"` + `responseSchema` 는 **채택하지 않았다.** JSON 은
완성 전까지 파싱이 불가능해서 (`{"summary": "고객님은 태어` 는 깨진 문자열이다) 점진 렌더가
죽는다. 첫 글자가 1.3초에 보이는 것을 4초로 바꾸는 대가라 형식을 다른 방법으로 강제한다.

**`lib/reading/sections.ts` 의 섹션 계약이 단일 소스다.**

- 제목 문자열은 거기에만 있다. `lib/prompt.ts` 가 그걸 읽어 `## 제목` 지침을 만들고,
  `components/ReadingSections.tsx` 가 같은 계약으로 도착한 마크다운을 가른다.
  **제목을 프롬프트나 컴포넌트에 문자열로 적지 말 것** — 둘이 어긋나면 화면이 폴백으로 떨어진다.
- 이 파일은 클라이언트에서 import 되므로 `server-only` 가 없고 **프롬프트 지시문도 두지 않는다.**
  섹션별 지시문은 `prompt.ts` 가 id 로 붙인다 (`SECTION_INSTRUCTION`).
- 파서는 **누적 문자열 전체를 매번 다시 파싱한다.** 증분 상태를 들면 조각 경계 버그를 다 떠안는다.
- `###` 은 섹션 경계가 아니다. 그리고 **해시만 도착한 마지막 줄은 보류한다** —
  `### 소제목` 이 오는 중이면 `##` 까지는 섹션 제목과 구별할 수 없어서 빈 카드가 번쩍인다.
- 계약을 어긴 출력을 **버리지 않는다.** 모르는 제목은 `id: null` 로 담고, 계약 섹션을 하나도
  못 잡으면 `recognized: false` 로 알려 원문 마크다운 폴백으로 간다. 형식 문제가 내용 손실로
  번지면 안 된다. 강제가 아니라 부탁이므로 이 폴백이 반드시 있어야 한다.
- `ResultView` 에 넘기는 `readingType` 은 **라우트 세그먼트에서 온다** (TASK-30). 생성 중에
  바뀔 수 없으므로 폼이 요청 시점 유형을 따로 붙들 필요가 없다 — 예전의 `resultType` 상태는
  없앴다. **폼 안에 유형 선택 컨트롤을 되살리지 말 것** — 두 곳에서 고를 수 있으면 계약이
  갈려 폴백으로 떨어진다. 테스트가 소스에서 막는다(`lib/form/birth-input.test.ts`).
- 아코디언은 쓰지 않는다. 접혀 있으면 스트리밍을 볼 수 없고 풀이는 통독하는 글이다.
  요약만 카드로 띄우고 나머지는 한 카드 안에서 구분선으로 나눈다.
- **판정 한 줄 콜아웃은 `chart` 값으로 직접 그린다** (TASK-47 · `VerdictCallout.tsx`).
  LLM 이 쓰지 않는다 — 판정은 코드가 한다는 경계이고, **원국은 60~70ms 에 오는데 첫 글자는
  1초 뒤**라 기다리는 동안 결론 한 줄이 먼저 뜬다. 스트리밍 중에도 낸다(읽고 있는 글을
  끊는 것이 아니라 그 글의 결론이다 — `OtherReadingLinks` 와 다른 점).
  - 어느 유형이 무엇을 내는지는 `Record<ReadingType, (chart) => Callout | null>` 이다.
    삼항으로 두면 새 유형이 조용히 남의 콜아웃을 달고 나간다.
  - **모든 유형이 낸다** (TASK-66). 글을 읽기 전에 키워드 하나를 먼저 쥐여 주는 카드라
    유형을 가릴 이유가 없다. **조건은 하나 — 라벨이 기존 판정에서 1:1 로 파생될 것.**
    억지로 만들면 동점 처리를 새로 정해야 하고 그 순간 "판정은 코드가 한다" 가 무너진다.
    (`deficient[0]` 처럼 목록의 첫 항목을 쓰는 것은 새 동점 처리가 아니다 — 그 목록은
    오행 열거 순서라 이미 고정돼 있고 공유 카드 칩이 같은 값을 쓴다.)
  - **"본문에서 되풀이하지 말라" 는 `gainLabel` 에만 건다.** 그것만 문장 꼴이라 본문에
    그대로 들어가면 같은 말이 화면 위아래에 두 번 나온다. 나머지(대사 기조·접근 순서 …)는
    낱말 꼴이고 `SYSTEM_INSTRUCTION` 이 **본문에 그대로 쓰라고 요구하는 값**이라, 되풀이를
    금지하면 라벨 인용률(기준선 48/48)을 스스로 깎는다.
  - **`diet-food` 라벨은 오행 이름이 아니라 재료 이름이다** (TASK-102 · `ELEMENT_FOOD.short`).
    옛 `금 계열을 곁들이기` 는 공개 다섯 유형의 라벨 중 **이것만 오행을 아는 사람만 읽을 수
    있었고**, 실제 재료는 본문이 도착해야 나왔다 — 이 카드가 메우려는 1초 동안 아무것도
    전달하지 못했다는 뜻이다. 공유 카드 칩도 같은 값을 쓴다(`무와 버섯 곁들이기`).
    - **새 동점 처리가 없다** — `deficient[0]` → 오행 → `short` 가 `Record` 로 1:1 이다.
    - **맛을 라벨에 올리지 않는다.** `ELEMENT_FOOD.basis` 가 `매운맛·흰색` 이라 그대로
      쓰고 싶어지지만, 한열이 `열` 인 사람 화면에서 `THERMAL_GUIDE.열` 의 "맵고 뜨거운
      음식과 술을 줄이기" 와 정면으로 부딪힌다. **재료는 오행, 조리·온도는 한열**이라는
      층 구분이 라벨에서 무너지는 자리다. 색만 쓰는 안(`흰빛 재료`)도 버렸다 — 우리가
      지어낸 말이라 한국어로 어색하다.
    - **근거 줄이 오행 이름과 나머지 재료를 함께 받는다.** 오행을 빼면 판정의 출처가
      사라지고(근거 카드 `사주 원국` 과 같은 낱말이어야 이어진다), 재료를 빼면 라벨의 두
      가지가 **그것만 먹으라는 말**로 읽힌다. 그래서 **이 유형의 콜아웃만 근거가 네 줄이고
      카드가 다른 유형보다 높다** — 재료와 조리 두 층을 함께 말하는 유일한 유형이라 치른 값이다.
    - 조사(`을`/`를`)는 받침으로 정한다 — 다섯 라벨마다 문자열을 손으로 적으면 표가 하나 더 생긴다.
    - **공유 카드 칩은 한 줄에 들어간다** (실측 · 실제 캔버스 `measureText`). 여섯 경우
      모두 두 칩 끝이 645~714px 이고 한계는 1,008px 이다. `card-model.test.ts` 가 글자 수
      상한(14자)으로 그 여유를 지킨다 — 칩에는 줄바꿈이 없어 넘치면 카드 밖으로 나간다.
    - **부족한 오행이 없으면 `오행이 고르게 퍼진 편`** 이다(옛 `곁들일 계열이 따로 없음`).
      없다는 말보다 판정을 그대로 말하는 쪽이 읽힌다. 칩은 `오행이 고름` 그대로다.
  - **라벨만 크게 띄우지 않는다** — 근거 한 줄을 함께 둔다. 라벨만 있으면 "당신은 먹는
    자리에서 찐다" 는 몸에 대한 확정 진술로 읽힌다.
  - **라벨 문구는 `GAIN_LABEL` 이 고른다** (`gainPattern` 에서 1:1 파생이라 새 동점이 없다).
    형태는 **"~할 때 붙는 성향"** 으로 고정했다 — 초안이던 `많이 먹어서 찐 살` 은 TASK-55
    경계의 (ii)몸의 인과에 걸린다. **`~할 때` 는 언제인지만 말하고 왜인지는 말하지 않는다.**
    `constitution.test.ts` 가 인과 어미와 hedge 어미를 양쪽에서 막고 형태도 고정한다.
  - 프롬프트에도 같은 라벨을 주되 **본문에서 되풀이하지 말라**고 지시한다. 안 그러면 화면
    위아래에 같은 말이 두 번 나온다 (`prompt.test.ts` 가 지시 존재를 확인).
  - **라벨마다 사진이 하나씩 붙는다** (TASK-90 · 94 · `public/verdict/<슬러그>.jpg` 21장).
    이 카드가 원래 첫 글자 오기 전 1초를 메우는 자리라 볼거리가 하나 더 붙는 것이다.
    - **슬러그는 유형이 아니라 축 값에 붙는다** (`Record<MetabolismTendency, …>` 처럼).
      유형에 붙이면 축에 값이 늘 때 컴파일이 잡아 주지 않는다. 21장의 내역은 대사 기조
      2 · 살 붙는 패턴 5 · 접근 순서 4 · 곁들일 계열 6(부족 오행 5 + 없음 1) · 대표 종목 4다.
    - **`priority` 를 주지 않는다.** `/` 첫 카드는 어느 장이 필요한지 서버가 아는데
      (세그먼트) 이쪽은 **`chart` 가 와야 안다** — 제출 전에는 preload 할 대상이 없다.
      유형별 후보를 다 미리 받는 것은 모바일에서 쓰지 않을 장을 받는 것이다. 대신
      `loading="eager"` 로 지연 로드만 끈다.
    - **피사체 규칙은 유형 사진과 같다** — 사람 없는 정물, 특정 식품 금지(식사
      도구까지), 운동 기구는 허용. **`element-*` 여섯 장이 가장 아슬아슬하다**: "목 계열을
      곁들이기" 를 채소 사진으로 찍으면 `ELEMENT_FOOD` 닫힌 목록을 그림으로 우회하는
      셈이라, 재료가 아니라 **오행 자체의 상징**(잎·불꽃·도자기·수저·물)을 쓴다.
    - **운동 기구 검색은 거의 전부 인물 사진이다** (실측: `kettlebell`·`barbell` 8건 중
      6건). 후보를 늘려도 안 걸러지고 기구가 바닥에 놓인 장면으로 좁혀야 정물이 나온다.
    - 받는 법은 `node --env-file=.env.local scripts/fetch-verdict-photos.mjs --count=6`
      → 컨택트시트를 보고 `--pick=<슬러그>:<번호>,…`. **컨택트시트가 없으면 고를 수 없다** —
      후보가 100장이 넘어 파일 이름만으로는 어느 슬러그의 몇 번인지 놓친다.
    - `lib/reading/verdict-photo.test.ts` 가 **슬러그를 세 곳에서 대조한다** (컴포넌트 ·
      스크립트 검색어 표 · `public/verdict/`). `Record` 는 값이 빠지는 것만 잡고 **슬러그
      오타는 못 잡는다** — 그 자리에서 어긋나면 그 판정이 나온 사람의 화면에서만 깨진다.
    - **유형 사진과 같은 장을 쓰지 않는다** (TASK-94). TASK-92 가 `/` 카드 사진을 이 화면
      상단 히어로로 올리면서 둘이 세로로 나란히 놓이는데, `movement-rhythm` 이
      `/cards/exercise.jpg` 와 **같은 Pexels 사진**(11513443)이라 **한 화면에 같은 그림이
      두 번** 나왔다 — 콜아웃이 히어로의 꼬리처럼 읽혔다. 검색어를 갈아 다시 골랐고
      (`sneakers by front door` → `empty crosswalk zebra stripes`) 같은 테스트가 두
      `photos.json` 의 id 가 겹치는지 본다. **눈으로는 잘 안 잡힌다** — 콜아웃은 실제로
      제출해야 뜨고 21종 중 그 판정이 나오는 생일을 넣어야 그 조합이 화면에 온다.
    - **`object-position` 을 주지 않는다** (TASK-94 실측 · 390px). 카드가 낮아 정사각
      원본이 크게 잘릴 것 같지만 **슬롯이 거의 정사각이다** — 폭 42% 가 147px 이고 카드
      높이가 138px(라벨 한 줄) ~ 166px(두 줄)이다. 가운데 있는 피사체(`element-hwa`
      촛불)가 그대로 산다. **21칸짜리 표를 하나 더 만들 이유가 없다.** 히어로는 사정이
      다르다(열 폭을 채워 세로 절반이 잘린다 · 위 "유형 사진" 절).
    - **가장 긴 라벨에서도 한 줄 결론으로 읽힌다** (실측). `긴장이 쌓일 때 붙는 성향`(13자)
      이 `text-xl` 62% 폭에서 두 줄이 되고 카드가 138 → 166px 로 자라는데, 큰 글자가
      여전히 카드의 주인이라 성격이 흐려지지 않는다. **어두운 사진도 확인했다** —
      `gain-geunyuk`(체육관)과 `element-hwa`(거의 검정)가 `bg-brand-subtle` 위에서
      글자와 겹치지 않고 왼쪽으로 곱게 풀린다.
- **본문 강조는 프롬프트가 만들고 코드가 판정 라벨을 보탠다** (TASK-65 · `lib/reading/emphasis.ts`).
  - **프롬프트가 본류다** — 어디가 핵심인지 아는 것은 문장을 쓰는 쪽뿐이다.
    `SYSTEM_INSTRUCTION` 이 **절마다 한두 곳까지**로 상한을 준다. 상한이 없으면 문단이 굵은
    글자로 뒤덮여 아무것도 강조되지 않는다.
  - **코드는 판정 라벨의 첫 등장만** 감싼다. 전부 감싸면 같은 이유로 망가진다.
  - **닫히지 않은 `**` 를 먼저 지운다.** 스트리밍 중에는 `**식욕형` 까지만 도착한 순간이
    반드시 있고 `react-markdown` 은 그걸 별표 그대로 그린다 (섹션 파서가 해시만 온 줄을
    보류하는 것과 같은 문제). **뒤 내용을 감추지 않고 표시만 지운다.**
  - 라벨을 `Prose` 까지 **context 로** 내린다 — prop 으로 흘리면 호출부 셋 중 **폴백**을
    빠뜨리게 되고 그 경로는 평소에 안 보인다.
  - **색을 주지 않는다.** `.reading strong` 이 굵기 600 + `ink`(본문은 `ink-soft`)로 이미
    두 단계를 만든다. `brand-ink` 를 얹으면 강조가 링크처럼 읽힌다.
- **절 제목은 `text-lg`(18px)이고 `globals.css` 의 `.reading h2` 와 같은 값이다** (TASK-67).
  예전에는 컴포넌트 쪽이 `text-base`(16px)라 본문과 크기가 같았고, **계약이 깨져 원문 폴백으로
  갈 때만 제목이 커졌다** — 두 경로가 어긋나 있었다. 한쪽을 고치면 다른 쪽도 같이 본다.
  요약 카드 제목은 `text-sm` 그대로다 — 강조 카드라 작아도 되는 자리다.
- **섹션 `key` 는 `id-index` 다.** id 만 쓰면 **모델이 같은 제목을 두 번 낼 때 겹친다**
  (표본 4건 중 1건에서 실제로 나왔다). index 는 스트리밍 중 앞쪽 섹션에서 바뀌지 않으므로
  "제목을 key 로 쓰지 않는다" 의 이유(조각마다 remount)는 그대로 지켜진다.
- **계약 제목은 모델이 재현하기 쉬운 낱말로 고른다** (TASK-58 · 48). 형식 강제 수단이
  부탁뿐이라 어려운 낱말은 그대로 파손률이 된다. **`움직-` 어간이 그랬다** —
  `움직일까` 는 12건 중 3건(`움직il까`·의역), `움직여 볼까` 는 6건 중 1건(`움직여 봈까`)이
  깨졌다. 같은 자리에서 `어떤 순서로 먹을까`·`무엇을 먼저 고정할까` 는 한 번도 안 깨졌다 —
  **어간이 문제이지 문형이 아니다.** 지금은 `어떤 운동 종류가 맞을까`·`어떤 강도로 할까` 이고
  12건 중 0건이다. **제목을 고칠 때는 표본으로 재현율을 확인할 것.**
- **마지막 절을 "실천 3가지" 로 두지 말 것** (TASK-58). `diet-method` 의 그 절이 위 세 절을
  한 줄씩 요약해 **새 정보가 0** 이었다(12건 중 예외 없음). 지금은 `first-step`
  (`이번 달 먼저 할 하나`)이고 **하나만 고르고 · 자리 잡았는지 무엇으로 아는지 · 그다음
  무엇으로 넘어가는지**를 요구한다. 파생 축을 접는 것보다 이쪽이 원인이었다.
- **제목 앞 아이콘은 계약이 아니라 화면에서 붙인다** (TASK-46). `SECTION_ICON` 이
  `Record<ReadingSectionId, string>` 이라 섹션을 늘리면 컴파일 오류로 잡힌다.
  - **`title` 에 이모지를 섞지 말 것.** 계약을 `## 🌿 오행으로 본 체질` 로 바꾸면 모델이
    그 이모지까지 글자 하나 틀리지 않게 재현해야 파싱된다. 어긋나는 순간 그 절이
    `id: null` 로 떨어져 **아이콘도 강조도 함께 사라진다.** 형식 강제 수단이 부탁뿐인데
    부탁할 것을 늘리는 셈이다. `sections.test.ts` 가 제목 문자 집합을 검사한다.
  - **계약에 없는 절(`id: null`)과 원문 폴백에는 붙이지 않는다** — 어느 절인지 모른다.
  - `aria-hidden` 이다. 스크린리더가 "나뭇잎 오행으로 본 체질" 로 읽으면 안 된다.
  - 이형자 선택자가 필요한 문자(`⚖️`·`🎚️`·`🍽️`·`🌤️`)는 **선택자를 붙인 채로** 저장한다.
    빼면 플랫폼에 따라 글자 모양(⚖)으로 나온다.
  - **장기·특정 식품 그림은 쓰지 않는다.** 🫀 는 진단처럼 읽히고 🍚 는 특정 식품이라,
    본문에서 막는 것을 아이콘으로 흘리는 셈이 된다 (👤 · 🍽️ 로 골랐다).
  - 공유 카드에는 넣지 않는다 — 캔버스 이모지는 OS 폰트에 따라 두부(□)가 된다.

### 문장 묶음 단위 줄바꿈 (TASK-28 → 80)

풀이 본문은 **렌더 직전에 문장 묶음마다 문단으로 갈라진다** (`lib/reading/line-breaks.ts`).

**TASK-28 은 문장마다 갈랐고 그것이 반대쪽으로 넘어갔다** — 한 문장짜리 문단이 스무 개쯤
이어져 글이 목록처럼 읽혔다(`diet` 실측 3,208자에서 문단 48개). TASK-80 이 **길이 문턱**을
넣어 되돌렸다. **없앤 것은 문단 수이지 규칙이 아니다** — 아래 넷은 그대로다.

- **프롬프트로 풀 수 없다.** `react-markdown` 은 CommonMark 라 단일 개행을 줄바꿈으로
  렌더하지 않는다(`remark-breaks` 없음). 모델이 개행을 넣어도 화면은 그대로다.
  그래서 형식은 코드가 만든다 — 모델이 어떻게 쓰든 같은 모양이 나온다.
- **`<br>`(hard break)이 아니라 문단이다.** 줄 간격이 본문과 같으면 모바일에서 한 문장이
  여러 줄로 접히는 순간 경계가 사라진다. 390px 에서 한국어 한 문장은 보통 두세 줄이다.
- **종결 부호 앞 글자가 한글이거나 닫는 괄호·따옴표일 때만** 가른다. 그래서 `3.5` ·
  `1. 항목` · `e.g.` 가 애초에 안 걸린다 — 예외 목록을 관리하지 않아도 된다.
- **종결 부호 뒤에 공백과 다음 글자가 다 와야** 가른다. 안 그러면 마지막 문장이 쓰이는 중에
  줄이 생겼다 사라진다. 테스트가 한 글자씩 늘려 가며 문단 수가 줄지 않는지 본다.
- **묶는 규칙은 길이 문턱 하나다** (TASK-80). 문장 경계를 왼쪽부터 훑다가 **쌓인 덩어리가
  `CHUNK_MIN_CHARS`(80자)를 넘으면 거기서** 가른다. 규칙 하나로 둘이 함께 풀린다 —
  모델이 이미 넣은 문단 경계가 살고(변환이 줄 단위라 빈 줄을 넘지 않는다), 긴 문단만 갈린다.
  - **문장 수로 세지 않는다.** 같은 두 문장이라도 30~120자로 흔들려 화면에서 보이는
    덩어리가 세 배씩 차이 난다. 읽는 사람이 느끼는 것은 문장 수가 아니라 줄 수다.
  - **80 은 우리 관례이고 근거는 390px 실측이다** (2026-08-19 · `diet` · `diet-method`).
    이 폭에서 한 줄이 24자 안팎이고 풀이 한 문장이 50~65자라, 문턱 80이면 덩어리가 대개
    두 문장 · 95~107자 · **4~5줄**이 된다. 60으로 내리면 한 문장짜리 문단이 다시 섞이고,
    120으로 올리면 가장 긴 문단이 200자(9줄)를 넘어 TASK-28 이 없앤 벽이 돌아온다.
  - **왼쪽부터 훑는 그리디여야 한다.** 어느 경계에서 가를지가 그 앞 글자에만 달려 있어야
    스트리밍 중 이미 내린 결정이 바뀌지 않는다. 문단 전체 길이를 보고 균등 분할하면
    마지막 문장이 도착할 때마다 앞쪽 경계가 움직여 읽던 자리가 튄다.
  - **문장을 쪼개지 않는다.** 그래서 100자가 넘는 긴 문장은 그대로 혼자 문단이 된다.
- **`<Markdown>` 을 직접 부르지 말고 `ReadingSections.tsx` 의 `Prose` 를 쓴다.** 호출부가
  셋(본문·프리앰블·**폴백**)이고 폴백은 평소에 안 보여서 빠뜨려도 눈에 안 띈다.
  테스트가 소스를 읽어 `<Markdown>` 이 한 곳에만 있는지 확인한다.
- `.reading p` 여백(`margin-bottom: 0.75rem`, `line-height: 1.75`)은 **그대로 뒀다.**
  390px·1280px 실측에서 문장 경계가 뚜렷하면서 과하지 않았다. **여백을 줄여 문단 수를
  유지하는 쪽(TASK-80 의 후보 (c))은 택하지 않았다** — 끊김의 원인은 여백이 아니라 덩어리가
  한 문장이라는 것이고, 여백만 줄이면 목록처럼 읽히는 것은 그대로인 채 경계만 흐려진다.

`.reading` 클래스는 **본문에만** 붙인다. `globals.css` 의 `.reading h2` 는 `@layer` 밖이라
Tailwind 유틸리티(`@layer utilities`)를 이긴다 — 섹션 제목에 걸면 컴포넌트의 `text-base` 같은
클래스가 조용히 무시된다.

## 관측 (TASK-13)

`lib/observability.ts` 가 요청마다 구조화된 JSON 한 줄을 stdout 에 남긴다 (Vercel Runtime Logs).

- **상태 코드로 집계하지 말 것.** Gemini 실패는 200 본문 안의 `error` 이벤트로 나가므로
  상태 코드만 세면 실패율이 0 으로 보인다. `SajuOutcome` 으로 센다 —
  `ok` / `rate_limited` / `invalid_input` / `chart_failed` / `config_error` / **`reading_failed`**.
  마지막 것이 그 200-속-실패다.
- 실패 종류는 `GeminiFailureKind`(`quota_day`·`quota_minute`·`auth`·`safety`·`empty`·`unknown`)이며
  **사용자 문구와 같은 분류에서 나온다**(`lib/gemini.ts` 의 `toGeminiError`). 두 곳에서 따로
  판별하면 어긋난다 — 일일 소진에 "1분 후 다시" 라고 하면 거짓말이 된다.
- **개인정보를 싣지 않는다.** `METRIC_FIELDS` 허용 목록에 있는 키만 직렬화되고, 타입을 우회해
  넣어도 버려진다. 성별은 값이 아니라 **지정 여부**만 남긴다. 필드를 늘릴 때는 "이것이 개인을
  식별할 수 있나" 를 먼저 답하고, `app/privacy/page.tsx` **6항의 목록도 같은 커밋에서 고친다.**
- 계측이 요청을 깨뜨리지 않는다 — `emit` 은 어떤 예외도 밖으로 내보내지 않는다.

**싱크는 stdout 하나뿐이다.** 외부 수집기(Sentry 등)도 방문자 분석 도구도 붙이지 않기로
확정했다 (아래 "확정된 결정사항"). 나중에 필요해지면 `emit` 만 갈아끼우면 된다 —
그때 제3자가 늘어나므로 `app/privacy/page.tsx` 4항도 함께 고칠 것.

참고로 **Vercel Web Analytics 의 custom events 는 Hobby 플랜에서 쓸 수 없다**(요금표의 Hobby
칸이 `-`). 지표를 Vercel 대시보드 패널로 올리려면 Pro 가 필요하다는 뜻이니, 무료 등급에서
관측을 다시 검토할 때 이 사실부터 확인할 것.

## 조회수와 좋아요 (TASK-51)

유형마다 **열람 수**와 **좋아요 수**를 센다. 로그인은 없다. `lib/counters.ts` 가 저장소를
맡고 `lib/counter-request.ts` 가 두 라우트(`/api/views` · `/api/likes`)의 앞단을 정한다.

- **저장소는 Upstash Redis 다** (Vercel 대시보드 > Storage 연동). 인메모리 `Map` 으로 두면
  인스턴스마다 다른 수가 화면에 찍힌다. 필요한 연산이 원자적 증가라 읽기 캐시로 대체할 수
  없다 — **레이트 리밋을 영속화할 일이 생기면 새로 붙이지 말고 이걸 쓴다.**
- **환경변수 이름이 두 벌이다.** 마켓플레이스 연동은 옛 Vercel KV 시절 이름
  (`KV_REST_API_URL`·`KV_REST_API_TOKEN`)으로 주입하고, Upstash 콘솔에서 직접 받으면
  `UPSTASH_REDIS_REST_*` 다. `lib/env.ts` 가 둘 다 받는다. 한쪽만 읽으면 **대시보드에서는
  연결됐는데 앱은 "설정 없음"** 이 된다 (실제로 한 번 겪었다).
- **키는 `views:<type>` · `likes:<type>` 여덟 줄이 전부다.** 결과별로 세지 않는다 — 결과에
  id 가 필요해지고 그건 결과를 서버에 두는 것이라 "저장하지 않습니다" 가 죽는다.
  **생년월일에서 파생된 값은 해시도 키에 넣지 않는다.**
- **누가 눌렀는지 서버에 남기지 않는다.** 좋아요 중복은 브라우저 `localStorage` 플래그로만
  막는다. 서버에 신원을 만들면 처리방침 3항("IP 최대 1분")을 고쳐야 하는데 좋아요는 정확한
  수가 필요한 값이 아니다. 개발자 도구로 풀 수 있다는 것을 알고 고른 쪽이다.
  **취소를 허용하므로 서버가 하한 0 을 강제한다** (저장소를 지운 브라우저에서 취소만 누르면
  `DECR` 이 음수로 내려간다).
- **조회수는 브라우저가 센다.** `/reading/[type]` 은 네 유형이 프리렌더돼 있어서, 렌더 경로
  에서 저장소를 건드리면 **모든 방문이 함수 실행**이 된다. `ViewBeacon` 이 마운트 뒤에 한 번
  두드리고 `useRef` 로 StrictMode 이중 실행을 막는다. 덤으로 **크롤러는 JS 를 안 돌려 세어지지
  않는다.**
- **`/` 는 여전히 클라이언트 JS 가 없다.** 숫자를 서버에서 읽고 `revalidate = 300` 으로 돌린다.
  그래서 `readCounters(초)` 는 **GET 경로 형태**를 쓴다 — Next 의 Data Cache 는 GET 만
  캐시하므로 POST 로 보내면 `/` 가 동적이 된다. `/admin` 은 반대로 `no-store` 다(지금 붙어
  있는지 보는 화면이라 캐시하면 뜻이 없다).
- **비콘에 풀이 생성용 레이트 리밋을 걸지 않는다.** 분당 5회는 Gemini 기준이라 유형을
  훑어보는 사람이 막힌다. 버킷과 한도를 따로 둔다(`counter:` 접두사 · 분당 60).
- **저장소가 죽어도 화면은 산다.** 숫자를 못 읽으면 그 자리를 숨긴다 — **0 을 대신 보여주지
  않는다.** 아무도 안 봤다는 거짓말이 되는데 실제로는 셀 수가 없는 상태다.
- 처리방침 4·5·6항이 이 절을 서술한다. **세는 항목을 늘리면 세 곳과 시행일·변경 안내를 같은
  커밋에서 고친다.**

## 결과 공유 (TASK-10)

두 갈래다. **개인 결과는 이미지로, 서비스는 링크로** 공유한다.

- **이미지 저장** — `components/ShareActions.tsx` 가 브라우저 캔버스에 1080×1350 카드를 그린다.
  `lib/share/card-model.ts`(무엇을 넣을지)와 `lib/share/draw-card.ts`(어떻게 그릴지)로 갈라 뒀다.
  서버를 거치지 않으므로 **원국 데이터가 브라우저를 떠나지 않고**, 개인정보 처리방침의
  처리 경로가 그대로다. `next/og`(satori)를 쓰지 않은 이유는 한글 글리프 때문에 폰트 파일을
  넣거나 요청마다 받아야 하기 때문이다.
- **제목 아래 설명 줄은 없앴다** (TASK-100 · 사용자 확정). 그 줄이 하던 말 둘 중
  **"이미지가 서버로 전송되지 않는다" 는 화면 어디에도 없어졌다** — `app/privacy/page.tsx`
  는 공유 이미지를 언급하지 않는다. 처리방침이 약속하는 범위가 바뀐 것은 아니므로
  (이미지는 여전히 브라우저에서만 만들어진다) 문서를 고칠 일은 없지만, **없앴다는 것을
  모르고 "안내가 있으니 괜찮다" 고 판단하지 않도록** 여기 적어 둔다. 나머지 한 말
  ("공개된 곳에 올릴 때 참고")은 카드에 생년월일·나이를 찍지 않는 규칙이 이미 위험의
  큰 쪽을 막고 있다. **되살린다면 처리방침 쪽에 두는 편이 낫다** — 결과 화면은 읽고 난
  뒤의 자리라 매번 같은 고지가 눈에 건너뛰인다.
- **버튼 둘은 어느 폭에서도 한 줄이다** (TASK-88 · `grid-cols-2`). 예전에는
  `flex-col sm:flex-row` 라 **모바일에서만** 세로로 쌓여, 주 사용자가 있는 쪽에서만 48px
  짜리 큰 면 둘이 화면을 차지했다. 절반 폭에 앉히느라 라벨을 줄이고(`이미지로 저장` →
  `이미지 저장`) 아이콘을 붙였다 — 아이콘은 `aria-hidden` 장식이고 버튼 이름은 옆 글자가
  만든다. **글자만 `text-sm` 으로 내리고 부품 규격(48px · radius 12px)은 그대로다** —
  `size="compact"` 로 낮추면 40px 이라 손가락으로 누르는 자리에 낮다. 360px 실측에서
  필요한 폭이 124px, 칸이 135px 이다.
- **카드에 생년월일·나이를 찍지 않는다.** 사용자가 공개된 곳에 올리는 이미지라 우리가 먼저
  날짜를 넣어 줄 이유가 없다. 띠·계절·간지·십신까지만 쓴다. 대운도 나이 구간을 빼고
  간지·십신만 적는다 (구간을 적으면 출생 연도가 좁혀진다). 테스트가 이 규칙을 막는다.
- **og:image 는 고정 카드다** — `app/opengraph-image.png`. 원본은 `docs/og-card.html` 이고
  1200×630 으로 렌더해 교체한다. 팔레트 값은 globals.css 에서 복사한 것이며 어긋나면
  `lib/design/tokens.test.ts` 가 실패한다. **유형별로 만들지 않는다** — 세 벌이 되면 원본
  HTML 도 팔레트 검사도 세 벌이 된다.
- **파일 규약 이미지는 그 세그먼트에만 붙고 하위로 상속되지 않는다.** Next 문서가
  "set ... **for a route segment**" 라고 쓴 그대로이며 실측으로 확인했다 —
  `app/opengraph-image.png` 만 두면 `/reading/*` 에는 og:image 가 아예 없다. 그래서
  `app/reading/[type]/page.tsx` 의 `generateMetadata` 가 **같은 고정 카드 경로**를 명시한다.
  사람들이 실제로 공유하는 URL 이 그쪽이므로 이 줄을 지우면 이미지 없는 카드가 나간다.
- `metadataBase` 없이는 og:image 가 상대 경로로 나가고 카카오톡 크롤러가 못 읽는다. 지우지 말 것.
- **파비콘·앱 아이콘도 같은 방식이다** (TASK-26 · 76). 원본은 `docs/icon.html` 이고
  `node scripts/render-icons.mjs` 로 `app/icon.png`(512) · `app/apple-icon.png`(180) ·
  `app/favicon.ico`(48) 를 만든다. 결과물은 커밋하므로 배포에 그 스크립트가 필요 없다.
  - **문양은 다시(dasii) 로고에서 왔다** (TASK-76). 글자(`다시,`)를 빼고 문양만 쓴다 —
    팔레트가 원래 그 브랜드에서 온 것이라(`docs/design-tokens.md`) 마크도 같은 곳을 따른다.
  - **로고 PNG 를 오려 붙이지 않았다.** 원본에서 문양은 55×55 라 512 로 키우면 뭉개진다.
    원본 픽셀에서 도형(링 둘 + 점 둘)을 역산해 **벡터로 다시 그렸다** — 55×55 마스크와
    대조해 3,025 화소 중 32 개(가장자리 안티에일리어싱)만 어긋난다. `docs/icon.html` 의
    좌표는 그 실측값이라 **눈대중으로 고치지 말 것.**
  - **좌측 패널은 반대로 글자를 원본 파일로 쓴다** (TASK-83 · `public/dasii/logo_text.png`).
    파비콘은 글자를 빼므로 벡터 문양만으로 끝나지만, 그 패널은 **브랜드 이름을 내는 자리**라
    글자꼴이 원본과 달라지면 안 된다 — 시스템 폰트 굵기 800 으로 흉내 냈던 것이 실제로
    어색했다. 512 로 키울 일이 없으니 래스터로 둬도 값을 치르지 않는다. 문양은 거기서도
    같은 벡터 좌표이고, 비율·출처는 `public/dasii/README.md` 에 있다.
  - **타일이 없어졌다.** 예전에 여기 적혀 있던 "브라우저 탭용 `icon` 은 우리가 둥글려야
    한다" 는 더는 해당 사항이 없다 — 둥글릴 면이 없다. 브라우저 변형은 **배경이 없고**,
    타일을 되살리면 그 타일 색이 곧 두 번째 브랜드 색이 된다 (초록 면 위 민트→블루 문양은
    서로 싸운다).
  - `apple-icon` 만 **면을 채운다** — iOS 는 투명을 검정으로 합성하므로 홈 화면 아이콘에
    투명을 남길 수 없다. 면은 `--raw-white-warm` 이고 **모서리는 여전히 둥글리지 않는다**
    (iOS 가 스스로 깎으므로 미리 깎으면 두 번 깎여 흰 틈이 남는다). 대신 문양을 작게 넣어
    깎이는 자리에 점이 걸리지 않게 한다.
  - **그라데이션 끝색만 팔레트에 새로 넣었다**(`--raw-mark-blue`). 시작색은 `green500` 이
    그대로 맞는다 — 원본 회귀값과 채널당 1~2 차이라 눈으로 구별되지 않는다.
    `--raw-blue-500` 은 더 진하고 붉어서 대체가 안 된다. **글자 배경으로 쓰지 말 것**,
    대비 검증을 하지 않은 장식 전용 값이다. **UI 팔레트는 그대로 그린이다** — 이 그라데이션은
    아이콘 안에서만 산다 (아래 "확정된 결정사항" 의 브랜드 컬러를 뒤집는 것이 아니다).
  - **playwright 는 의존성이 아니다.** 아이콘을 다시 그릴 때만 쓰므로 브라우저 바이너리를
    상시로 두지 않는다. 스크립트가 `PLAYWRIGHT_CORE_PATH`·`CHROMIUM_PATH` 를 받는다.
    `playwright-core` 는 CJS 라 `PLAYWRIGHT_CORE_PATH` 로 파일을 직접 가리키면
    `import()` 결과에 `chromium` 이 안 붙을 수 있다 — 그때는 `export const chromium =`
    한 줄짜리 ESM 껍데기를 저장소 **밖에** 두고 그것을 가리킨다.
  - `.ico` 는 인코더 없이 만든다. Vista 이후 ICO 는 **PNG 를 그대로 품을 수 있어서**
    22바이트 헤더만 붙이면 된다 (`pngToIco`).
  - **`/apple-touch-icon.png` 는 404 로 둔다.** Next 규약상 파일은 `/apple-icon.png` 로
    나가고 `<link rel="apple-touch-icon">` 이 그쪽을 가리킨다 — iOS 가 읽는 것은 그 태그다.
    같은 이미지를 `public/` 에 복사해 두면 두 벌을 맞춰야 하므로 하지 않았다.

### 결과 영구 링크(`/r/[id]`)는 도입하지 않았다

결과별 OG 카드를 만들려면 크롤러가 읽을 URL 이 있어야 하고, 그러려면 결과를 서버에 두어야
한다. 그건 `app/privacy/page.tsx` 의 "저장하지 않습니다" 를 무효화한다. 지금 그 약속은
이 서비스의 성격을 규정하는 문장이라 이미지 공유로 대체했다.

다시 볼 시점은 **결과를 서버에 캐싱할 때**다. 캐싱이 저장 계층과 보관 기간 문구를
이미 손대므로 그때 같이 판단하면 비용이 한 번만 든다. 그 전에 넣으면 저장 계층을 두 번 만든다.

## 도식과 애니메이션 (TASK-25)

결과 화면의 그림 넷은 `components/charts/` 에 있다 — 오행 분포 막대(`OhaengBars`),
상생·상극 오각형(`OhaengCycle`), 한열 눈금(`ThermalScale`), 대운 타임라인(`DaeunTimeline`).
**차트 라이브러리를 넣지 않는다.**

- **좌표가 필요한 것만 SVG 다** (오각형 하나). 막대·눈금·타임라인은 div 로 그린다 — 폭이
  좁아질 때 칸만 줄고 글자는 그대로 남는다. SVG 뷰박스로 하면 좁은 화면에서 라벨까지 같이
  작아져 못 읽는다.
- **점수를 숫자로 찍지 않는다.** 막대 길이·원 크기의 근거는 `ohaeng.score`(개수 × 계절 배수)
  인데 그 배수는 우리 관례다. 프롬프트에서 숫자 인용을 금지하는 것과 같은 이유로 **길이로만**
  쓰고, 화면에 적는 숫자는 사실인 `count` 다.
- 색은 시맨틱 토큰만 쓴다. SVG 의 `fill-*`·`stroke-*` 도 마찬가지이며
  `lib/design/tokens.test.ts` 의 검사 목록에 네 파일이 들어 있다.
- **공유 카드와 시각 언어를 맞춘다.** 화면이 막대를 쓰므로 카드(`draw-card.ts`)도 배지 아래
  같은 막대를 그린다 (`ShareCardBadge.weight`). 한쪽만 바꾸면 화면과 카드가 달라 보인다.

### 풀이가 먼저, 도식은 근거로 뒤에 (TASK-52 · 61 · 81)

결과 화면은 위에서부터 **판정 한 줄 → 풀이 → 좋아요 → 공유 → 근거 도식 → 다른 유형 링크**
순이다. 도식 셋
(`사주 원국` · `기운의 관계` · `대운 · 세운`)은 `내 사주` 묶음으로 **풀이 아래**에
있고, 그중 `기운의 관계`(오각형·한열)와 `대운 · 세운` 은 `ResultView.tsx` 의
`FoldCard`(`<details>` + `globals.css` 의 `.fold`)로 접혀 있다.

- **접는 것만으로는 부족했다.** TASK-52 는 도식 둘을 접었는데, 접힌 제목 줄 셋과 펼쳐진
  원국 카드가 여전히 본문보다 위에 있어서 **사람들이 보러 온 풀이가 화면 아래에서
  시작했다.** 그래서 TASK-61 이 순서를 뒤집었다. 접기와 순서는 **둘 다 필요했다.**
- **`ReadingSections` 의 "아코디언을 쓰지 않는다" 와 모순이 아니다.** 그쪽은 지금 써지고
  있는 글이라 접으면 스트리밍이 안 보인다. 도식은 요청 즉시 확정되고 그 뒤로 변하지
  않으므로 접어 둬도 놓치는 순간이 없다. **이 구분이 판단 기준이다** — 스트리밍으로
  채워지는 것은 접지 않는다.
- **원국 카드도 접는다** (TASK-73). 근거 카드 셋이 **모두 같은 모양**이다.
  - 예전에는 원국만 펼쳐 뒀다. 이유는 "무엇으로 이 풀이가 나왔는가 를 보여주는 카드라
    접으면 결과의 출처가 사라진다" 였는데, **TASK-61 이 그 카드를 본문 아래로 내린 뒤
    그 이유가 약해졌다** — 근거는 읽고 난 뒤에 확인하는 것이다. 접힌 제목 줄 둘 옆에서
    이 카드만 화면 한 뭉치를 차지하던 것이 실제 문제였다.
  - **출처는 `note` 가 지킨다** — `양력 {날짜} · {띠}` 다. 펴지 않고 알아야 하는 것은
    "내가 넣은 값으로 계산된 것이 맞는가" 이고, 그 확인이 이 카드가 근거로서 하는 일이다.
  - **자동 스크롤은 영향을 받지 않는다.** `SajuForm` 의 `resultRef` 는 원국 카드가 아니라
    결과 묶음 전체를 감싼 `div` 에 붙어 있다. 스크롤이 도착했을 때 보이는 것은 **콜아웃과
    풀이 첫 줄**이다. (예전에 여기 적혀 있던 "접으면 자동 스크롤이 겨냥할 것이 없어진다"
    는 처음부터 사실이 아니었다.)
  - **등장 애니메이션은 저절로 산다** — `FoldCard` 가 펼칠 때마다 `key={openCount}` 로
    내용을 remount 하므로 `OhaengBars` 도 펼치는 순간 재생된다.
- **좋아요는 공유 바로 위, 근거보다 앞이다** (TASK-81). `도움이 됐어요` 는 방금 읽은 글에
  대한 반응인데 예전에는 **근거 카드 셋 뒤**에 있어서 다 지나친 다음에야 나왔다. 읽고 →
  반응하고 → 공유하는 순서다. 둘은 `space-y-2` 한 묶음으로 감싼다 — 바깥 `space-y-6` 에
  나란히 두면 칩의 `py-2` 가 더해져 사이가 32px 로 벌어져 한 덩어리로 읽히지 않는다.
  **`OtherReadingLinks` 는 따라오지 않는다** — 그건 이 화면을 다 본 뒤의 동선이라
  `SajuForm` 맨 아래가 제자리다. 셋 다 `!streaming` 조건은 그대로다.
- **근거 카드는 넷이고 마지막이 `계산 기준` 이다** (TASK-101 · `components/CalculationBasis.tsx`).
  폼에 있던 `만세력 고급 설정`(출생시각 보정 · 자시 기준)을 여기로 내렸다.
  - **없앤 것이 아니라 옮긴 것이다.** 23시대 출생자는 자시 기준에 따라 **일주가 통째로**
    바뀌고, 다른 만세력과 결과가 다를 때 원인을 짚는 수단이 이것뿐이다. 지우면
    `app/disclaimer/page.tsx` 의 "바꿔 다시 볼 수 있습니다" 도 함께 거짓이 된다.
  - **옮긴 근거는 TASK-73 과 같다** — 근거는 읽고 난 뒤에 확인하는 것이다. 이 설정을
    만지고 싶어지는 순간은 폼을 채울 때가 아니라 **결과를 보고 다른 곳과 다를 때**다.
    아무도 폼 앞에서 "진태양시로 봐야지" 하고 생각하지 않는다. **폼으로 되돌리지 말 것.**
  - **`고급` 이라는 낱말을 쓰지 않는다.** 열면 안 되는 것처럼 읽히는데 23시대 출생자에게는
    필수 항목이다. `birth-input.test.ts` 가 폼과 카드 양쪽에서 0건을 검사한다.
  - **`note` 는 고른 값이 아니라 실제로 적용된 값이다** (`경도 보정 −32분 · 자정 기준`).
    폼에서는 계산 전이라 보여줄 수 없던 것이고, **이 카드를 열 이유가 있는지**를 펴지 않고
    판단하게 해 준다. 시각 미상이면 `시각 미상 · 시주 제외` 다 — 그때는 두 값 다 쓰이지
    않으므로 `자정 기준` 이라고 적으면 쓰이지도 않은 값을 적용된 것처럼 말하게 된다.
  - **바꾸면 자동으로 다시 받지 않는다.** 값이 바뀌면 화면의 풀이·원국이 옛 기준의 것이
    되므로 그 사실을 알리고 `이 기준으로 다시 보기` 버튼을 낸다. 즉시 재제출하지 않는
    이유는 둘을 함께 바꿀 때 **요청이 두 번** 나가기 때문이다 (무료 등급의 병목은 토큰이
    아니라 요청 수 500 RPD). 제출 경로는 `SajuForm.handleSubmit` 하나이고 카드는
    `onReapply` 로 그것을 부른다 — **두 번째 요청 경로를 만들지 말 것.**
  - **바뀐 것이 없으면 버튼을 내지 않는다.** 꺼진 버튼을 늘 띄우면 왜 눌리지 않는지
    설명할 자리가 또 필요해진다. 비교는 **실제로 쓰인 값**과 한다 — 시각 미상이면
    `pillars.ts` 가 보정을 강제로 꺼서 고른 값과 쓰인 값이 다르다.
  - **시각 미상이면 숨기지 않고 잠근다** — 숨기면 왜 보정이 안 됐는지 물을 자리가 없어진다
    (시주가 빠진 이유와 같은 이야기다).
  - **폼의 지역 잠김 문구가 이 카드를 가리킨다.** 보정 컨트롤이 폼 밖으로 나갔으므로
    잠긴 이유만 적으면 **어디서 풀 수 있는지** 알 수 없다. 잠금 자체를 푸는 쪽은 택하지
    않았다 — 고를 수 있게 두면 반영되는 줄 안다(TASK-37).
  - **캐시는 손댈 것이 없다.** `reading-cache.ts` 가 입력 스냅샷 전체를 훑으므로 두 값이
    바뀌면 통째로 비워진다 — 그 규칙이 있는 이유가 이런 경우다.
- **묶음 제목(`내 사주`)을 카드로 감싸지 않는다.** 카드 안에 카드가 되어 여백이 두
  겹이 된다. 이름이 있어야 이 카드들이 왜 뒤에 붙어 있는지 읽힌다.
- **묶음 이름은 `내 사주` 이고, 근거임을 말하는 일은 바로 아래 한 줄이 맡는다**
  (TASK-82 · `이 풀이는 아래 값에서 나왔습니다.`). 옛 이름 `이 풀이의 근거` 는 설명문처럼
  읽혔지만 **"이 카드들이 왜 본문 뒤에 있는가" 를 혼자 설명하고 있었다** — 이름만 바꾸면
  그 일을 아무도 하지 않는다. **둘 중 하나만 두지 말 것.** 카드 `note`(양력 날짜·한열·
  현재 대운)는 그 일을 못 한다 — 무엇이 들었는지만 말하고 왜 뒤에 있는지는 말하지 않는다.
- **묶음이 `h2`, 그 아래 카드 제목이 `h3` 다.** 제목 탐색으로 근거 카드 셋을 한 단계
  아래로 훑을 수 있어야 한다 — `FoldCard` 의 제목도 같은 `h3` 다.
- **접힌 줄에 `note` 한 줄을 함께 둔다** (한열 판정 · 현재 대운 간지). 제목만 있으면 무엇이
  들었는지 알 수 없어 결국 전부 펴 보게 된다 — 그러면 접은 의미가 없다.
- **`DaeunTable` 의 가로 가운데 맞춤은 `open` 을 의존성에 넣어야 한다.** 접힌 `<details>`
  안은 `display: none` 이라 `getBoundingClientRect()` 가 전부 0 이다. 마운트 때 한 번만
  계산하면 펼쳤을 때 현재 대운이 아니라 맨 왼쪽이 보인다.
- 화살표는 `.select-shell` 과 같은 방식이다 — 기본 마커를 지우고 `clip-path` 삼각형에
  시맨틱 토큰 색을 준다. **`summary` 에 `focus:ring-*` 을 붙이지 말 것**, 전역
  `:focus-visible` 이 이미 건다.

### 모바일이 기본값이다 (TASK-69)

주 사용자는 **모바일**이다. 반응형은 유지하되 **좁은 화면이 기본이고 넓은 화면이 파생**이다
(지금 Tailwind `sm:` 를 파생으로 쓰는 방향이 맞다). 기준 레퍼런스는 `docs/ui_ref/ref1.png`
(선택 화면) · `ref2.png`(결과 화면) · **`list_reference.jpg`(`/` 의 리스트 카드 · TASK-86)**
이다.

- **확인 수단이 먼저다.** `node scripts/screenshot.mjs` 로 진짜 390px 를 본다 (위 "컬러 토큰"
  절). 수단 없이 "모바일 우선" 을 선언하면 눈으로 확인하지 않은 채 작업하게 된다.
- **속도도 눈이 아니라 수단으로 본다.** `node scripts/measure-load.mjs <url>` 이 느린 회선을
  흉내 내 FCP·LCP 와 자산 도착 시각을 찍는다 (조건과 실측값은 아래 "배포" 절). 둘은 CDP
  플러밍(`scripts/lib/cdp.mjs`)을 나눠 쓴다.
- 한 화면에 맞추는 구성에는 **`dvh`** 를 쓴다 — 모바일 주소창 때문에 `vh` 는 튄다.
  하단에 고정되는 요소가 생기면 **`env(safe-area-inset-bottom)`** 을 함께 쓴다.

#### 레퍼런스에서 받은 것과 받지 않은 것 (2026-08-18 확정)

| 레퍼런스 | 판단 |
| --- | --- |
| 유형별 파스텔 **전면 배경** | **캐릭터가 앉는 면에만** 쓴다 (아래) |
| 상단 우측 **햄버거 메뉴** | **받지 않는다** |
| 상단 **가로 아이콘 줄 + 현재 항목 강조** | **받지 않는다** — 받았다가 되돌렸다 (TASK-71 → 93) |
| 화면 가운데 **큰 캐릭터** | **받지 않는다** — 받았다가 되돌렸다 (TASK-70 → 92) |
| **크림색 종이 슬립** 결과 카드 | **한 줄 결론 카드에만** |
| dasii 웹의 **3열 셸**(좌 패널 · 512px 열 · 우 균형추) | 받는다 (TASK-74) |
| dasii 웹의 **상단 헤더** | **받지 않는다** — 받았다가 되돌렸다 (TASK-74 → 91) |
| dasii 웹의 **하단 탭바** | **받지 않는다** — `usePathname()` 이 `/` 를 동적으로 만든다 |
| dasii 웹의 **QR 두 개 + 스토어 링크** | 받는다 · 자산은 우리가 서빙한다 (TASK-74) |
| `list_reference.jpg` 의 **리스트 카드 + 오른쪽 그림 + 원형 화살표** | 받는다 (TASK-86) |
| 같은 레퍼런스의 **강조색 윗줄 + 큰 제목 + 옅은 그라데이션** | 받는다 (TASK-86 · 제목은 한 줄 · TASK-103) |

- **유형 색은 캐릭터 뒤 면에만.** 전면 배경으로 가면 "유형별로 테마를 갈지 않는다"(확정
  결정)를 뒤집어야 하고, 파스텔 다섯 면 × 그 위 텍스트 대비 조합을 전부 `tokens.test.ts` 에
  넣어야 한다. 긴 본문이 그 배경 위에 얹히므로 대비 관리가 카드마다 두 겹이 된다.
  **캐릭터 면 위에는 글자가 올라가지 않으므로** 그 비용이 통째로 사라진다 — 그래서 확정
  결정을 뒤집지 않아도 된다. 늘어나는 것은 테마가 아니라 **장식 면 색 다섯**이다.
  `--raw-ohaeng-*-bg` 를 끌어 쓰지 말 것 (오행을 인코딩하는 값이다).
- **햄버거를 받지 않는 이유:** 열고 닫는 상태가 필요해 `/` 에 클라이언트 JS 가 들어가고
  **`/` 를 통째로 정적으로 두는 성질이 깨진다.** 그 버튼이 하는 일은 `BackLink` 와
  `SiteFooter` 가 이미 한다.
- **아이콘 줄이 `/` 가 아니라 `/reading/[type]` 인 이유:** `/` 에는 "선택된 유형" 이라는
  것이 없다(선택이 곧 이동이다). 상태를 들면 클라이언트가 된다. `/reading/[type]` 은
  **세그먼트로 현재 유형을 서버가 아는** 화면이라 클라이언트 JS 없이 강조할 수 있다.
  **`next/link` 여야 하고 폼 안에 두면 안 된다** — 유형 선택 *컨트롤* 금지는 그대로이고
  `birth-input.test.ts` 가 소스에서 막는다(링크는 그 경계 밖이며 `OtherReadingLinks` 가
  이미 같은 일을 한다).
- **결과 화면은 정보 구조를 받지 않는다.** 레퍼런스 결과는 한 줄 인용 한 장이고 우리 결과는
  2,000자 스트리밍이다. 종이 슬립 모양을 통째로 쓰면 긴 글이 그 안에서 읽히지 않는다.

### 유형 선택은 리스트 카드다 (TASK-64 → 85)

`/` 의 유형 선택이 한 줄에 한 장씩 쌓이는 **리스트 카드**다. 카드마다 왼쪽에 글(묻는 것 ·
제목 · 설명 · 조회수), 오른쪽에 사진, 오른쪽 아래에 원형 화살표가 있다. 기준 레퍼런스는
`docs/ui_ref/list_reference.jpg` 이고 **레이아웃과 디자인을 그대로 받았다**
(2026-08-19 사용자 확정).

**"`/` 를 없애고 폼을 첫 화면으로 올린다" 는 하지 않는다** (2026-08-19 사용자 확정).
하루 전 그렇게 정했다가(구 TASK-84) **레퍼런스를 받아 다시 만드는 쪽으로 뒤집혔다.**
그러니 고르는 화면이 한 겹 더 있다는 이유로 다시 없애려 하지 말 것 — 지금 `/` 는
"고르기만 하는 화면" 이 아니라 **무엇을 물어볼 수 있는지 보여주는 화면**이다.
그래서 "`/` 에는 클라이언트 컴포넌트가 없다" 도 그대로 산다.

**TASK-64 의 두 칸 그리드를 뒤집었다.** 그때 두 칸으로 둔 이유는 "공개 유형 다섯이 한 번
스크롤 안에 들어온다" 였고 **그 이유는 사라진 것이 아니라 감수한 것이다** — 지금은 다섯
장이 두 화면에 걸친다. 대신 카드 하나가 제목·설명·사진을 다 보여주므로 **고르는 데
필요한 것이 카드 안에서 끝난다.** 스크롤 길이와 카드당 정보량을 맞바꾼 것이다.

- **화살표를 되살렸다.** TASK-64 는 "카드는 면 전체가 링크라 화살표가 가리킬 방향이 없다"
  로 뺐는데, 리스트 카드에서는 **카드가 가로로 길어 오른쪽 끝이 줄의 끝**이 된다.
  레퍼런스가 거기에 원형 화살표를 두는 이유도 그것이다. `aria-hidden` 인 장식이고 링크
  이름은 제목이 만든다.
- **`li` 의 `display: contents` 를 걷어냈다.** 그건 두 칸 그리드에서 같은 줄 카드의 높이를
  맞추려던 장치다 — 한 줄에 한 장이면 맞출 상대가 없다.
- **설명 줄을 지우지 않는다** (TASK-64 에서 그대로 온다). 사진이 오른쪽을 먹어 글 폭이
  좁아지므로 세 줄로 자르되(`line-clamp-3`) **지우지는 않는다** — 거기가 `diet-food` 의
  컨셉 한 줄이 사는 곳이다. `sm:` 부터는 저절로 다 들어간다.
  - **`line-clamp-*` 에 `block` 을 함께 주지 말 것.** `line-clamp` 은 `display: -webkit-box`
    로 동작하는데 `block` 이 그걸 덮어 잘림이 통째로 죽는다. 실측에서 네 번째 줄이 카드
    밖으로 잘려 나갔다.
- **글 폭은 `100% - 사진 폭` 이다** (TASK-99 · 글 `58%` + 사진 `42%`). 예전 `62%` 는 합이
  104% 라 **설계상 겹쳐** 있었고 설명 줄이 사진 위로 4~12px 넘어갔다(390 · 360 · 1280px
  셋 다 · 좁은 화면만의 문제가 아니었다). 두 %가 **기준이 다른 것**에 주의할 것 — 글 쪽은
  카드 콘텐츠 폭(패딩 안쪽), 사진 쪽은 카드 폭이다. 그래서 58% 로 두면 왼쪽 패딩 20px 만큼
  어긋나 **어느 폭에서나 3.2px 가 일정하게** 남는다(카드 폭에 비례하지 않는다).
  - **눈으로는 잘 안 잡힌다.** 사진이 글 쪽으로 흐려지므로 겹친 자리가 흐린 면 위에 얹혀
    "조금 붙은" 것처럼 보인다. `birth-input.test.ts` 가 두 소스에서 %를 읽어 합을 본다.
  - **사진 마스크를 더 흐리게 해서 덮는 쪽으로 풀지 말 것** — 사진 색은 `tokens.test.ts`
    검사 밖이라 그 위 글자의 대비를 보증할 수 없다. 겹치지 않게 하는 것이 유일한 답이다.
- **글 묶음에 `break-keep` 을 준다.** 없으면 한글이 낱말 가운데서 끊겨 `다이어트 방/법` 이
  된다. 카드가 좁을수록 티가 난다.
- **묻는 것 한 줄(`READING_TYPE_QUESTION`)이 제목 위에 붙는다.** 레퍼런스의 강조색 윗줄
  자리다. 지금까지 `schema.ts` 주석과 이 파일에만 있던 유형 구분(몸이 어떤 쪽인가 / 왜
  붙는가 / 무엇을 어떻게 하는가 / 무엇을 먹는가 / 어떤 운동을 하는가)을 **화면에서 읽히게**
  한다 — 다섯 카드가 이름만으로는 서로 비슷해 보이던 것이 이 목록의 실제 문제였다.
  **라벨을 다시 쓴 것이 아니다**(라벨은 무엇을 주는지, 이 줄은 무엇을 묻는지) —
  `schema.test.ts` 가 둘이 겹치는지, 금지 어휘·숫자가 섞였는지, 한 줄에 들어갈 길이인지를 본다.
- **제목은 한 줄이다** (TASK-103). `<br />` 로 끊어 두 줄로 두던 것을 폈다. 390px 실측:
  글자 폭 310px / 열 폭 350px 로 들어간다. **크기는 `title-lg`(24px) 그대로이고 임의값을
  쓰지 않았다** — 타이포는 `globals.css` 의 세 단계가 단일 소스다.
  - **`whitespace-nowrap` 을 붙이지 않았다.** 더 좁은 폭에서는 그냥 두 줄로 접히는 것이
    맞다 — 안 접히게 막으면 글자가 열 밖으로 나가거나 가로 스크롤이 생긴다.
  - **좁은 폭·폰트 폴백 실측**(같은 날): 360px 은 웹폰트로 한 줄(310/320)이지만 **폴백
    폰트에서는 두 줄**이다(333/320). 느린 회선에서 약 1.9초 동안 두 줄로 보이다가 한 줄로
    접힌다는 뜻이다. 320px 은 두 폰트 다 두 줄이다. **390px 이 기준이라 그대로 뒀다** —
    거슬린다는 말이 나오면 문구를 한 글자 줄이는 쪽(`나의` → `내`)이 먼저다. 크기를
    `title-md` 로 내리면 히어로 제목이 `내 사주` 절 제목과 같은 크기가 된다.
- **머리 부분도 레퍼런스를 받는다** — 강조색 윗줄 + 큰 제목 + 위에서 옅게 시작해
  본문 흰 면으로 풀리는 그라데이션. 띠는 음수 여백으로 **콘텐츠 열 끝까지** 펼친다(본문
  폭에만 걸치면 잘린 사각형으로 보인다). 색은 시맨틱 토큰이고 **유형별로 갈지 않는다.**
- **`/` 는 여전히 클라이언트 JS 가 없다.** `next/image` 는 서버 컴포넌트에서 그대로 쓰이고
  빌드가 `/` 를 정적(ISR 5분)으로 유지하는지 표로 확인한다.
- `ul` / `li` 구조와 `aria-label` 은 그대로다 — `birth-input.test.ts` 가 소스에서 본다.

### 화면 1 구조 — 히어로 사진 (TASK-70 · 71 → 92 · 93 · 97)

`/reading/[type]` 상단이 **히어로 사진(맨 위) → 제목 → 한 줄 → 폼** 이고, 뒤로가기 `<` 는
**사진 위 좌상단에 얹힌다** (TASK-97).

- **사진이 콘텐츠 열의 맨 위·양 끝까지 닿는다.** `app/reading/[type]/page.tsx` 의
  `relative -mx-5 -mt-10` 이 `main` 의 여백(`px-5 py-10`)을 되돌린다. **뷰포트 폭이 아니라
  열 폭**이라 데스크톱 3열 셸에서 사진이 열을 넘지 않는다. 예전에는 `<` 아래에서 `mt-4`
  만큼 떨어져 시작해 **열 안에 얹힌 띠**로 보였고, `/` 카드에서 이어져 온 그림이 거기서
  한 번 끊겼다.
- **음수 여백과 `relative` 는 같은 요소에 있어야 한다** — 버튼을 사진 좌상단에 맞추려면
  위치 기준이 사진과 같은 상자여야 한다. 그래서 그 둘은 페이지가 들고 `ReadingHeroPhoto`
  는 슬롯 높이와 마스크만 맡는다. **펼치는 곳이 두 곳이면 두 겹이 되어 사진이 열 밖으로
  나간다** — `birth-input.test.ts` 가 소스에서 한 곳뿐인지 본다.
- **슬롯은 그대로 200px 이다.** 바뀐 것은 **자리**이지 크기가 아니라서 크롭
  (`object-position: 50% 80%`)·마스크·원본 480 유지 판단을 다시 잴 것이 없었다. 화면에서
  달라지는 것은 그 아래 전부가 **100px(= `py-10` + 버튼 줄) 위로 올라오는 것**이다.
- **마스크도 그대로다.** `.hero-photo` 의 타원은 중심이 위쪽 화면 밖(`50% -8%`)이라 값이
  세로로 비례한다 — 슬롯 크기가 같으면 알파 분포가 같다. 위 가장자리는 원래도 불투명해
  **직선이었고**, 그때는 그 위에 흰 면이 있어 경계로 읽혔다. 지금은 그 직선이 곧 화면의
  끝이라 경계가 아니다. 위 두 모서리만 옅게 풀려 사진이 위로 흘러 나가는 것으로 보인다.
- **`<` 는 `surface` variant 로 자기 면을 들고 간다** (`components/ui/Button.tsx`).
  사진 안의 색은 `tokens.test.ts` 가 닿지 않으므로 면 없는 `ghost` 로 두면 사진에 따라
  아이콘이 보이거나 안 보인다. **스크림을 까는 안은 버렸다** — 어느 사진에서도 통하려면
  진해야 하고, 그러면 띠 위쪽에 직선이 하나 생겨 방금 없앤 "얹힌 띠" 가 돌아온다.
  자리는 `left-5`(= `main` 의 좌측 여백 · 면을 든 버튼이라 원의 **왼쪽 끝**이 본문
  시작선과 맞는다) · `top-3` 이다. 규격 44px 원형은 `buttonClass({ size: "icon" })` 에서
  계속 온다.
- **글자를 사진 위에 얹는 것으로 넓히지 말 것.** 예외는 이 아이콘 하나이고, 그것도
  아이콘이 놓이는 면이 사진이 아니라 `canvas` 라서 성립한다. 제목·설명은 사진 **아래**다.
- 공개 다섯 유형을 390px 에서 눈으로 확인했다(2026-08-20). `diet-method` 처럼 상단이 밝은
  사진에서는 흰 원의 가장자리가 흐려지지만 **아이콘은 그대로 또렷하다** — 보증해야 하는
  것은 원의 테두리가 아니라 글리프다.

**유형별 캐릭터(`ReadingCharacter.tsx`)는 지웠다** (TASK-92). TASK-70 의 "화면 가운데 큰
캐릭터를 받는다" 를 뒤집은 것이니 왜인지를 남긴다.

- **`/` 가 사진으로 바뀌면서(TASK-86) 두 화면의 그림 언어가 갈라졌다.** 카드에서 사진을
  누르면 손그림 캐릭터가 나왔다 — **눌린 카드가 무엇이었는지가 끊겼다.** 지금은 같은
  그림이 이어지고, 그것이 히어로를 사진으로 바꾼 이유의 절반이다.
- **캐릭터는 유형마다 손그림을 새로 그려야 했다.** 유형이 늘 때마다 소재 하나를 고르고
  그것이 피사체 경계(신체 평가·특정 식품)에 걸리는지 매번 판단해야 했다. 사진 쪽은
  `scripts/fetch-card-photos.mjs` 가 이미 사람이 고르는 단계를 갖고 있다.
- **`--color-seat-*` 다섯과 `--color-character-*` 둘은 함께 지웠다.** 쓰는 곳이 그
  컴포넌트뿐이었다 — 쓰는 곳 없는 토큰은 다음 사람이 아무 데나 쓴다. `tokens.test.ts` 의
  "캐릭터 면" 검사도 같이 빠졌다(없는 토큰을 재는 검사가 된다). 유형 톤을 다시 면으로
  내리려면 그때 새로 정할 것이고, **`--raw-ohaeng-*-bg` 를 끌어 쓰지 않는다**는 판단은
  그대로다(오행을 인코딩하는 값이다).
- **되붙이려면 위 셋을 어떻게 치를지 먼저 답할 것.** "손그림이 따뜻하다" 는 답이 아니다.

히어로 사진의 규칙은 아래 "유형 사진 — 두 화면이 같은 그림을 쓴다" 절과 `components/ReadingHeroPhoto.tsx` 에 있다.
- **유형 줄은 없앴다** (TASK-93 이 TASK-71 을 되돌렸다). 레퍼런스의 가로 아이콘 줄을 받은
  것이었는데, 다섯 칸이 제목 바로 위에서 같은 굵기로 늘어서 **지금 고른 유형이 무엇인지가
  오히려 흐려졌다.** 그 자리에 `<` 버튼 하나만 둔다 (위 "홈으로 돌아가는 동선").
  - **`READING_TYPE_SHORT` 는 함께 지웠다** — 쓰는 곳이 그 줄뿐이었다. 되살릴 때는 짧은
    이름이 왜 필요한지(전체 라벨은 390px 에서 넘친다)부터 다시 답할 것.
  - **결과 전에 다른 유형으로 갈 길은 `<` → `/` → 카드 다섯**이다. 카드에는 사진·묻는 것·
    설명이 함께 있어 고르는 데 필요한 것이 그 화면에서 끝난다. 결과 뒤 동선은
    `OtherReadingLinks` 가 그대로 들고 있으므로 **풀이 캐시(TASK-60)의 존재 이유는 흔들리지
    않는다** — `diet` → `gain-cause` → 다시 `diet` 왕복이 그쪽에서 산다.
- **유형 선택 *컨트롤* 을 폼 안에 두지 않는 경계는 그대로다** (`birth-input.test.ts` 가 폼
  안을 본다). 줄이 없어져도 그 검사는 계속 산다 — 되살린다면 폼 밖의 `next/link` 여야 한다.
- **CTA 는 검정 면 · radius 12px 이다** (TASK-75 에서 뒤집혔다). TASK-71 은 필 모양 +
  `brand-solid` 였고 이유는 "레퍼런스의 검정 필을 쓰려면 새 토큰과 대비 검증이 필요하다"
  였는데, **재보니 흰 글씨 17.46:1 로 통과해 그 유보 사유가 사라졌다.** 원시값도 새로
  만들지 않았다 — `--raw-gray-900`(#181a1b)이 저쪽 #171717 과 채널당 1~4 차이다.
  모양·색·상태는 전부 `components/ui/Button.tsx` 가 정한다. 비활성은 여전히 면을 비운다.
- **중앙 정렬은 제목과 한 줄 설명까지만.** 레퍼런스가 중앙 정렬인 것은 한 줄 인용이라서다 —
  본문 문단을 가운데로 놓으면 줄 시작점이 흔들려 읽기가 나빠진다.
- **웹폰트는 제목 자리에만 한 벌 넣었다** (TASK-75 에서 부분적으로 뒤집혔다). TASK-71 은
  아예 넣지 않았고 이유는 "한글 웹폰트는 무겁고 모바일 첫 화면 속도를 직접 깎는다" 였다.
  그 이유는 **본문에 대해서는 그대로 유효하다** — 본문에 얹으면 2,000자 스트리밍이 폰트
  로드에 묶인다. 지금 받은 것은 굵기 800 한 벌(112KB)이고 **제목만** 쓴다. 시스템 한글
  폰트에 800 이 아예 없어서 그 자리만 메운 것이다. 근거·실측·라이선스는
  `public/fonts/README.md`. **본문으로 넓히거나 굵기를 더하지 말 것.**

### 유형 사진 — 두 화면이 같은 그림을 쓴다 (TASK-50 → 85 → 92)

**같은 사진이 두 자리에 나온다** (파일은 `public/cards/<type>.jpg`).

| 자리 | 컴포넌트 | 모양 |
| --- | --- | --- |
| `/` 리스트 카드 오른쪽 면 | `ReadingCardPhoto` | 42% 폭 · `.card-photo` 로 **왼쪽**으로 흐려진다 |
| `/reading/[type]` 상단 히어로 | `ReadingHeroPhoto` | 열 폭 · 200px · **열 맨 위부터** · `.hero-photo` 로 **아래·좌우**로 풀린다 |

**경로 표는 `lib/reading/type-photo.ts` 한 곳에 있다** (`READING_TYPE_PHOTO`). 두 벌로 두면
사진을 갈아끼울 때 한쪽만 바뀌어 **`/` 에서 고른 카드와 들어간 화면의 그림이 달라진다** —
그 이어짐이 히어로를 사진으로 바꾼 이유의 절반이다.

예전의 인라인 SVG 모티프(`ReadingThumbnail`)와 캐릭터(`ReadingCharacter`)는 **둘 다 지웠다** —
앞의 것은 카드가 리스트로 바뀌며 56px 자리가 없어졌고(TASK-86), 뒤의 것은 위 "화면 1 구조"
절의 이유로 히어로 사진에 자리를 넘겼다(TASK-92).

**TASK-50 이 사진을 뺀 이유 셋 중 하나만 사라졌다.**

| 옛 근거 | 지금 |
| --- | --- |
| 56px 에서 사진은 알아볼 수 없는 색 덩어리다 | **사라졌다** — 슬롯이 150px 다 |
| 피사체가 이미 막아 둔 경계에 걸린다 | **그대로 남는다** → 아래 피사체 규칙 |
| 래스터는 `tokens.test.ts` 검사 밖이다 | **그대로 남는다** → 값으로 치른다 |

- **피사체는 사람 없는 정물이다.** 사람 몸은 `내가 살이 찌는 이유` 옆에서 신체 평가가 되고,
  특정 음식 한 접시는 `ELEMENT_FOOD` 닫힌 목록을 **판정 코드 밖에서** 우회한다.
  **식사 도구까지가 경계다**(그릇·냄비 — 옛 캐릭터 규칙과 같은 선). **운동 기구는 쓴다** —
  TASK-48 이 대표 종목을 콕 집어 권하는 유형을 열었으므로 그림이 본문보다 더 말하지 않는다.
  **히어로가 되면서 이 경계가 더 중요해졌다** — 같은 그림이 150px 슬롯에서 열 폭짜리
  띠로 커졌다. 실제로 `gain-cause` 는 벽에 걸린 초상 액자가 있어 이 규칙에 가장 가까이
  붙어 있던 한 장인데, 히어로의 크롭(아래)이 그것을 화면 밖으로 밀어낸다.
- **팔레트 검사 밖인 것은 감수한 것이다.** 그래서 **사진이 화면의 색을 정하지 않게** 둔다 —
  카드 면·글자·화살표는 전부 시맨틱 토큰이고 사진은 오른쪽 끝에서 흐려지는 장식이다.
  톤이 어긋나면 그건 색 규칙이 아니라 **고르기**의 문제다.
- **흐려지는 마스크는 `globals.css` 의 `.card-photo` 다.** 사진을 그냥 얹으면 세로 경계선이
  하나 생겨 카드가 두 칸으로 쪼개져 보인다. `-webkit-` 접두사를 함께 둔다(없으면 옛 사파리
  에서 마스크가 통째로 무시된다). 마스크의 검정은 팔레트 값이 아니라 **알파 마스크**다.
- **글은 사진과 겹치지 않는 폭에 둔다.** 흐려지는 구간에 글자가 얹히면 대비를 보증할 수
  없다 — 사진 색은 검사가 닿지 않는다. **히어로도 같다** — 제목(`h1`)과 한 줄 설명은
  사진 **아래**에 있고 위에 얹지 않는다. **예외는 뒤로가기 `<` 하나뿐이고**(TASK-97)
  그것도 `surface` variant 로 자기 면을 들고 가서 아이콘이 사진 위에 직접 놓이지 않는다
  (위 "화면 1 구조 — 히어로 사진").
- **히어로 마스크는 `.hero-photo` 이고 `.card-photo` 를 재사용하지 않는다.** 방향이 다르다
  (왼쪽 vs 아래·좌우). **레이어 하나로 끝낸다** — 아래로 흐려지는 것과 좌우로 흐려지는
  것을 두 겹 쌓으면 `mask-composite` 가 필요하고 그 키워드가 브라우저마다 달라 **한쪽에서
  사진이 통째로 사라진다.** 위쪽에 중심을 둔 타원 하나가 세 방향을 한 번에 만든다.
- **히어로의 크롭 위치도 `.hero-photo` 가 정한다** (`object-position: 50% 80%` · 한 값).
  정사각 원본을 열 폭에 채우면 세로로 절반가량만 보이는데, 가운데로 두면 **바닥·책상에
  놓인 피사체가 아래 흐려지는 구간에 걸려 반쯤 사라진다**(`exercise` 신발 · `diet-method`
  공책). 390px 실측으로 80% 를 골랐다. **유형마다 다른 값을 주지 말 것** — 유형 수만큼
  칸이 늘어나는 표가 하나 더 생기고, 사진을 갈아끼울 때마다 다시 재야 한다. 한 사진이
  이 값에서 안 맞으면 그건 배치 문제가 아니라 **고르기**의 문제다.
  - **마스크를 느슨하게 해서 아래를 살리는 쪽은 재보고 버렸다** — 피사체는 살지만 띠의
    아래 경계가 직선으로 드러나 "얹힌 띠" 가 된다. 그게 히어로로 쓰면서 피하려던 모습이다.
- **히어로에는 `priority` 를 준다** (실측 · 2026-08-20). 이 사진이 `/reading/[type]` 의
  **LCP 요소**이고 390px · 1.6Mbps 프로덕션 빌드에서 **LCP 1.40초 → 0.69초** 다.
  사진을 열 맨 위로 올린 뒤(TASK-97) 같은 조건에서 **0.63초**로, 슬롯 크기가 같아 값이
  거의 그대로다 (아래 "배포" 절).
  `/` 는 "첫 카드만" 이라는 단서가 붙지만 이 화면에는 사진이 하나뿐이라 preload 가
  서로를 밀어낼 상대가 없다.
- **`alt=""` 인 장식이다.** 링크 이름은 카드 제목이 만든다. 그래서 촬영자 표기도 화면이
  아니라 `public/cards/CREDITS.md` 에 둔다(Pexels 라이선스는 표기를 요구하지 않지만 API
  가이드라인이 권한다).
- **자산은 우리 도메인에서 서빙한다.** Pexels URL 을 직접 물면 방문자 브라우저가 제3자에
  요청을 보낸다 — `public/dasii/` QR 두 개와 같은 판단이다. 파일은 커밋한다.
- **`Record<ReadingType, …>` 다.** 배열이면 새 유형이 조용히 남의 사진을 달고 나간다
  (옛 모티프가 `Record` 였던 이유가 그대로 적용된다).
- **사진은 `scripts/fetch-card-photos.mjs` 로 받는다.** 두 단계다 — 후보를 임시 디렉터리에
  받아 **눈으로 고르고**(`--pick=diet:3,...`), 고른 것만 `public/cards/` 로 확정하면서
  `photos.json` · `CREDITS.md` 를 다시 쓴다. 첫 장을 자동으로 물리면 세트 톤이 깨진 채로
  화면까지 간다(`docs/cardnews/fetch-photos.mjs` 와 같은 판단).

  ```bash
  node --env-file=.env.local scripts/fetch-card-photos.mjs --count=6            # 후보
  node --env-file=.env.local scripts/fetch-card-photos.mjs --only=exercise      # 한 유형만
  node --env-file=.env.local scripts/fetch-card-photos.mjs --pick=exercise:3    # 확정
  ```

  - **크기는 받을 때 정한다** (`SIZE = 480`). Pexels 는 `w`/`h`/`fit` 만 받고 **화질(`q`)은
    먹히지 않는다**(q=72 와 q=50 이 같은 바이트 수). 640 으로 받으면 필름 그레인이 있는
    사진 하나가 590KB 였다(480 에서 326KB). **이 크기는 저장소 용량 이야기이지 전송량이
    아니다** — 화면에 가는 것은 `next/image` 가 다시 인코딩한 8~33KB 짜리 webp 다
    (실측은 아래 "배포" 절). 480 은 카드 슬롯에서 DPR 3 까지 덮는 하한이라 더 줄이면 흐려진다.
  - **히어로 슬롯에는 480 이 모자란다 — 그래도 올리지 않았다** (실측 · 2026-08-20).
    열 폭을 채우므로 DPR 2 에서 780 · DPR 3 에서 1,170 픽셀이 필요하고, 눈으로 재보니
    **DPR 2 는 멀쩡하고 DPR 3 에서 `diet-food` 냄비의 꽃무늬가 무르다.** 800 으로 올리면
    ① 저장소가 484KB → 약 1.33MB 가 되는데 **거의 전부 `diet-food` 몫이다**(그 원본만
    Pexels 가 PNG 로 준다 · 480/800/960 = 327/945/1383KB), ② **전송량도 오른다** —
    지금은 최적화기가 원본 폭에서 멈춰 사용자가 13KB 를 받는다. 이 화면에서 급한 것은
    사진이 아니라 **폼**이라 그대로 뒀다. **DPR 3 의 무름이 거슬린다는 말이 나오면
    그때 올린다** — 위 두 값을 함께 치르는 결정이라는 것만 기억할 것.
  - **`size=large` 를 걸지 말 것.** 후보 풀이 확 줄어 검색어와 상관없는 사진이 올라온다 —
    `dumbbells` 검색에 성운 사진과 고양이가 나왔다.
  - **검색어에 사람이 섞이는 것이 기본값이다.** 운동·요가 검색은 거의 전부 인물 사진이라
    후보를 늘려도 안 걸러진다. 정물이 나오는 낱말(신발·매트·기구)로 좁혀야 한다.
  - **`.next/dev/cache/images` 를 지워야 새 사진이 보인다.** 파일을 갈아끼워도 dev 서버가
    옛 최적화 결과를 돌려준다 — `.next/cache/images` 가 아니라 **`dev`** 쪽이다.
    (실제로 여기서 한참 헤맸다: 원본은 새것인데 화면만 옛것이었다.)

### 화면 사이 전환은 CSS View Transitions 다 (TASK-96)

`/` 의 카드를 누르면 상세가 **오른쪽에서 밀려 들어오고** 맨 위 `<` 로 나가면 반대로 빠진다.
`/` 의 카드 사진과 `/reading/[type]` 의 히어로 사진이 같은 그림인데(TASK-92) 두 화면이 툭
갈려 그 이어짐이 전환에서 끊기던 자리다.

- **`/` 에 클라이언트 JS 를 들이지 않는 것이 이 구현의 축이다.** 라우터 전환에 JS 상태를
  들면 `/` 가 동적이 되어 "유형 선택 화면은 통째로 정적" 이 깨진다. 그래서 React 의
  `<ViewTransition>` (서버 컴포넌트에서 그대로 쓰인다) + `globals.css` 로만 만든다.
  빌드 표에서 `/` 가 여전히 `○ (Static) · Revalidate 5m` 인지 확인할 것.
- **감싸는 자리는 레이아웃이 아니라 페이지다.** 레이아웃은 이동에서 다시 렌더되지 않아
  `enter`/`exit` 가 아예 일어나지 않는다 (`BirthInputProvider` 를 루트 레이아웃에 둔 이유의
  뒷면이다). `components/PageTransition.tsx` 를 **참여하는 페이지가 각자** 쓴다 —
  한쪽만 감싸면 절반짜리 전환이 된다.
- **방향은 링크가 말한다.** `transitionTypes={[NAV_FORWARD]}` / `[NAV_BACK]` 이고 자동으로
  정해지지 않는다. **앞뒤가 같은 방향이면 전환이 앞뒤를 구분해 주지 못한다.**
- **세 곳이 같은 문자열을 써야 산다** — 링크의 `transitionTypes` · `PageTransition` 의 유형
  표 · `globals.css` 의 `::view-transition-*(.nav-*)`. 어긋나면 오류가 아니라 **아무 일도
  일어나지 않고**, 전환은 이동하는 찰나에만 보여서 스크린샷에도 안 남는다.
  `motion.test.ts` 가 셋을 대조하고 방향이 서로 반대인지까지 본다.
- **두 고지 페이지의 `BackLink` 에는 방향을 달지 않는다.** 그쪽은 `PageTransition` 으로
  감싸지 않았고, 한쪽만 달면 나가는 화면은 가만히 있는데 들어오는 화면만 미끄러진다.
- **셸(좌측 패널·푸터·흰 열)은 움직이지 않는다.** 이름 없는 것은 전부 `root` 한 장으로
  찍히는데 그것까지 크로스페이드하면 화면 전체가 흔들려 무엇이 움직였는지 읽히지 않는다.
- **`::view-transition-*` 는 모션 최소화의 `*` 규칙에 잡히지 않는다** — 문서 안의 요소가
  아니라 브라우저가 전환 중에만 만드는 가짜 요소다. 그 블록에 따로 적어야 하고
  `animation-delay` 도 함께 지운다(안 그러면 들어오는 화면이 150ms 늦게 나타난다).
  키프레임 `from` 규칙(아래)이 여기서도 그대로 값을 한다 — `to` 로 최종값을 적으면
  모션 최소화에서 **빈 화면에 갇힌다.**
- **전환 겹장은 기본적으로 포인터를 가로챈다.** `::view-transition { pointer-events: none }`
  이 없으면 전환 중 클릭이 통째로 사라진다.
- **`@types/react` 는 `ViewTransition` 타입을 `react/canary` 에 숨겨 둔다.**
  런타임에는 되는데 `tsc` 만 모른다 — `types/react-canary.d.ts` 한 줄이 그것을 끌어온다.
- **눈으로만 확인할 수 없는 것이 아니다.** CDP 로 `startViewTransition` 을 감싸
  `ready` 직후 `document.getAnimations()` 를 찍으면 어느 겹장이 어떤 키프레임으로 도는지
  다 보인다(모션 최소화 에뮬레이션까지). 전환 200ms 지점 스크린샷이 방향을 보여준다.
- **속도는 그대로다** (2026-08-20 실측 · `measure-load.mjs` · 390px · 1.6Mbps · 3회 중앙값):
  `/reading/diet` LCP 0.68초(기준선 0.69) · `/` LCP 0.66초(기준선 0.72).

### `/` 는 위에서부터 차례로 떠오른다 (TASK-104)

머리 띠와 리스트 카드 다섯이 `.anim-card-rise` 로 **위에서 아래로** 60ms 씩 늦게
등장한다(마지막 카드 300ms · 재생 450ms). 목록을 훑는 방향과 같아 순서가 그대로 읽힌다.

- **라이브러리를 넣지 않는다.** 서버 컴포넌트가 인라인 `animationDelay` 만 박아 내보내면
  `/` 는 여전히 정적(ISR 5분)이다. 도식 넷이 이미 그 방식이고, 빌드 표로 확인한다.
- **`opacity` 로 시작하면 안 된다 — LCP 가 무너진다** (실측 · 2026-08-20).
  크롬은 요소가 **처음 그려질 때** LCP 후보로 등록하는데 그때 투명하면 그 요소는 후보에서
  빠지고 **다시 들어오지 않는다.** 카드에 `opacity: 0` 출발을 걸면 다섯 장이 통째로 빠지고
  LCP 가 **웹폰트를 기다리는 `h1`** 으로 떨어진다 — **0.72초 → 2.48초.**
  그래서 이 키프레임은 **`transform` 만** 쓴다(12px 올라오기). `motion.test.ts` 가
  `card-rise` 에 `opacity` 가 없는지 검사한다. **눈으로는 절대 안 잡히는 회귀다.**
- **`.anim-rise` 를 재사용하지 않는다.** 그쪽 6px 은 8px 글자 옆에서 잰 값이라 160px 짜리
  카드에서는 움직였는지 알 수 없다.
- **모션 최소화에서는 지연도 0 이다.** 재생 시간만 줄이면 `both` 때문에 지연 동안
  `from`(숨은 상태)에 갇혀 **여전히 시차를 두고 튀어나온다.** 그래서 `*` 규칙에
  `animation-delay: 0s !important` 를 함께 뒀고 **도식 넷의 지연도 같이 0 이 된다** —
  그쪽이 오히려 이 설정이 요구하는 동작이다. CDP 로 확인했다(여섯 요소 delay 0 · dur 0).
- **스크린샷으로는 확인할 수 없다** — 정지 화면은 최종 모습만 보여준다. CDP 의
  `Animation.setPlaybackRate` 로 늦추거나 `document.getAnimations()` 로 지연·재생 시간을
  찍어 본다.

### 애니메이션 규칙 — 어기면 조용히 깨진다

`lib/design/motion.test.ts` 가 아래 둘을 소스에서 검사한다.

1. **키프레임은 `from` 만 쓴다.** 쉬는 상태(요소의 기본 스타일)가 최종 모습이고 애니메이션은
   숨은 상태에서 출발하기만 한다. `to` 로 최종값을 지정하면 `prefers-reduced-motion` 이
   재생 시간을 0.01ms 로 줄이는 순간 **숨은 상태에 갇혀 화면이 비어 보인다.**
   그래서 모션 최소화 대응에 JS 분기가 필요 없다.
2. **재생이 한 번인 것도 CSS 에 맡긴다.** 스트리밍 중 컴포넌트는 조각마다 다시 렌더되지만,
   CSS 애니메이션은 같은 요소에 남아 있는 동안 다시 시작하지 않는다. 대신 **요소가 remount
   되면 재생되므로 섹션 `key` 를 안정적으로 유지해야 한다** — `ReadingSections` 의 key 는
   제목이 아니라 **id** 다. 계약에 없는 제목은 글자가 도착하는 대로 늘어나서, 제목을 key 로
   쓰면 조각마다 remount 되고 글이 떨린다.

`anim-*` 클래스는 `globals.css` 에 키프레임과 나란히 정의한다. Tailwind 임의값
(`[animation:…]`)으로 흩뿌리지 말 것 — 테스트가 정의/사용 양쪽을 대조한다.

`chart-draw`(선이 그려지는 효과)를 쓰려면 `stroke-dasharray` 가 **선 길이와 같아야** 한다.
짧으면 점선이 되고 길면 다 그려지기 전에 끝난다. `OhaengCycle` 은 좌표에서 길이를 계산해
`--draw-length` 로 넘긴다. **점선 자체가 의미인 선(상극)에는 쓸 수 없다** — dashoffset 을
움직이면 점선이 미끄러진다. 그쪽은 서서히 나타나게 한다.

## 보안 규칙 (env / 키)

- `NEXT_PUBLIC_` 접두사는 **브라우저로 노출된다.** 키·시크릿에 절대 붙이지 않는다.
- 커밋 대상은 `.env.example` **하나뿐**이다. `.env`, `.env.local` 등은 `.gitignore` 처리되어 있다.
- 에러 응답·로그에 키 원문을 넣지 않는다. 로그는 `lib/gemini.ts` 의 `toSafeLogMessage()` 로 마스킹하고,
  디버깅이 필요하면 `maskedApiKey()` 를 쓴다.
- 클라이언트로 내보내는 에러는 일반화된 문장만 쓴다. 스택·원문 메시지·설정 상세는 서버 로그에만 남긴다.
- 사용자 생년월일은 개인정보다. **저장하지 않는다.** 로그에도 남기지 않는다.
  `/api/*` 는 `Cache-Control: no-store` 로 응답한다 (`next.config.ts`).
- 키가 노출되면: Google AI Studio 에서 즉시 폐기 → 재발급 → Vercel 환경변수 갱신 → 재배포.

### env 계층이 두 개로 나뉘어 있는 이유

`lib/env.ts` 는 `getSecrets()`(키, 없으면 `EnvError`)와 `getRuntimeConfig()`(모델명·레이트 리밋, 기본값 있음)를
분리한다. 레이트 리밋 같은 앞단 로직이 "키 없음" 때문에 먼저 터지면 입력 검증 결과가 가려지기 때문이다.
검증은 import 시점이 아니라 **최초 호출 시점(lazy)** 이다 — 키 없이도 `next build` 는 성공해야 한다.

## LLM (Gemini) 취급

- 모델은 `GEMINI_MODEL` 로 주입되며 기본값은 **`gemini-3.5-flash-lite`** 다.
  **`gemini-2.5-*` 는 신규 API 키에 404 NOT_FOUND 로 폐지됐다.** 되돌리지 말 것.
  **Pro 모델은 2026-04 부터 유료 전용**이다. 무료 키로 pro 로 바꾸면 호출이 실패한다.
- **`thinkingLevel` 은 `MINIMAL` 고정이다** (TASK-54 로 `gemini-3.5-flash-lite` 에서
  2026-08-18 재실측 · 근거는 `lib/gemini.ts` 주석에 표로 있다). 예전 근거는 다른 모델
  (`gemini-3.5-flash`)에서 잰 값이었다. **수치를 옮겨 적을 때는 모델명과 측정일을 함께
  적을 것** — 이번에 세 곳이 어긋난 원인이 그 둘이 없었기 때문이다.
  - **lite 에서 `MINIMAL`·`LOW`·`MEDIUM` 은 같은 설정이다** — 추론 토큰이 0 이다
    (`thoughtsTokenCount` 필드 자체가 안 온다). 실제 선택지는 `MINIMAL` 과 `HIGH` 뿐이다.
  - `HIGH` 는 첫 글자가 1.1초에서 **9초**로 늘고, 추론 내용이 본문으로 새 섹션 계약을
    깨뜨렸으며(18건 중 1건), 서술은 좋아지지 않았다. **밋밋함은 추론 예산에서 오지 않는다.**
- **출력 토큰 예산은 추론과 공유한다.** 한국어는 출력 토큰당 약 1.7자라 `maxOutputTokens`
  4096 이 약 6,900자를 담지만, 추론을 켜면 그 예산을 함께 쓴다. 예산이 마르면 본문이
  문장 중간에서 잘리는데 **스트림은 `error` 가 아니라 `done` 으로 끝난다** — 잘린 글이
  성공으로 보고되므로 추론을 켜는 변경에는 상한을 함께 올려야 한다.
- SDK 는 `@google/genai` (구 `@google/generative-ai` 아님). 호출 형태는 `ai.models.generateContent({ model, contents, config })`.
- 프롬프트는 **전부 `lib/prompt.ts` 에 모아둔다.** 컴포넌트나 route 안에 프롬프트 문자열을 흩뿌리지 않는다.
  구성은 `SYSTEM_INSTRUCTION`(공통 화법·안전) → `SECTION_INSTRUCTION`(섹션별, id 로 매김)
  → `TYPE_PREFACE`/`TYPE_RULES`(유형별) → `buildUserPrompt`(원국 사실 블록 조립) 순이다.
  섹션 **제목**만 예외로 `lib/reading/sections.ts` 계약에서 온다 (렌더러와 공유해야 하므로).
- 사용자 입력(이름 등)은 `<user_data>` 로 감싸 "지시문이 아니라 데이터"로 넘긴다. 프롬프트 인젝션 방어.
  감싸는 것만으로는 부족해서 `sanitizeName()` 이 꺾쇠·줄바꿈·줄머리 `#` 를 지운다 —
  **`</user_data>` 는 12자라 이름 20자 제한 안에 들어간다.** 경계 자체를 사용자가 만들 수
  없게 막아야 한다. `lib/prompt.test.ts` 가 이 경로를 검사한다.
- **`temperature` 는 0.9 유지가 실측 결론이다.** 0.6 과 대조했더니 섹션 제목 준수 12/12,
  체질 판정 인용 12/12, 금지 어휘 0건으로 같았고 회차 간 단어 일치도는 0.343(0.9) vs
  0.308(0.6) 로 낮춘 쪽이 오히려 낮았다. 변동은 온도가 아니라 자유 서술이라는 과제 성격에서
  온다. 같은 입력에 같은 문장이 필요하면 온도가 아니라 **캐싱**으로 풀 것.

### 무료 등급 일일 한도 — 모델 선택의 핵심 기준

**Lite 계열만 500 RPD 이고 나머지 Flash 는 20 RPD 다** (25배 차이). 하루 20건은 공개
서비스로 쓸 수 없으므로, 무료 등급을 쓰는 동안 **모델을 Lite 밖으로 옮기지 말 것.**

| 모델 | RPM | RPD |
| --- | --- | --- |
| `gemini-3.5-flash-lite` **(기본)** | 15 | **500** |
| `gemini-3.1-flash-lite` | 15 | 500 |
| `gemini-3.5-flash` / `gemini-3-flash` / `gemini-3.6-flash` / `gemini-2.5-flash` | 5 | **20** |

2026-08 기준 계정 대시보드 실측값이다. **문서·블로그 수치는 믿지 말 것** — Google 이
공식 문서에서 무료 등급 표를 내렸고, 남아 있는 글들은 대개 옛 수치(2.5-flash 250 RPD 등)다.
확인처는 두 곳뿐이다: https://aistudio.google.com/rate-limit 대시보드, 또는 429 응답의
`quotaId`/`quotaValue`.

품질 비교도 했다. 실제 프롬프트로 3건을 돌려 Flash 와 Flash-Lite 를 대조했더니 근거 인용
7개 항목이 동일하게 충족되고 Lite 가 오히려 빨랐다 (4.5~5.0초 vs 7~8초). 원국은 코드가
계산하므로 모델과 무관하게 같다.

- `lib/rate-limit.ts` 의 IP 제한은 **계정 단위 한도와 별개**다. IP 제한을 통과해도
  계정 쿼터가 소진되면 429 가 난다.
- 429 문구는 분당/일일을 구분한다. `quotaId` 에 `PerDay` 가 있으면 "내일 다시",
  아니면 "1분 후 다시" 다. 일일 소진에 "1분 후" 라고 하면 거짓말이 된다.

## 도메인 주의사항

### lunar-javascript 를 그대로 믿지 말 것

실측으로 확인한 결함 3가지를 `lib/saju/` 가 보정한다. 근거와 재현법은
[docs/saju-validation.md](docs/saju-validation.md) 에 있다. **이 보정을 되돌리면 결과가 틀린다.**

1. **절기를 베이징 시간(UTC+8)으로 계산한다.** 한국 시각을 그대로 넣으면 월주 경계가 1시간
   어긋난다. → 년주·월주는 `instant.beijingClock` 을 넣어 같은 좌표계에서 비교한다.
2. **`sect` 가 일주에만 적용되고 시주에는 반영되지 않는다** (`lunar.js` 의 `_computeTime`).
   기본값 sect=2 면 일주는 자정 기준인데 시주는 23시 기준 일간을 써서 학파가 섞인다.
   → 시주는 `ganji.ts` 의 오서둔으로 직접 계산한다. 라이브러리의 `getTime*()` 을 쓰지 말 것.
3. **진태양시·서머타임·표준자오선 변경 개념이 없다.** → 일주·시주는 보정된 지방시로 넣는다.

### 고전 규칙과 우리 관례를 섞지 말 것

명리학은 학파마다 기준이 다르다. 코드가 만든 "사실"이 실제로는 임의의 선택인 경우
그것을 사실처럼 내보내면 안 된다. `lib/saju/analysis.ts` 가 두 종류를 나눠 담는다.

- **고전 규칙**(이견 없음): 지지 십신(본기 기준), 지장간 표(`JI_JANGGAN`), 왕상휴수사,
  신강신약 3기준(득령·득지·득세), 대운 순행/역행, 대운·세운 간지
- **우리 관례**(상수로 분리): 오행 점수 배수(`SEASONAL_MULTIPLIER`), 신강신약 4단계 표현,
  대운수 반올림

#### 지장간 통근은 신강신약 판정에만 쓴다 (TASK-32)

`isRootedIn` 이 지지의 **지장간 전체**에 일간을 돕는 천간(비겁·인성)이 있는지 본다.
본기만 보면 숨은 뿌리를 놓쳐 신약 쪽으로 치우친다.

- **표시는 언제나 본기다.** `jiSipsin` 은 그대로 두고 화면·프롬프트의 지지 십신도 그대로다.
  표시까지 바꾸면 근거 표시가 흔들린다. 그래서 "일지 편재인데 득지 ○" 가 나올 수 있고,
  `strength.rooted` 가 어느 자리에 뿌리가 섰는지 함께 낸다 (화면과 프롬프트가 그 줄을 쓴다).
- **득령은 본기 그대로다.** 월령은 그 달을 지배하는 기운이라 숨은 글자가 아니다. 세 기준을
  전부 열면 신강 계열이 32%→73%로 쏠려 대사 기조 2분법이 죽는다 (득지·득세만 열면 59%).
  실측 분포표는 `docs/saju-validation.md` 3-2 절.
- **일수 가중치를 넣지 않는다.** "있나 없나" 만 보면 우리 관례가 늘지 않는다. 일수 배분은
  유파마다 다르다.
- 지장간 표는 `jiBongi` 가 마지막 항목을 읽는 **단일 소스**다. 본기를 따로 적지 말 것.

관례에서 나온 값은 프롬프트에서 **숫자로 인용하지 말라고 지시**한다. 새 파생 지표를 추가할
때도 이 구분을 유지하고 `docs/saju-validation.md` 에 어느 쪽인지 적는다.

### 체질 판정 (`lib/saju/constitution.ts`)

`diet` 유형의 체질은 **코드가 규칙표로 정한다.** 판정 축은 다섯이다 — 오행 과다·부족,
한열(조후), 대사 기조, 살이 붙는 패턴, **다이어트 접근 순서**. 여기에 **재료 범주**가
과다·부족 오행마다 딸려 붙는다. 근거·임계값·검증은 `docs/saju-validation.md` 3-3 절.

- **LLM 에게 체질을 정하게 하지 말 것.** 프롬프트는 판정을 `계산 완료 · 수정 금지` 로 주고
  "다시 판정하지 말고 서술하라"고 지시한다. 완료 기준이 "같은 사주 → 같은 판정" 이다.
- `constitution.ts` 의 한국어 문자열은 프롬프트가 아니라 **판정에 딸린 근거 데이터**다.
  프롬프트 문장 자체는 `lib/prompt.ts` 한 곳에 있고, 그 파일은 배치와 지시만 한다.
- 새 축을 추가하면 동점 처리를 반드시 고정한다. 고정하지 않으면 판정이 흔들려 완료 기준이 깨진다.
- **오행-장부 배속(목=간·담 …)을 사용자에게 내보내지 않는다.** 고전 배속이지만 장기 이름이
  나가면 진단처럼 읽힌다. `BODY_AXIS.classical` 은 출처 주석일 뿐이고, 노출은 생활어
  축(`axis`)으로 한다. 테스트가 판정 결과와 프롬프트 양쪽에서 막는다.
- 사용자에게 인용될 수 있는 문구는 `constitution.test.ts` 가 금지 어휘(치료·효능·진단·질환…)로
  훑는다. 문구를 추가하면 그 검사를 통과해야 한다.
- **실행 방법은 (B) 일부 개방이다** (2026-08-14 확정 · TASK-40). **방법은 구체적으로 쓰고
  수치는 계속 막는다.** 금지의 근거 넷 중 셋(근거 없음·즉흥 판정·안전)은 **수치에만** 걸리고,
  방법 자체는 판정에서 코드가 고를 수 있는 값이라 열어도 같은 사주에 같은 답이 나온다.
  - **여는 것** — 운동 종류(유산소·근력·이완·저강도 지속)와 강도의 감각, 식사 순서와 시각,
    조리 방식과 음식 온도.
  - **막는 것** — 칼로리·목표 체중·체지방률·감량 기간·횟수/세트/분 수·섭취량, 단식(간헐적
    단식 포함)·상표 식단 이름, 영양제·보조식품. **"많이 먹어라 / 끊어라" 도 그대로 금지**다.
  - `constitution.test.ts` 가 처방 어휘와 **문구에 숫자가 섞이는 것**을 막고, **여는 것이
    실제로 열려 있는지도 검사한다** — 막는 것만 보면 나중에 조용히 다시 닫혀도 모른다.
  - `prompt.test.ts` 가 유형 규칙과 섹션 지침 **두 곳**에 금지가 있는지 본다.
  - `app/disclaimer/page.tsx` 가 이 경계를 그대로 서술한다. **범위를 바꾸면 그 문단도 같은
    커밋에서 고친다** — 약속과 출력이 어긋나면 안 된다.
- **한열은 접근 순서에도, 실행 방법 표에도 넣지 않는다.** 한열은 같은 방법을 어느 온도·
  시간대에 실행하느냐를 정하는 **다른 층**이다. 섞으면 값이 20가지가 되어 각 칸의 근거를
  설명할 수 없다. `MOVEMENT_PLAN`·`MEAL_PLAN` 이 온도·성미를 말하기 시작하면 층이 무너지므로
  테스트가 그것도 막는다.
- **새 축은 새 동점을 만들지 않아야 한다.** `MOVEMENT_PLAN` 은 `dietApproach` 에서,
  `MEAL_PLAN` 은 `gainPattern` 에서 **1:1 로 파생**된다 — 둘 다 이미 결정론적이라 동점 처리가
  새로 필요 없다. 축을 더 넣을 때는 동점 처리를 반드시 함께 고정할 것.
- **식품 이름은 실행 방법 표에 적지 않는다.** 무엇을 먹을지는 `ELEMENT_FOOD`(오행)와
  `THERMAL_GUIDE`(조리·온도)가 정하고, `MEAL_PLAN` 은 **순서와 시각의 규칙**만 정한다.
  섞으면 목록 밖 식품이 이 표를 통해 새어 나간다.
- **식품도 코드가 고른다** (`ELEMENT_FOOD`, TASK-27). 오행-오미·오색 배속(고전)에서 흔한
  재료를 고른 목록(우리 관례)이고, `focus` 에 얹혀 나간다. 프롬프트가 **"이 목록 밖의 식품
  이름을 쓰지 말라"** 고 지시한다 — 목록을 닫는 것은 코드가 하지만 본문에 다른 식품이
  등장하는 것은 프롬프트만 막을 수 있다.
  - **재료는 오행이, 조리·온도는 한열이 정한다.** 성미(온성·냉성) 용어를 쓰지 말 것 —
    출처가 한의학이라 면책 고지의 "한의학과 무관" 약속이 깨진다. 조리 문구는
    `THERMAL_GUIDE` 하나에만 두고 여기서 표를 새로 만들지 않는다.
  - 상표명·영양제·보조식품·섭취량을 넣지 않는다. **많이 먹으라거나 끊으라고 하지 않는다** —
    알레르기·지병·복약·임신 여부를 입력받지 않아 알 수 없기 때문이다. 부족은 "곁들이기",
    과다는 "더 늘리지 않기" 까지다.
  - **식품 이름 검출기에 명리 용어와 겹치는 부분 문자열을 쓰지 말 것** — "견과" 는
    "비견과 겁재" 에 걸린다 (실제로 오탐이 났다).

### 올해 세운 판정 (`lib/saju/yearly.ts`) — 지금 어디에 쓰이는가

**유형이 아니라 `diet` 의 한 섹션("올해의 몸 흐름")이 쓴다** (TASK-39). 운세는 LLM 이 가장
즉흥적으로 흐르기 쉬운 영역이라 — 같은 사주에 어떤 날은 "빠지는 해", 어떤 날은 "찌는 해" 가
나오면 서비스가 아니라 점괘 뽑기가 된다 — **판정을 코드가 한다.**

- 축은 둘이지만 **밖으로 나가는 것은 하나뿐이다.**
  - **작용**(세운 오행이 원국의 부족을 채우는가 / 과다에 더하는가 → 보완·가중·중립) —
    `constitution.deficient`/`excess` 에서 계산되므로 **원래 몸 쪽 값**이다. 프롬프트로 나간다.
  - **주제**(세운 천간의 십신 그룹 → 경쟁과 독립 / 책임과 압박 …) — **프롬프트로 내보내지
    않는다.** 생활 영역 어휘라 몸 이야기에 얹으면 겉돌고, 옮기려면 근거 없는 대응표가 필요하며
    "압박이 늘어 몸이 상한다" 류의 추론은 면책 고지가 막는 의학적 주장에 닿는다.
    계산은 남겨 둔다 — 십신 그룹 판정 자체는 고전이고, 생활 영역 유형이 생기면 그대로 쓴다.
  - 작용 3단계와 주제 대응표는 둘 다 **우리 관례**다.
- **월별 운세는 계산하지 않는다.** 프롬프트가 "몇 월에 무엇" 을 쓰지 말라고 **두 곳**
  (판정 블록 · `TYPE_RULES.diet`)에서 막고 `prompt.test.ts` 가 두 곳 모두를 확인한다.
  월운을 넣으려면 절입 경계까지 따져야 하는데 지금 범위가 아니다.
- **사건을 예고하지 않는다.** 문구 표는 `yearly.test.ts` 가 금지 어휘(반드시·대박·사고·합격…)로
  훑는다 — **유형이 없어져도 문구는 여전히 사용자에게 나가므로 이 검사를 지운다.**
  면책 고지가 "확정된 사실이나 미래 예측으로 받아들이지 마세요" 를 약속한다.
- 판정 블록은 **체질 판정과 같은 프롬프트 안에** 실린다. 작용의 근거(오행 과부족)가 거기
  있어야 모델이 둘을 이어 쓸 수 있다.

### 대운(10년) 판정 (`lib/saju/decade.ts`) — `decade` 유형이 쓴다 (TASK-45)

**우리 관례는 하나만 늘었다: "대운을 세운과 같은 3단계(보완·가중·중립)로 본다."**
판정 규칙·동점 처리를 `yearly.ts` 에서 그대로 가져오므로 새로 정한 값이 없다.
**대운 세기 가중이나 천간/지지 비중 같은 값을 새로 만들지 말 것** — 관례가 불어난다.

- **직전 구간과의 비교도 새 관례가 아니다.** 같은 규칙을 한 번 더 적용한 것이고 두 판정이
  같으면 `유지` 라 동점 처리가 필요 없다. "20대엔 안 쪘는데 30대 들어 붙는다" 가 이 유형이
  답하려는 질문이라 그 차이를 정면으로 답한다.
- **다음 구간은 판정하지 않는다.** 아직 오지 않은 시간을 말하면 예측이 된다.
  프롬프트가 **세 곳**(판정 블록 · 유형 규칙 · 서두)에서 막고 `prompt.test.ts` 가 셋을 본다.
- **문구 표를 `yearly.ts` 와 공유하지 않는다.** 층이 다르다 — 세운은 한 해를 어떻게 보낼지,
  대운은 10년 동안 몸의 기본값이 어디로 기울어 있는지다. 같은 문장을 쓰면 `diet` 의
  "올해의 몸 흐름" 과 이 유형이 같은 말을 한다. `decade.test.ts` 가 대조한다.
- **`decade` 는 세운 판정을 받지 않는다.** 올해는 `diet` 몫이다.
- **`DaeunPeriod` 는 `gan`·`ji` 인덱스를 함께 든다.** `ohaeng` 은 표시용 두 글자 문자열이라
  판정에 쓸 수 없다 — **문자열을 쪼개 쓰지 말 것.**
- **현재 대운을 찾는 규칙은 `findCurrentDaeun` 한 곳에 있다.** 예전에는 같은 `find` 가
  `DaeunTimeline`·`ResultView`·`card-model` 셋에 흩어져 있었다. 어긋나면 화면·공유 카드·
  풀이가 서로 다른 대운을 가리킨다.

#### 성별을 요구하는 유형이 생겼다

대운 순행/역행이 성별로 정해지므로 미지정이면 `chart.daeun` 도 `chart.decade` 도 null 이다.
**임의로 순행을 정하지 않는다** — 그러면 같은 사주에 다른 판정이 나간다.

- `READING_TYPE_NEEDS_GENDER` 가 정하고 **막는 자리가 둘**이다: 폼이 이유와 함께 제출을 막고
  (`missingForType`), `sajuInputSchema` 의 refine 이 서버에서 다시 막는다.
- **미지정이면 무엇이 빠지는지 폼이 말하던 줄은 지웠다** (TASK-98 이 TASK-85 를
  되돌린다). 성별 칩 아래에 `고르면 10년 단위 흐름(대운)까지 함께 봅니다.` 가 있었다.
  - **뒤집은 이유는 폼 길이다.** 그 줄만 있던 것이 아니라 시각·지역 아래에도 한 줄씩
    있었고, 셋 다 **지금 당장 할 일을 말하지 않는 문장**이라 컨트롤 사이를 벌리기만 했다
    (위 "폼의 보조 설명은 기본값으로 대체한다").
  - **TASK-85 가 지적한 사실 자체는 그대로다** — 공개 다섯 유형에서 성별을 안 고르면
    `대운 · 세운` 근거 카드가 사라지고 공유 카드 칩도 빠진다. 지금은 그것을 폼이 미리
    말하지 않는다. **되살리려면 폼이 다시 길어지는 값을 어떻게 치를지 답할 것.**
  - **막는 자리는 그대로다.** `decade` 는 `missingForType` 이 버튼 위에 `role="alert"`
    로 이유를 낸다 — **그 문구는 지우지 않았고 섞지도 않는다.**
- **성별은 칩 둘이고 `선택 안 함` 칩은 없다** (TASK-85). 둘 다 비선택인 것이 곧
  `unspecified` 다 — 값 셋(`BirthInput["gender"]`)과 `sajuInputSchema` 는 그대로다.
  **필수로 만들지 말 것**: `READING_TYPE_NEEDS_GENDER` 를 전부 `true` 로 돌리면 유형을
  생략한 API 요청이 통째로 막히고(`schema.test.ts` 가 기본 유형을 검사한다), 성별을
  요구하는 것은 대운 방향을 정하려는 계산의 사정이지 정체성을 밝히라는 요구가 아니다.
- **홈에서 카드를 막지 않는다.** 성별은 프로바이더의 메모리에 있어 클라이언트만 안다.
  카드를 흐리게 하려면 `/` 에 클라이언트 컴포넌트가 들어가고 **`/` 를 통째로 정적으로 두는
  성질이 깨진다.** 카드는 누구에게나 보이고 폼이 이유를 말한다 — 왜 막혔는지 그 자리에서
  읽히는 쪽이기도 하다.
- **`readingType` 기본값은 성별을 요구하지 않는 유형이어야 한다.** `schema.test.ts` 가 막는다.
- `app/disclaimer/page.tsx` 가 "시기를 정해 드리는 것이 아니다" 와 "성별이 필요한 이유" 를
  밖에서 약속한다. **범위를 바꾸면 그 문단도 같은 커밋에서 고칠 것.**

### 리딩 유형을 늘릴 때

`READING_TYPES` 에 값을 넣으면 **`Record<ReadingType, …>` 를 쓰는 곳이 전부 컴파일 오류로
잡힌다.** 이걸 유지하는 것이 중요하다 — 삼항이나 인덱스 시그니처로 두면 새 유형이 조용히
다른 유형의 값을 달고 나간다. TASK-15 에서 실제로 `buildShareCardModel` 의 칩이 그렇게
새어 나갈 뻔했고, `prompt.test.ts` 도 `type === "diet" ? … : …` 때문에 새 유형을 general
프롬프트로 검사하고 있었다. 지금은 둘 다 `Record` 와 `READING_TYPES` 순회로 바꿨다.

손대야 하는 곳: `schema.ts` 의 유형 문구 셋(`READING_TYPE_LABEL` · **카드 설명**
`READING_TYPE_DESCRIPTION` · **검색·공유 문구** `READING_TYPE_META`) · **노출 구분**
(`READING_TYPE_VISIBILITY`) · **성별 요구 여부**(`READING_TYPE_NEEDS_GENDER`) ·
섹션 계약(`reading/sections.ts`) · 섹션 지침과 유형별
서두·규칙(`prompt.ts`) · **유형별 판정 블록**(`prompt.ts` 의 `VERDICT_BLOCK`) · 공유 카드 칩
(`share/card-model.ts`). 유형 선택 카드 · 결과 뒤 링크 · `/admin` · `/reading/[type]`
프리렌더는 목록을 순회하므로 자동이다.

#### 노출 구분 — 목록과 프리렌더는 다른 목록을 쓴다 (TASK-41)

`READING_TYPE_VISIBILITY` 가 `public`/`internal` 을 정하고, `PUBLIC_READING_TYPES` ·
`INTERNAL_READING_TYPES` 가 **거기서 파생된다.** 손으로 유지하는 두 번째 목록을 만들지 말 것.

| 쓰는 곳 | 도는 목록 | 왜 |
| --- | --- | --- |
| 유형 선택 카드 · 결과 뒤 링크 | `PUBLIC_READING_TYPES` | 내부 유형은 보이면 안 된다 |
| `/admin` | `INTERNAL_READING_TYPES` | 내린 유형으로 들어가는 유일한 입구 |
| `generateStaticParams` | **`READING_TYPES` 전부** | 빼면 `/reading/general` 자체가 죽는다 |

- **내부 유형 페이지와 `/admin` 은 `noindex` 다.** 메인에서 링크가 사라져도 이미 색인됐을 수
  있고, 검색에서 들어오면 내린 의미가 없다.
- **`/admin` 에 인증이 없다 — 숨김이지 보호가 아니다.** URL 을 아는 사람은 누구나 들어온다.
  지금 목적(본인이 다른 서비스와 결과 대조)에는 충분하고 비밀도 아니다. **여기에 진짜 관리
  기능을 붙이려면 그 전에 인증을 먼저 넣어야 한다.**
- **`readingType` 기본값은 공개 유형이어야 한다.** 유형을 생략한 요청이 화면에서 고를 수 없는
  유형으로 가면 안 된다. `schema.test.ts` 가 막는다.
- **공개 유형이 하나뿐이면 결과 뒤 링크(`OtherReadingLinks`)는 아무것도 내지 않는다.**
  제목만 남은 빈 상자가 되기 때문이다. 유형이 늘면 저절로 다시 나타난다.

**카드 설명과 메타 설명은 사용자에게 그대로 보이는 문구다.** 그 유형의 표현 규칙이 판정
문구와 똑같이 적용된다 — `diet` 는 처방·수치도, 예언 어휘도 쓰지 않는다 (올해 흐름을
다루므로 후자도 걸린다). `lib/saju/schema.test.ts` 가 모든 유형의 문구를 금지 어휘로 훑는다.
둘을 하나로 합치지 말 것 — 카드는 제목 바로 밑이라 짧아야 하고, 메타는 링크만 보고
판단하는 사람이 읽으므로 무엇을 넣어야 하는지(생년월일시)까지 말해야 한다.

### 그 밖의 도메인 규칙

- 라이브러리는 **중국어(간체)** 문자열을 돌려준다. 경계에서 즉시 인덱스로 바꾸고
  (`ganji.ts` 의 `parseGanjiHanja`) 노출 직전에만 한글로 바꾼다. 문자열을 중간 표현으로
  들고 다니면 간체/정체 표기 차이로 깨진다.
- 서머타임·표준자오선 이력은 **표를 손으로 만들지 말고** Node 내장 tz 데이터(`Asia/Seoul`)를 쓴다.
  1954-03-21 / 1961-08-10 전환과 모든 서머타임 구간이 정확히 들어 있음을 검증했다.
- 타입 정의가 없어 `types/lunar-javascript.d.ts` 에 직접 선언했다. 새 API 를 쓰면 여기 먼저 추가한다.
- 출생시각 미상이면 **시주(時柱)를 제외**하고 해석한다 (`timeUnknown`). 임의의 시각으로 채워 넣지 않는다.
- **시각 입력은 시·분 드롭다운 두 개이고 분은 1분 단위다** (`lib/form/birth-time.ts`, TASK-23).
  거칠게 만들지 말 것 — 경도 보정으로 시지 경계가 시계시의 정시에서 밀리므로(서울 −32분이면
  자시가 01:32 에 끝난다) **5분 단위로 줄이면 경계가 선택지 사이에 끼어 시주가 틀어진다.**
  같은 이유로 **12지시(자시~해시)만 고르게 하지 않는다** — 대표 시각을 우리가 채워야 하고
  그건 위의 "임의 시각을 채워 넣지 않는다" 와 충돌한다. 한쪽만 고른 상태는 제출을 막는다
  (분을 `00` 으로 메우면 사용자가 지정하지 않은 값이 시주를 바꾼다).
  근거 표는 `docs/saju-validation.md` 2-3-1 절이고 `birth-time.test.ts` 가 만세력 계산으로 고정한다.
  이때 시각 보정도 강제로 끈다 (정오를 임시로 쓰는 것이므로 보정에 의미가 없다).
- 기본 옵션: 시각 보정 `longitude`(경도만), 자시 학파 `yajasi`(자정 기준). 둘 다 한국 만세력 다수 관행.
- 대운은 순행/역행을 성별로 정하므로 **성별 미지정이면 `daeun === null`** 이다. 임의로 정하지 않는다.
- 절기 테이블은 기준일 주변 1년여만 담는다. 대운수 계산은 앞뒤 45일씩 옮긴 기준일에서도
  모아 합친다 (`collectJeolInstants`). 키가 간체(`惊蛰`)이고 인접 연도는 로마자(`LI_CHUN`)로
  중복 제공되므로 두 표기를 모두 받는다.
- 사주 계산 로직을 수정하면 **반드시 `npm run validate:saju` 를 통과시킨다.**
  테스트는 같은 라이브러리를 두 번 부르지 않는다 — 60갑자 연속성·오호둔·오서둔을
  테스트 안에서 독립 구현해 대조하고, 절기는 천문 공표값과 맞춘다. 새 규칙을 넣으면
  같은 방식으로 문헌값을 테스트에 박아 둔다.
- **출생지는 시/군 단위로 고른다** (TASK-37). `lib/form/birthplaces.ts` 는 통계청 행정구역
  경계에서 계산한 **자동 생성 표**다 — `node scripts/build-birthplaces.mjs` 로 만들고
  결과물을 커밋한다. **손으로 고치지 말 것.** 광역시는 구를 합치되 **군(郡)은 뗀다**
  (인천을 옹진군까지 합치면 백령도가 중심을 5분어치 끈다). 근거표는
  `docs/saju-validation.md` 3-5 절.
  - **지역 이름은 서버로 보내지 않는다** — 계산에 필요한 건 경도뿐이다. 화면 표시는
    폼이 들고 있는 값으로 한다(`ResultView` 의 `birthplace` prop).
  - **쓰이지 않을 값은 보내지 않는다** — 시각 미상·보정 없음이면 서버가 경도를 버리므로
    요청에서 빼고 폼도 잠근다. 고를 수 있게 두면 반영되는 줄 안다.
  - **해외 출생은 지원하지 않는다.** 경도 범위가 124~132° 이고 `wallClockToUtc` 가
    `Asia/Seoul` 고정이라 현지 시각을 넣으면 한국 시각으로 취급된다. 한계로 문서화됨.
- 결과 문구에서 단정적 예언·의학적 진단을 하지 않는다. 시스템 프롬프트에 규칙으로 박혀 있다.

#### 판정은 단정하고 인과·예고는 막는다 (TASK-55)

성질이 다른 두 문장을 한 줄로 묶지 말 것. **이 구분이 프롬프트 표현 규칙의 축이다.**

| | 예 | 다루는 법 |
| --- | --- | --- |
| (i) 판정에 대한 사실 | "걸리는 지점은 먹는 것, 패턴은 **식욕형입니다**" | **단정한다** |
| (ii) 몸에 대한 인과·예고 | "당신은 스트레스 **때문에** 찝니다", "~하게 됩니다" | **계속 막는다** |

(i)은 `constitution.ts` 가 정한 결정론적 값이고 `app/disclaimer/page.tsx` 가 "언제나 같은
판정" 이라고 이미 약속한 내용이다. 여기에 `~인 편입니다` · `~일 수 있습니다` 를 덧대면
**아무것도 말하지 않은 글**이 되고 적중감이 사라진다. 실측에서 hedge 를 걷어내자 판정 라벨
인용이 93/96 → 48/48 로 올랐다 (`docs/saju-validation.md` 3-6 절).

- 경계는 `SYSTEM_INSTRUCTION` 에 한 번, `TYPE_RULES` 셋의 **맨 앞 줄**에 한 번씩 있다.
  `prompt.test.ts` 가 **양쪽을 검사한다** — (ii)가 막혀 있는지 **+ (i)이 실제로 열려 있는지.**
  막는 것만 보면 나중에 조용히 다시 닫혀도 모른다.
- **금지 규칙 넷 중 셋은 그대로다** — 수치·단식·처방(우리가 모르는 것) · 근거 없는 값 ·
  장부·성미·효능·월별 운세(문서가 이미 약속한 것). 열린 것은 hedge 어미 하나뿐이다.
- **지시문이 금지 낱말을 직접 요구하지 않는지 확인할 것.** 실제로 `gain-misread` 지시문이
  "진단한 것이 아니라고 밝혀라" 를 요구해 `진단`·`질환` 이 본문에 6건 나왔다. 부정문이어도
  `TYPE_RULES` 가 막는 낱말이면 **지시문 쪽을 고친다.**

#### `결` 은 화면에도 프롬프트에도 쓰지 않는다 (TASK-68 → 72)

`결` 은 **사주 용어가 아니었다.** 나뭇결·물결의 그 낱말을 빌려 와, 위 (i)판정을 단정하면서
(ii)몸의 인과로 넘어가지 않는 자리를 맡긴 장치였다. **장치는 맞았지만 낱말이 틀렸다.**

TASK-68 은 **양을 줄이고 경계를 정의하는 문구는 남기는** 쪽으로 정리했다(`lib/prompt.ts` 66회
→ 10회). **그 절충이 틀렸다는 것이 TASK-72 의 결론이다** — 남긴 그 문구가 하필
`VerdictCallout` 의 제목(`이 사주에서 읽은 결`), 즉 결과 화면 맨 위 큰 글씨 자리라
**줄이든 말든 제일 먼저 읽혔다.** 한 번만 나와도 처음 보는 사람에게는 전문용어다.
지금은 **사용자 노출 문자열과 프롬프트에서 0건**이고 `prompt.test.ts` 가 0건을 검사한다.

- **없앤 것은 낱말이지 경계가 아니다.** (i)판정을 단정하고 (ii)몸의 인과로 넘어가지 않는
  구분은 그대로다 — `prompt.test.ts` 가 (ii)가 막혀 있는지 **+ (i)이 열려 있는지**를 양쪽에서
  본다. **hedge 로 도망가지 말 것**: 대체어를 고르다 `~인 편입니다`·`~일 수 있습니다` 로
  흐르면 TASK-55 가 되돌린 자리(라벨 인용 48/48 → 93/96)로 돌아간다.
- **한 낱말로 갈아 끼우지 않았다.** 그러면 새 쏠림이 생겨 같은 문제를 다른 글자로 되풀이한다.
  자리마다 다른 말을 골랐다 — 쪽 · 방식 · 성향 · 흐름 · 감각 · 치우침 · 판정, 아니면 문장을
  다시 썼다. 경계를 정의하던 줄은 **`이 사주에서 읽어 낸 것`** 이다(우리 쪽 말이 아니라
  "읽어 낸" 행위를 가리키므로 같은 일을 한다).
- **`GAIN_LABEL` 은 `~할 때 붙는 성향` 이다.** 바뀐 것은 머리 낱말뿐이고 **`~할 때` 는
  그대로다** — 그 자리가 "언제인지" 만 말하고 "왜인지" 는 말하지 않아 (ii) 경계를 성립시킨다.
  `~해서`·`~라서` 로 바꾸면 형태만으로 경계를 넘는다. `constitution.test.ts` 가 인과 어미와
  hedge 어미를 양쪽에서 막고 **형태 자체도 고정한다.**
- **절 제목 `오행이 만드는 결` 은 `오행이 만드는 치우침` 이다.** 지시문 본문이 이미
  "치우침" 으로 쓰여 있어 제목과 지침이 같은 말을 하게 됐다. 계약 제목은 **모델이 재현하기
  쉬운 낱말**이어야 하므로(`움직-` 어간 사례) 제목을 고칠 때는 표본으로 재현율을 볼 것.
- **`SYSTEM_INSTRUCTION` 이 되풀이를 막는다** — "같은 낱말로 모든 절을 쓰지 마세요 ·
  글 전체에서 두세 번까지". **여기에 금지 낱말을 예로 적지 않는다** — 지시문에 적는 순간
  그 낱말이 모델 눈앞에 놓인다(바로 아래 부정문 기전과 같다). 소스만 고치고 이 지시가
  없으면 모델이 스스로 되살리므로, 지시는 남기되 **예시는 대체어 쪽으로만** 든다.
- **금지 낱말이 부정문으로 새는 것은 낱말이 아니라 횟수 문제다** (TASK-77 · 2026-08-19).
  범위를 밝히라는 지시가 그 낱말을 끌어온다 — "특정 질환을 진단하는 것이 아닙니다".
  **막기만 하지 말고 쓸 문장을 준다**(`저희가 아는 것은 생년월일시뿐이라…` ·
  `효과적입니다` 대신 `~하는 편이 덜 흔들립니다`). 이 방향은 맞지만 **그것만으로는 절반까지다.**
  - **가장 큰 지렛대는 상한이었다** — "범위를 밝히는 문장은 글 전체에서 한 번까지".
    절마다 "~이 아닙니다" 를 붙이는 것이 양의 정체였다. `diet-food`·`diet-method` 기준
    **0.92 → 0.54 → 0.33/표본**, 낱말 없는 표본 11/24 → **19/24**.
  - **지시문이 견주기를 요구하면 그 낱말이 나간다.** `한의학의 사상체질과 같은 것으로
    말하지 마세요` 가 "한의학적 진단이 아니라" 를 낳았다. **견주지 말고 근거만 말하게 한다** —
    "이 판정의 근거는 명리학 오행뿐입니다".
  - **대안 문장 자체가 부정문 꼴이면 안 된다.** 처음 준 `몸을 고치는 이야기가 아니라…` 가
    "치료제처럼"·"처방이 아니므로" 를 낳았다. 지금은 `여기서 다루는 것은 밥상까지입니다` 다.
  - **남은 부정문은 그대로 둔다.** 더 조이면 범위를 밝히는 문장 자체가 사라진다.
    판단 기준은 **절대량과 "진짜 위반 0건"** 이다 — **부정문 비율을 목표로 삼지 말 것**,
    낱말을 걷어낼수록 그 비율은 오히려 올라간다(잔여물만 남으므로).
  - **작은 표본으로 좋아졌다고 적지 말 것.** TASK-68 이 적은 "0.14/표본" 은 21건짜리 절반
    회차 값이었고, 두 회차로 다시 재니 고치기 전과 같은 자리였다.
  - 이 기전은 TASK-55 · 68 · 72 에서 세 번 걸렸다. `prompt.test.ts` 가 상한 문구와 대안
    문장을 **양쪽에서** 검사한다.

#### 분량은 절마다 다르고 여는 자리는 장면이다 (TASK-56)

**균등 배분을 되살리지 말 것.** `각 절은 3~5문장 / 전체 2,000자` 를 모든 절에 똑같이 걸면
그 자리에 근거 인용 + 용어 풀이 + 근거→해석 연결이 다 들어가 **장면 쓸 자리가 물리적으로
없다.** 공감은 장면에서 온다.

- 분량은 `SECTION_INSTRUCTION` 마지막 줄의 **`분량:`** 이 정하고, 유형마다 **중심 절**이
  하나 이상 있다 (`diet`=체질 · `gain-cause`=상황 · `diet-method`=움직임·먹는 순서).
  `prompt.test.ts` 가 모든 절에 `분량:` 이 있는지, 중심 절이 있는지, **요약이 중심이 아닌지**를
  검사한다. 요약은 강조 카드라 길어지면 본문을 다 밀어낸다.
- **장면은 중심 절에만 요구한다.** 모든 절에 넣으면 장면 나열이 되고 근거가 사라진다.
- **여는 자리 규칙은 전 절 공통이다** — 첫 문장을 명리 용어로 열지 않는다. **`이 사주는` 도
  같다**: 우리 쪽 말이라 읽는 사람이 자기 이야기로 받지 못한다. 규칙만으로는 안 잡혀서
  `SYSTEM_INSTRUCTION` 에 **나쁜 예 둘 + 좋은 예 하나**를 붙였고, 그러고 나서야 0.64 → 0.17 로
  내려갔다. 예문을 지우지 말 것.
- **근거는 빼는 것이 아니라 뒤로 옮기는 것이다.** "반드시 언급" 은 그대로다 — 그게 이
  서비스가 점괘와 다른 지점이다.
- **모델은 지시한 분량의 절반에서 멎는다** (3,000 지시 → 1,500~2,000 산출). 그래도 지시값을
  내리지 말 것 — 내리면 다시 1,000자대로 내려앉는다.
- **분량을 늘리면 늘어난 자리를 무엇이 채우는지 따로 볼 것.** 첫 회차에서 hedge 가 기준선
  위로 되돌아가고 라벨 인용이 떨어졌다. 늘린 뒤 한 번 더 재지 않으면 놓친다.

#### 말투는 리포트가 아니라 상담이다 (TASK-57)

페르소나는 원래도 "20년 경력 상담가" 였는데 **문체 지시가 한 줄도 없어서** 리포트로 나왔다.
지금은 `SYSTEM_INSTRUCTION` 이 아래를 정한다.

- **존댓말은 `-습니다`체다** (2026-08-18 확정). 예전에는 결정이 아니라 프롬프트가 그 체로
  쓰여 있어서 따라간 우연이었다. **`-해요`체로 바꾸지 말 것** — 판정을 단정하는 문체(TASK-55)와
  면책 문구의 톤이 함께 흔들린다.
- **용어는 글 전체에서 처음 한 번만 풀고, 괄호가 아니라 문장으로 녹인다.**
  `일간(태어난 날의 천간)이 …` 패턴이 절마다 반복되는 것이 교과서 말투의 정체였다.
- **판정에 딸린 문구는 말맛을 살려 쓴다.** `constitution.ts` 의 문구는 이미 동사 중심이고
  장면이 들어 있다(`그릇을 작게 쓰고 천천히 씹기`). 예전 지시("그대로 베끼지 말고 다시
  쓰세요")가 그 구체성을 추상적인 재서술로 바꾸는 경로였다. **막을 것은 한 줄씩 나열하는
  것이지 문구의 말맛이 아니다.**
- **말투 예시 셋을 `# 말투 예시` 로 붙였다.** 규칙으로 설명하는 것보다 보여주는 쪽이 정확하다.
  예시는 **어느 판정에도 없는 사주**로 만들고 "복사하지 말고 말투만 따르라" 를 함께 건다 —
  few-shot 이 그대로 복사되는 것을 막는 장치다. 표본에 예시 문장이 그대로 나오는지 확인할 것.
- **되짚기(`혹시 ~한 적 있으시죠?`)는 절마다 하나까지, 요약 절에는 없다.** 넘치면 누구에게나
  맞는 말을 던지는 글이 되고, 판정이 결정론적인 이 서비스에서는 오히려 신뢰를 잃는다.
  **`~때문에 그러셨죠?` 는 금지** — 질문 형태여도 몸의 인과를 단정한다(TASK-55 경계).
- **이름이 없으면 호칭을 지어내지 않는다.** `sanitizeName` 의 기본값이 `고객님` 이라 글 전체에
  거리가 생겼다. 지금은 "호칭 없이 쓰라" 고 알린다. **그 함수의 인젝션 방어는 그대로다.**

##### 전문가 상담 권유는 본문이 아니라 화면에 있다

`components/ResultView.tsx` 맨 아래 고정 문구가 그 역할을 한다. 예전에는 `TYPE_RULES` 셋이
"마지막에 한 문장 덧붙이세요" 를 요구해 **모든 풀이가 법적 고지로 끝났다** — 읽고 나서 마지막에
남는 인상이 그것이었고, 매번 같은 문장이라 눈이 건너뛰었다.

옮기면 본문을 사람 말로 끝낼 수 있고 **약속은 오히려 더 확실해진다 — LLM 이 빠뜨릴 수 없다.**
`prompt.test.ts` 가 **양쪽을 검사한다**: 화면 문구에 있는지 + 프롬프트에서 사라졌는지.
한쪽만 보면 고지가 없어지거나 같은 문장이 두 번 나온다. **되돌리려면 두 곳을 같은 커밋에서
고칠 것.**

## 법적 고지 (코드와 반드시 일치)

`app/privacy/page.tsx` 와 `app/disclaimer/page.tsx` 는 **실제 코드 동작을 서술한 문서**다.
데이터 흐름을 바꾸면 같은 커밋에서 문서도 고친다. 지금 문서가 약속하는 내용:

- 생년월일·시각·성별·이름을 **저장하지 않는다** (DB 없음, 로그에도 남기지 않음)
- IP 는 레이트 리밋 목적으로 **메모리에 최대 1분**만 둔다 (`lib/rate-limit.ts`)
- 입력이 **Google 로 전송**되며 무료 등급이라 제품 개선에 쓰일 수 있다
- 추적 쿠키를 쓰지 않는다 (첫 방문 안내 닫힘 여부만 localStorage)
- **6항에 방문 통계와 운영 지표의 수집 항목을 전부 나열해 뒀다.** `lib/observability.ts` 의
  `METRIC_FIELDS` 를 늘리면 그 목록도 같은 커밋에서 고친다

**결과 캐싱은 "저장하지 않는다"를 무효화한다.** 캐싱을 넣으면 보관
기간·항목을 반드시 다시 쓸 것. 결과 영구 링크(`/r/[id]`)도 같다 — 그쪽은 위 "결과 공유" 절.

면책 고지에는 만세력 학파 차이(경도 보정·자시 기준)로 다른 서비스와 결과가 다를 수 있다는
설명이 들어 있다. 기본 옵션을 바꾸면 그 문단도 갱신한다.

또한 **체질 판정이 코드로 결정된다**는 것과 **한의학 사상체질·건강검진과 무관하다**는 것을
약속한다. `lib/saju/constitution.ts` 의 판정 방식을 바꾸면 그 문단도 같은 커밋에서 고친다.

## 컬러 토큰과 부품 규격

**색은 `app/globals.css`, 부품 규격은 `components/ui/` 가 단일 소스다** (TASK-75).
예전에는 색만 규율하고 높이·radius 는 파일마다 달랐다 — 버튼이 `rounded-full`·`xl`·`lg`
셋을 섞어 쓰고 입력 높이가 44px 인데 버튼은 52px 이었다. 지금은 둘 다 한 곳에 있다.

- 버튼: `components/ui/Button.tsx` — 48px · radius 12px · 누를 때 1px · variant 넷.
  **규격 문자열은 `buttonClass()` 가 만들고 `Button` 이 그것을 쓴다** (TASK-93).
  `<button>` 이 아닌 요소(뒤로가기 링크)도 같은 규격을 써야 하는데 `Button` 은
  `ButtonHTMLAttributes` 를 받으므로, 호출부에서 44px 원형을 다시 스타일링하는 대신
  **문자열만** 가져간다. 새 꼴이 필요하면 `SIZE`·`VARIANT` 에 더하고 호출부에서 유틸리티를
  덧대지 말 것 — 특히 radius 는 덧대면 스타일시트 순서가 이긴다(아래).
  **아이콘 하나만 드는 `size="icon"` 은 44px 원형이다** (TASK-89) — 글자가 없어 누를 면이
  아이콘 크기로 정해지므로 터치 타깃 최소 크기가 dasii 값(36px)보다 앞선다. **radius 는
  `SIZE` 안에 둔다** — 공통 문자열에 두면 호출부에서 `rounded-full` 을 덧대야 하는데 두
  유틸리티는 특정도가 같아 **스타일시트 순서**가 이기고 클래스 문자열 순서로는 정해지지 않는다.
  쓰는 곳은 접힌 폼의 `수정` 과 `/reading/[type]` 맨 위의 뒤로가기(`BackIconLink`)이고,
  **둘 다 이름을 `aria-label` 이 만든다**(아이콘은 `aria-hidden`)
  **`variant="surface"` 는 대비를 보증할 수 없는 면 위에 얹히는 꼴이다** (TASK-97 ·
  지금은 히어로 사진 위의 뒤로가기 하나). 면(`canvas`)을 들고 가므로 아이콘이 사진이
  아니라 토큰 색 위에 놓이고, 그 조합을 `tokens.test.ts` 가 잰다. **사진 위에 무엇을
  얹을 일이 또 생기면 이 꼴을 쓰고, 글자로는 넓히지 말 것** (근거는 위 "화면 1 구조 —
  히어로 사진").
- 입력: `components/ui/field.ts` 의 `FIELD_BASE` — 48px · radius 12px
- 칩(선택지 둘): `components/ui/ChoiceChips.tsx` — 48px · radius 12px · 라디오 그룹
  (TASK-85). 성별과 양력/음력이 쓴다. 선택된 면은 `bg-brand` + `text-on-brand` 이고
  화살표로 옮길 수 있다. **`<select>` 가 공짜로 주던 접근성을 직접 만드는 자리다.**
- 라벨: `components/ui/FormField.tsx` 의 `Field` · `LABEL_CLASS` — 폼과 `계산 기준`
  카드가 함께 쓴다 (TASK-101 에서 `SajuForm` 밖으로 뺐다). **파일 이름이 `Field.tsx` 가
  아닌 이유는 `field.ts` 와 대소문자만 달라 `tsc` 가 TS1149 로 멈추기 때문이다.**
- 드롭다운 껍데기: `components/ui/SelectShell.tsx` — `select` 를 그리는 파일이 둘이
  됐으므로(폼 · 계산 기준 카드) 부품도 파일로 나왔다. `birth-input.test.ts` 가 **두 파일
  각각에서** `<select` 개수와 껍데기 개수를 대조한다. **새 파일에서 select 를 그리기
  시작하면 그 목록에 더할 것** — 빠뜨리면 그 화면만 검사 밖이 된다.
- **부품만 만들고 호출부를 안 바꾸면 규격이 두 벌이 된다.** 새 버튼·입력을 손으로
  스타일링하지 말 것. 예외는 `LikeButton`(칩)과 `SelectShell`(화살표 규칙)이고 둘 다
  이유가 주석에 있다.
- 타이포 스케일 `title-lg/md/sm` × `title-bold/extrabold` 도 `globals.css` 에 있다.
  **`title-sm` 과 `.reading h2` 는 같은 값이어야 한다** — 어긋나면 계약이 깨져 원문
  폴백으로 갈 때만 제목이 달라 보인다 (TASK-67 이 겪은 버그다).
- **dasii 에서 어느 층을 가져오고 어느 층을 왜 버렸는지**는 `docs/design-tokens.md` 에 있다.
  요약하면 팔레트와 부품 규격은 가져왔고 **shadcn 시맨틱 층(`--background`·`--ring` 등)은
  버렸다** — 토큰 이름이 세 번째 체계가 되고, 다크모드가 딸려 오고, 저쪽 포커스 링이
  2.58:1 로 우리 기준(3:1)에 미달한다.

색 자체는 `app/globals.css` 가 **단일 소스**다. 2층 구조로 되어 있다.

1. **원시 팔레트** `--raw-*` (`:root`) — "무슨 색인가". 컴포넌트에서 직접 쓰지 않는다.
2. **시맨틱 토큰** `--color-*` (`@theme inline`) — "어디에 쓰는가". 컴포넌트는 이것만 쓴다.

`@theme inline` 이어야 `var(--raw-*)` 참조가 유틸리티로 정상 생성된다 (`inline` 없으면 깨진다).

- 이름에 `text`/`border` 를 쓰지 않는다. `--color-text-secondary` 는 `text-text-secondary` 라는
  유틸리티가 되어 읽기 나쁘다. 그래서 **`ink`**(텍스트)와 **`line`**(선)을 쓴다.
- **초록 계열은 용도가 둘로 갈린다** (TASK-21). 섞어 쓰면 대비가 깨진다.
  - **연한 면** `brand`(green500)·`brand-hover`(green600) — 강조 블록 배경, 선택된 칩,
    스트리밍 커서, 현재 대운 링. 그 위 글씨는 `text-on-brand`(어두운 잉크, 9.61:1)다.
    **`green500` 위에 흰 텍스트를 올리지 말 것** — 1.82:1 로 AA 미달이다.
  - **채운 버튼** `brand-solid`(green700) + `text-on-brand-solid`(흰 글씨, 5.14:1),
    hover 는 `brand-solid-hover`(green800, 7.40:1). 주 액션 버튼 전용이다.
- **중간 밝기 초록에는 어느 글씨색도 통과하지 않는 사각지대가 있다.** 실측: `#1F9155` 은
  흰 글씨 4.01 · 어두운 잉크 4.36 으로 둘 다 미달이다. 그래서 "조금만 어둡게" 로 타협하면
  더 나빠진다 — 연한 면이나 충분히 어두운 면 둘 중 하나로 가야 한다.
- **비활성 채운 버튼을 회색으로 채우지 말 것.** 같은 사각지대에 걸린다 — 흰 글씨를 유지할
  만큼 어두운 회색은 `green700` 과 명도가 1.07:1 이라 색상으로만 구분된다. 면을 비운다
  (`bg-brand-solid-disabled` + `text-on-brand-solid-disabled`). 근거표는
  `docs/design-tokens.md` 의 "채운 버튼의 사각지대" 절.
- 원본 팔레트(dasii)의 `green600`·`blue500`·`red` 는 본문 텍스트로 쓰면 3.1~3.5:1 로 미달이다.
  그래서 `--raw-*-700` 파생값을 만들었다. `docs/design-tokens.md` 에 원본/파생 구분이 있다.
- 대비비와 "컴포넌트에 raw 색상 없음"은 `lib/design/tokens.test.ts` 가 **globals.css 를 파싱해서**
  검증한다. 토큰을 추가하면 그 테스트에 조합도 추가한다.
- **드롭다운 화살표는 `globals.css` 의 `.select-shell` 이 그린다** (TASK-38). `select` 에는
  `::after` 를 붙일 수 없어 감싸는 요소가 필요하고, 폼의 `SelectShell` 이 그 역할이다.
  - **모든 `select` 가 이걸 쓴다.** 하나만 빠지면 그 칸만 브라우저 기본 화살표가 남아
    폼 안에 화살표가 두 종류가 된다. `birth-input.test.ts` 가 `<select` 개수와 껍데기
    개수를 대조하므로 새 select 를 추가하면서 빠뜨리면 걸린다.
  - 삼각형은 `clip-path` 로 그린다 — data URI 에 색을 박으면 팔레트에 없는 색이 하나 생긴다.
  - `.select-shell > select` 에 **`min-width: 0`** 이 필요하다. `select` 의 내재 최소 폭은
    가장 긴 항목 기준이라 좁은 화면에서 껍데기를 밀어낸다.
- **가로 스크롤바도 `globals.css` 의 `.scroller-x` 가 정한다** (TASK-43). 적용처는 대운 카드
  줄 하나뿐이고, 클래스로 뺀 것은 두께·색을 한 곳에 두기 위함이다.
  - **표준 속성(`scrollbar-width`·`scrollbar-color`)과 `::-webkit-scrollbar` 를 둘 다 둔다.**
    하나만 두면 다른 쪽 브라우저에서 OS 기본 스크롤바가 남아 팔레트 밖 회색이 생긴다.
    크로미움은 표준 속성이 있으면 의사요소를 무시하므로 겹쳐 그려지지는 않지만, **두 벌의
    값이 어긋나면 브라우저마다 다르게 보인다** — `tokens.test.ts` 가 막대 색을 대조한다.
  - **여백은 스크롤러의 `padding-bottom` 이 만든다.** 스크롤바는 패딩 박스 밖 거터에
    그려지므로 카드에 `mb-*` 를 줘도 간격이 생기지 않는다.
  - **트랙에 색을 주지 않는다.** 이 스크롤러는 `-mx-5` 로 카드 폭 전체에 걸쳐 있어서
    트랙에 색을 주면 바로 아래 `border-t` 와 나란히 구분선이 둘로 읽힌다.
  - **숨기지 않는다.** 안내 문구가 있어도 남은 양을 알려주는 단서는 스크롤바뿐이다.
- **모바일 폭 확인은 `node scripts/screenshot.mjs` 로 한다** (TASK-69).

  ```bash
  npm run dev                                                    # 다른 터미널에서
  node scripts/screenshot.mjs http://localhost:3000/ out.png --full
  node scripts/screenshot.mjs http://localhost:3000/ out.png --width 1280 --dpr 1 --desktop
  ```

  기본값이 **390×844 · DPR 2 · 모바일**이다 — 주 사용자가 모바일이라 기본값을 그쪽에 둔다.
  - **`chrome --headless --window-size=390,844 --screenshot` 을 쓰지 말 것.** 그건 **PNG 만**
    390px 로 자른다. 레이아웃 뷰포트는 창 최소 폭(약 500px)에 걸려 더 넓게 잡히므로 결과물은
    넓게 배치된 화면의 왼쪽을 오려낸 그림이고, **글자가 오른쪽에서 잘려 레이아웃이 깨진 것처럼
    보인다.** TASK-64 에서 실제로 한 번 속았다.
  - 진짜 모바일 폭은 CDP 의 `Emulation.setDeviceMetricsOverride` 로만 만들어진다. 그래서
    스크립트가 크로미움을 `--remote-debugging-port` 로 띄우고 CDP 에 직접 붙는다.
  - **의존성이 없다.** Node 21+ 의 전역 `WebSocket` 을 쓴다 (`scripts/render-icons.mjs` 가
    playwright 를 상시 의존성으로 두지 않는 것과 같은 판단). 실행 파일은 `CHROMIUM_PATH` →
    설치된 Chrome → Edge 순으로 찾는다.
  - iframe 으로 우회하려 해도 `next.config.ts` 의 `X-Frame-Options: DENY` 에 막힌다.
- **포커스 링은 `globals.css` 의 전역 `:focus-visible` 하나로 정한다.** 컴포넌트에
  `focus:ring-*` 을 붙이지 말 것 — 버튼·`summary`·링크에서 빠뜨리게 되고, 같은 클래스의
  `outline-none` 이 유틸리티 레이어 우선순위로 전역 규칙을 조용히 덮는다.
  색은 `brand`(green500, 1.82:1) 가 아니라 **`brand-hover`**(green600, 3.10:1)다 — 초점 표시도
  3:1 을 요구한다(WCAG 1.4.11).
- **다크 모드는 도입하지 않는다.** 팔레트가 밝은 톤 전제로 설계됐고, 지금 단계에서 두 벌을
  유지할 이유가 없다. 필요해지면 시맨틱 토큰만 다시 정의하면 되도록 구조는 열어 뒀다.

## 카드뉴스 (docs/cardnews)

이 저장소는 웹 서비스 말고 **인스타그램 카드뉴스**(1080×1350 PNG + 캡션)도 만든다.
앱 코드와 섞이지 않는다 — `docs/` 아래에만 있고 빌드·테스트·배포에 들어가지 않는다.

**규칙은 [docs/cardnews/CLAUDE.md](docs/cardnews/CLAUDE.md) 에 있고, 치수표와 고친 사례는
[docs/cardnews/README.md](docs/cardnews/README.md) 다.** 카드뉴스 파일을 다룰 때 자동으로
읽히므로 **여기로 되옮기지 말 것** — 앱 작업 세션마다 로드되는 것을 피하려고 나눴다.

**앱의 표현 규칙과 서로 옮기지 않는다.** 수치·식품 이름 금지(`lib/saju/constitution.ts` ·
`lib/prompt.ts`)는 카드뉴스에 걸리지 않고, 반대로 표지의 구어체·`ㅠㅠ` 를 앱으로
가져오지 않는다.

## 작업 방식

할 일은 [docs/TASK.md](docs/TASK.md) 에서 `[TASK-NN]` 단위로 관리한다.
착수 전 해당 태스크를 읽고, 끝나면 체크박스와 상태를 갱신한다.
**TASK.md 에는 태스크만 둔다.** 범례·규칙·배경 설명·끝낸 일의 결론은 전부 이 파일에 쓴다.

### 끝낸 태스크의 결론은 어디에 있나

**태스크 본문은 지운다. 결론만 남긴다.** 같은 결정을 다시 하지 않으려면 그 결론이 어디
사는지 찾을 수 있어야 해서 색인만 둔다 (TASK-45~50 · 53~60 · 2026-08-18 완료).

| 무엇 | 결론이 사는 곳 |
| --- | --- |
| 풀이 서술 품질 측정 도구·기준선 | `docs/saju-validation.md` 3-6 절 · `scripts/sample-readings.mjs` |
| 추론 예산 (`thinkingLevel`) | 위 "LLM 취급" · `lib/gemini.ts` 주석 |
| hedge 경계 · 절 구성 · 말투 | 위 "판정은 단정…" · "분량은 절마다…" · "말투는 리포트가…" |
| 유형 일곱 (공개 다섯) | 아래 "확정된 결정사항" |
| 대운·종목 판정과 우리 관례 | `docs/saju-validation.md` 3-3-1 · 3-4-1 절 |
| 계약 제목은 재현하기 쉬운 낱말로 | 위 "계약 제목은…" (`움직-` 어간 실측) |
| 유형 사진(카드·히어로)과 리스트 카드 | 위 "유형 선택은 리스트 카드다" · "유형 사진 — 두 화면이 같은 그림을 쓴다" |
| 금지 낱말 부정문 누수 | 위 "금지 낱말이 부정문으로…" · `docs/saju-validation.md` 마지막 절 |
| dasii 부품 규격·웹폰트 | `docs/design-tokens.md` · `public/fonts/README.md` |
| `결` 을 아예 뺀 판단과 실측 | 위 "`결` 은 화면에도…" · `docs/saju-validation.md` 마지막 절 |
| 근거 카드 셋을 모두 접은 판단 | 위 "풀이가 먼저, 도식은 근거로 뒤에" |
| dasii 웹 셸(좌측 패널·3열)과 헤더를 두 번 뒤집은 판단 | 위 "홈으로 돌아가는 동선" · `components/AppPanel.tsx` |
| 문단 묶음 문턱(80자)과 실측 | 위 "문장 묶음 단위 줄바꿈" · `lib/reading/line-breaks.ts` |
| 카드뉴스 서술·양식·사진 규칙 | `docs/cardnews/CLAUDE.md` · `docs/cardnews/README.md` |
| 카드뉴스 B_style 실측 치수 | `docs/cardnews/README.md` 의 "B_style 쓰는 법" · `card-b.css` |
| 결과 화면 순서(좋아요·공유)와 근거 묶음 이름 | 위 "풀이가 먼저, 도식은 근거로 뒤에" |
| 첫 화면 속도 실측과 `priority` 판단 (`/` · `/reading/*`) | 위 "배포" · `scripts/measure-load.mjs` |
| 좌측 패널 로고를 원본 자산으로 바꾼 판단 | `public/dasii/README.md` · `components/AppPanel.tsx` |
| 선택지 둘을 칩으로 바꾼 판단과 접근성 | 위 "컬러 토큰과 부품 규격" · `components/ui/ChoiceChips.tsx` |
| 판정 라벨마다 사진을 붙인 판단 | 위 "판정 한 줄 콜아웃" 하위 항목 · `scripts/fetch-verdict-photos.mjs` |
| 공유 버튼 둘을 한 줄에 앉힌 판단 | 위 "결과 공유" · `components/ShareActions.tsx` |
| 아이콘 버튼 규격(44px 원형)과 접힌 폼 요약 줄 | 위 "컬러 토큰과 부품 규격" · "두 단계 플로우" · `docs/design-tokens.md` |
| 전역 헤더·유형 줄을 둘 다 되돌린 판단 | 위 "홈으로 돌아가는 동선" · "화면 1 구조 — 히어로 사진" |
| 캐릭터를 히어로 사진으로 바꾼 판단과 실측 | 위 "화면 1 구조 — 히어로 사진" · "유형 사진 — 두 화면이 같은 그림을 쓴다" |
| 히어로를 열 맨 위까지 올리고 `<` 를 그 위에 얹은 판단 | 위 "화면 1 구조 — 히어로 사진" · `components/ui/Button.tsx` 의 `surface` |
| 폼의 보조 설명 줄을 걷어내고 출생지 기본값을 둔 판단 | 위 "폼의 보조 설명은 기본값으로 대체한다" |
| 판정 사진 배치 실측(크롭·긴 라벨·중복 사진) | 위 "판정 한 줄 콜아웃" 하위 항목 · `lib/reading/verdict-photo.test.ts` |
| 화면 사이 좌우 밀림 전환 | 위 "화면 사이 전환은 CSS View Transitions 다" · `components/PageTransition.tsx` |
| 만세력 계산 기준을 결과 화면으로 내린 판단 | 위 "풀이가 먼저, 도식은 근거로 뒤에" 의 근거 카드 넷 항목 · `components/CalculationBasis.tsx` |
| `diet-food` 라벨을 재료 이름으로 바꾼 판단 | 위 "판정 한 줄 콜아웃" 의 `diet-food` 항목 · `lib/saju/constitution.ts` 의 `ELEMENT_FOOD` |
| `/` 순차 등장과 opacity·LCP 실측 | 위 "`/` 는 위에서부터 차례로 떠오른다" · `lib/design/motion.test.ts` |
| 리스트 카드 글 폭이 사진과 겹치던 문제 | 위 "유형 선택은 리스트 카드다" 의 글 폭 항목 · `lib/form/birth-input.test.ts` |

보드는 **위에서 아래로 우선순위 순**이다. 사용자 확인 없이 순서를 건너뛰지 말고,
순서를 바꿀 때는 왜 그런지를 태스크 본문에 남긴다. 태스크에 딸린 "이 순서인 이유" 문단이
선행 관계를 담고 있으므로 착수 전에 읽는다. **번호 순서와 우선순위는 다르다** — 지금 틀린
값이 나가고 있는 문제는 번호가 뒤여도 맨 위로 올린다.

### 모델 배정 범례

| 표기 | 모델 | 쓰는 경우 |
| --- | --- | --- |
| **(O)** | Opus 5 | 도메인 정확도가 걸린 계산, 아키텍처 설계, 애매한 리팩터, 프롬프트 설계 — 틀리면 되돌리기 비싼 작업 |
| **(S)** | Sonnet 5 | 스펙이 정해진 기능 구현, API 연동, 테스트 작성, 리팩터 실행 — 대부분의 작업 |
| **(H)** | Haiku 4.5 | 문안 수정, 스타일 정리, 반복 치환, 문서 갱신 — 판단이 거의 필요 없는 작업 |

`/model` 로 전환한다. 배정은 권고이며, 막히면 한 단계 올린다.

### 상태 표기

`⬜ 대기` · `🟡 진행중` · `✅ 완료` · `⏸ 보류` · `❌ 취소`

계정·플랜이 있어야 착수할 수 있는 태스크는 제목 끝에 `🔒 <무엇이> 필요` 를 붙인다.
계정 없이 시작하면 중간에 막히므로 착수 전에 보이는 곳에 적어 둔다.

### 새 태스크 추가 형식

```markdown
### [TASK-NN] 제목 · **(O|S|H)** · ⬜ 대기

- [ ] 체크 가능한 작업 단위
- [ ] ...

→ 완료 기준: 무엇이 되면 끝인가 (검증 가능한 문장).
```

### 그 밖의 규칙

- **완료된 태스크는 TASK.md 에서 삭제한다.** 보드에는 남은 일만 둔다.
  번호는 재사용하지 않는다 — **다음 번호는 TASK.md 맨 아래에만 적는다** (여기 적으면 두 곳이
  반드시 어긋난다).
- **보류로 돌린 태스크는 TASK.md 에서 지운다.** 다시 하기로 하면 새 번호로 만든다.
- 새 UI 문자열은 한국어로 쓴다.
- 커밋 메시지는 한국어 요약 + 관련 태스크 번호. 예: `결과 공유 OG 이미지 추가 (TASK-10)`
- 작업을 마치면 `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` 를
  통과시킨 뒤 완료로 보고한다. 같은 네 개를 `.github/workflows/ci.yml` 이 푸시·PR 마다 돌린다.

## 확정된 결정사항

- **유형은 일곱이고 그중 다섯이 공개다** (2026-08-14 확정 · TASK-39 · 40 · 41 · 44 /
  2026-08-18 `decade`·`exercise` 추가 · TASK-45 · 48 / 같은 날 `decade` 를 내리고
  `diet-food` 를 더했다 · TASK-62 · 63).
  공개: `종합 체질 풀이`(`diet`) · `내가 살이 찌는 이유`(`gain-cause`) ·
  `나에게 맞는 다이어트 방법`(`diet-method`) · `나에게 맞는 다이어트 식단`(`diet-food`) ·
  `나에게 맞는 운동`(`exercise`).
  내부: `종합 사주 풀이`(`general`) · `몸이 바뀌는 10년`(`decade`).
  - **다이어트 계열은 묻는 것이 서로 다르다.** `diet` 는 **몸이 어떤 쪽인가**,
    `gain-cause` 는 **왜 붙는가**, `diet-method` 는 **무엇을 어떻게 하는가**,
    `diet-food` 는 **무엇을 먹는가**, `exercise` 는 **어떤 운동을 하는가**.
    `READING_TYPES` 순서가 UI 순서이므로 몸 → 원인 → 방법 → 식단 → 운동으로 둔다.
  - **`올해 운세`(`yearly`) 는 유형에서 없앴다.** 2026-08-13 에는 "셋을 모두 유지한다" 였으나,
    세운 판정의 작용 축이 **원래 몸 쪽 값**이었다는 것이 뒤집은 이유다 — `analyzeYearly` 는
    `constitution.deficient`/`excess` 에서 작용을 계산하므로, 그 판정을 운세 유형에만
    흘려보내던 쪽이 이상했다. 지금은 `diet` 의 "올해의 몸 흐름" 이 그 판정을 근거로 쓴다.
  - **`lib/saju/yearly.ts` 는 남긴다.** 사라진 것은 유형뿐이다. 판정은 계속 계산된다.
  - **`diet` 의 id 는 바꾸지 않는다.** 라벨만 `종합 체질 풀이` 로 바뀌었다. 세그먼트가 곧
    URL 이라 id 를 바꾸면 이미 공유된 `/reading/diet` 링크와 공유 카드가 죽는다.
  - **`/reading/yearly` 는 404 가 아니라 `/` 로 리다이렉트한다** (`next.config.ts`).
    한때 유효했고 검색·공유 문구까지 있던 URL 이라 밖에 링크가 남아 있을 수 있다.
    목적지가 `/reading/diet` 가 아닌 이유: 운세를 보러 온 사람에게 체질 풀이를 들이미는 것은
    다른 것을 준 것이다. 유형 선택 화면에서 직접 고르게 한다.
  - **`나에게 맞는 다이어트 방법`(`diet-method`) 을 더했다** (2026-08-14 · TASK-40).
    `diet` 는 **몸이 어떤 쪽인가**(체질·패턴·올해 흐름), `diet-method` 는 **그래서 무엇을
    어떻게 하는가**(순서·움직임 종류·식사 순서·실행 조건)를 맡는다. 섹션 id 는 겹치지
    않는다 — 옮긴 것이지 공유하는 것이 아니다. `SECTION_INSTRUCTION` 이 id 로 매기므로
    id 를 공유하면 맥락이 다른 두 유형이 한 지시문을 떠안는다.
  - **`내가 살이 찌는 이유`(`gain-cause`) 를 더했다** (2026-08-14 · TASK-44). 판정 축은
    하나도 늘리지 않았다 — `gainSite` · `gainPattern` · `metabolism` · `dominantGroup` ·
    오행 과부족이 이미 다 있었고, **조립과 서술 각도만** 다르다. `constitution.ts` 를
    건드리지 않았으므로 새 동점 처리도 없다.
    - **`diet` 의 `살이 붙는 패턴` 절은 그대로 뒀다.** `diet-method` 때와 달리 절을
      떼어 오지 않은 이유는 `diet` 가 **"종합"** 이라는 약속을 지켜야 하기 때문이다.
      대가는 같은 판정(`gainPattern`)을 두 유형이 쓴다는 것이라, **섹션 id 를 나누고
      지침이 서로 다른 것을 요구하게** 해서 같은 문장이 두 번 나오지 않게 막는다
      (`prompt.test.ts` 가 두 절 지침을 대조한다).
    - **원인 유형은 세운 판정을 받지 않는다.** 원인은 원국 쪽 이야기이고 올해 흐름은
      `diet` 몫이다. 체질 판정 블록만 받는다.
    - **표현 범위를 넓히지 않았다** — 수치·처방·식품 이름 전부 계속 막는다. 그래도
      면책 고지에 한 문단을 더했다: 제목이 인과를 함축하므로 **"원인을 진단하지 않는다"**
      를 밖에서도 약속해야 한다. 표현 규칙을 손대면 그 문단도 같은 커밋에서 고칠 것.
  - **`general` 은 메인에서 내려 `/admin` 에서만 들어간다** (2026-08-14 · TASK-41).
    나중에 없앨 유형이지만 그때까지 다른 사주 서비스와 결과를 대조하는 데 쓴다.
    `READING_TYPES` 에서 빼지 않는 이유는 그러면 `Record<ReadingType, …>` 가 general 몫을
    전부 지우라고 하고, 되살릴 때 프롬프트·섹션 계약을 다시 써야 하기 때문이다.
    지금 필요한 건 노출 제어뿐이다 — 자세한 것은 아래 "노출 구분" 절.
  - **`몸이 바뀌는 10년`(`decade`) 은 더했다가 같은 날 내렸다** (TASK-45 → 62).
    만든 이유는 공개 유형 셋이 전부 **현재 시점의 원국**만 말하고 시간축은 `diet` 의 한
    절뿐이었기 때문이다. 내린 이유는 **다이어트와 묻는 축이 다르다**는 것 — 나머지 넷은 몸을
    다이어트 축으로 묻는데 이것만 시간축을 묻는다.
    - **`internal` 로 내렸을 뿐 지우지 않았다.** `general` 과 같은 판단이다 —
      `READING_TYPES` 에서 빼면 `Record` 가 몫을 전부 지우라고 하고, 되살릴 때 섹션 계약
      5절과 프롬프트 지침을 다시 써야 한다. `/admin` 에서 계속 들어갈 수 있다.
    - **`app/disclaimer/page.tsx` 의 10년·성별 문단은 남겼다.** 유형이 여전히 접근 가능하고
      성별 요구 기계도 그대로라 그 약속은 사실이다. **완전히 지울 때 함께 뺀다.**
    - `lib/saju/decade.ts` 는 그대로다 — `findCurrentDaeun` 을 `DaeunTimeline`·`ResultView`·
      `card-model` 셋이 쓰고 대운 도식도 화면에 남아 있다 (`yearly.ts` 와 같은 취급).
    - **시간축 유형이 없다고 다시 만들지 말 것.** 위 이유로 내린 것이다.
    - **판정 축은 하나만 늘었다** — 위 "대운(10년) 판정" 절. `analyzeYearly` 의 규칙을
      그대로 대운에 적용한 것이라 새 임계값이 없다.
    - **이 유형만 성별을 요구한다.** 대운 방향이 성별로 정해지기 때문이고, 임의로 순행을
      정하지 않는다. `READING_TYPE_NEEDS_GENDER` 가 그 사실을 든다.
    - **`diet` 의 "올해의 몸 흐름" 을 떼어 오지 않았다.** 층이 다르다 — 올해와 10년이다.
      대가는 둘 다 시간을 말한다는 것이라, `gain-cause` 때처럼 **섹션 id 를 나누고 지침이
      서로 다른 것을 요구하게** 했다 (`prompt.test.ts` 가 두 절 지침을 대조한다).
  - **`나에게 맞는 다이어트 식단`(`diet-food`) 을 더했다** (2026-08-18 · TASK-63).
    컨셉은 **"다이어트의 열에 아홉은 식단"** 이고 그 문장이 카드 설명 첫 줄이다.
    - **숫자를 쓸 수 없어 "90%" 가 아니라 "열에 아홉" 이다.** `schema.test.ts` 가 모든 유형의
      카드·검색 문구를 `/\d/` 로 막는다 — 다이어트 계열에서 숫자는 곧 처방으로 읽힌다.
      **그 검사를 완화하지 말 것.** 이 문장은 다이어트 일반에 대한 주장이지 이 사람 몸에
      대한 인과가 아니라 TASK-55 의 (ii) 경계에는 걸리지 않는다.
    - **판정 축은 늘지 않았다.** 재료는 `ELEMENT_FOOD`(오행 과부족), 조리·온도는
      `THERMAL_GUIDE`(한열)에서 그대로 온다. `constitution.ts` 를 건드리지 않았으므로
      새 동점 처리가 없다 (`gain-cause`·`exercise` 와 같은 방식).
    - **`diet-method` 의 `eating` 절을 떼어 오지 않았다.** 방법 유형에서 먹는 순서를 빼면
      "무엇을 어떻게" 가 반쪽이 된다. 대신 **층을 나눴다** — `diet-method` 는 **순서와 시각**,
      `diet-food` 는 **재료 범주·조리·온도**다. `eating` 지침에 "이 절의 중심은 순서와
      시각이다" 를 못 박았고 `prompt.test.ts` 가 두 지침을 대조한다.
    - **이 유형만의 위험은 목록 밖 식품이 새는 것이다.** 이름이 "식단" 이라 모델이 메뉴를
      지어내려는 압력이 가장 세다. 금지를 **서두와 표현 규칙 두 곳**에 두고 테스트가 본다.
    - **과다 절(`무엇이 이미 충분한가`)을 "덜어내라" 로 쓰지 않는다.** 알레르기·지병·복약·
      임신 여부를 모르므로 표현 범위는 **"굳이 더 늘리지 않아도"** 까지다. 절 제목도 그래서
      `무엇을 덜어낼까` 가 아니다.
    - **콜아웃은 비워 뒀다.** 재료 범주는 과부족 오행 수만큼 여러 개라 한 줄로 1:1 파생되지
      않는다. 억지로 고르면 동점 처리를 새로 정해야 한다.
    - `app/disclaimer/page.tsx` 가 "식이 처방이 아니다 · 알레르기·지병·복약·임신 여부를
      모른다" 를 밖에서 약속한다. **표현 범위를 손대면 그 문단도 같은 커밋에서 고칠 것.**
  - **`나에게 맞는 운동`(`exercise`) 을 더했다** (2026-08-18 · TASK-48). `diet-method` 가
    **무엇을 어떤 순서로** 를 다루는 데 비해 이 유형은 **어떤 운동을** 하나로 좁혀 콜아웃으로
    보여준다. **판정 축은 늘지 않았다** — 대표 종목이 `MOVEMENT_PLAN` 의 필드이고
    `movementKind` 에서 1:1 파생이라 새 동점 처리가 없다.
    - **`diet-method` 의 `movement` 절을 떼어 오지 않았다.** 방법 유형에서 움직임을 빼면
      "무엇을 어떻게" 가 반쪽이 된다 (`diet` 에 `gain-pattern` 을 남긴 것과 같은 판단).
      대신 지침이 서로 다른 것을 요구한다 — `diet-method` 는 **종류까지**이고 대표 종목을
      콕 집지 않으며, `exercise` 는 **종목·강도·시간대·주의까지**다.
    - **종목 선택에 한열을 넣지 않았다.** 콜아웃이 두 표에서 각자 가져와 합친다
      (종목 = `MOVEMENT_PLAN`, 실행 조건 = `THERMAL_GUIDE`). 근거표는
      `docs/saju-validation.md` 3-3-1 절.
    - **먹는 이야기를 받지 않는다.** 식품·식사 순서·끼니 시각은 `diet-method` 몫이다.
    - **세운·대운 판정도 받지 않는다.** 시간 이야기는 `diet`(올해)와 `decade`(10년) 몫이다.
    - **지병·부상·임신 여부를 모른다는 것을 밖에서도 약속한다** (`app/disclaimer/page.tsx`).
      종목을 콕 집어 권하는 유형이라 운동 처방으로 읽힌다. **표현 범위를 손대면 그 문단도
      같은 커밋에서 고칠 것.**
  - 새 기능은 **일곱 유형 모두**에 적용되는지 확인한다 (`Record<ReadingType, …>` 가 잡아 준다).
  (궁합·직업 적성은 도입하지 않았다 — 궁합은 입력이 2인분이 되어 개인정보 처리방침 1항까지
  손대야 하고, 직업 적성은 십신→직업 매핑표를 새로 만들어야 한다.)
- **브랜드 컬러는 그린** (같은 날 확정). 저장소 이름대로 건강·다이어트 톤으로 간다.
  종합 풀이도 같은 팔레트를 쓴다 — 유형별로 테마를 갈지 않는다.
- **신살(神殺)은 도입하지 않는다** (2026-08-14 확정). 공망·역마·도화·양인·괴강·백호대살·
  귀문관살·원진·12신살 어느 것도 계산하지 않는다. 지금 `lib/saju/` 에 합·충이 없는 것과 같다.
  → **막힌 곳은 계산이 아니라 의미다.** 대부분 지지 대조표라 결정론적이고 "사실은 코드가
  만든다" 원칙은 지킬 수 있다 — 보류 중인 삼합보다 오히려 쉽다 (반합 같은 폭발이 없다).
  - **신살 → 몸·식습관 대응표가 고전에 없다.** 역마가 "이동·출장" 인 것은 고전이지만 그것을
    "그래서 유산소" 로 옮기는 근거는 어디에도 없다. 양인→근력, 도화→외모 동기도 전부 우리
    발명품이 된다. 판정 축 다섯은 오행 과부족·조후 같은 **몸 쪽 근거**에서 나오는데 신살은
    그 계열이 아니다 — 위 "고전 규칙과 우리 관례를 섞지 말 것" 에 정면으로 걸린다.
  - **이름 자체가 면책 고지를 깬다.** 귀문관살의 고전적 의미는 예민·불면·강박이고 정신질환까지
    쓰는 책이 있다. 백호대살은 사고·수술·피다. 우리가 순화해 써도 사용자가 화면의 그 이름을
    검색하면 원래 해석을 만나므로, `app/disclaimer/page.tsx` 의 "의학적 진단이 아니다 ·
    미래 예측으로 받아들이지 마세요" 가 무력해진다. 살 빼는 법을 보러 온 사람에게
    "정신 계통 조심" 을 준 셈이 된다.
  - **판본이 갈린다.** 귀문관살 쌍 목록이 책마다 다르고, 12신살·도화는 기준을 년지로 볼지
    일지로 볼지가 유파마다 갈린다. 도입하면 그 선택마다 `docs/saju-validation.md` 에
    "이건 우리 관례" 를 적어야 한다.
  - **착수 신호는 삼합과 같다** — 다른 만세력과 결과가 어긋나는 사례가 나올 때. 다만 신살은
    세력 판정을 바꾸지 않으므로(공망 정도가 예외) 그런 사례가 나올 가능성은 삼합보다 낮다.
    **생활 영역 유형이 생기면 다시 볼 것** — 위 두 번째 이유는 여전히 남지만 첫 번째는 풀린다.
- **관측은 서버 로그까지만 한다** (2026-08-13 확정). 에러 추적 서비스도, **제3자** 방문자
  분석 도구도 붙이지 않는다 — Google Analytics 류의 스크립트를 넣지 않는다.
  → 그래서 개인정보 처리방침이 **"외부 방문자 분석 도구를 전혀 쓰지 않습니다"** 를 약속할 수
  있다(5항). 스크립트를 하나라도 넣으면 그 문장이 거짓이 되므로 5항·4항을 같은 커밋에서
  고쳐야 한다. 실패는 `lib/observability.ts` 의 구조화 로그로 Vercel Runtime Logs 에서 본다.
  - **예외 하나 — 유형별 누계는 우리가 직접 센다** (2026-08-14 · TASK-51). 아래 "조회수와
    좋아요" 절. 2026-08-13 판에는 "페이지 조회수를 세는 스크립트조차 넣지 않는다" 가 있었고
    그 문장은 이제 사실이 아니다. **제3자에게 보내지 않는다는 부분은 그대로다.**

## 미확정 결정사항

착수 전에 사용자와 확정해야 하는 것들. 정해지면 위 절로 옮긴다.

- **수익화 여부**: 광고나 유료 결제가 들어가면 **레이트 리밋 영속화** 우선순위가 올라가고,
  법적 고지 문서(`app/privacy`, `app/disclaimer`)에 결제·광고 관련 내용을 추가해야 한다.
  Gemini 유료 등급으로 올리면 "무료 등급이라 Google 이 학습에 쓸 수 있다"는 고지도 걷어낸다.
  → **결제는 키가 아니라 Google 프로젝트 단위로 붙는다.** 그래서 이미지 생성 같은 곁가지에
  결제를 붙일 때 **서비스 키와 같은 프로젝트에서 키를 발급하면 안 된다** — 풀이 생성 키까지
  유료 등급이 되어 위 고지가 사실과 어긋난다. 곁가지는 별도 프로젝트의 별도 키로 하고
  Vercel 환경변수로 올리지 않는다.

## 배포 (Vercel)

- 프로덕션: https://diet-saju.vercel.app
- `main` 브랜치 푸시 → 자동 배포. 프리뷰는 PR 단위로 생성된다.
- 함수 리전은 `icn1`(서울)로 지정되어 있다. `X-Vercel-Id` 에서 **확인할 것은 두 번째 구간**이다
  (`kix1::icn1::...` 처럼 앞이 다를 수 있다 — 앞은 요청을 받은 엣지 지점이라 오사카로 잡히기도
  한다. 함수가 서울에서 돌았는지는 `icn1` 이 두 번째에 있는지로 본다).
  응답 시간 대부분은 Gemini 생성 시간이며 네트워크 왕복은 0.1~0.8초다.
- **프로덕션 실측 (2026-08-19 · `gemini-3.5-flash-lite` · 공개 유형 5 × 2회 = 10건).**
  수치를 옮겨 적을 때는 **모델명과 측정일을 함께** 적을 것 — 그 둘이 없어서 세 곳이 어긋난
  적이 있다 (`thinkingLevel` 사례).

  | 구간 | 값 |
  | --- | --- |
  | 원국 (웜) | **85~143ms** (평균 106ms) |
  | 원국 (콜드 스타트) | **549ms** — 첫 요청 한 번뿐이고 그다음부터 웜 |
  | 첫 글자 | **0.93~1.62초** (평균 1.26초) |
  | 전체 | **6.5~9.9초** (평균 8.1초) |
  | 산출 분량 | 1,853~2,824자 |

  유형별 전체 평균은 `diet` 8.7초 · `exercise` 8.4초 · `gain-cause` 8.0초 ·
  `diet-food` 7.9초 · `diet-method` 7.3초로 **유형 간 차이가 크지 않다.**
  10건 모두 `X-Vercel-Id` 두 번째 구간이 `icn1` 이었고 `error` 이벤트는 0건이다.

  **2026-08-13 값과 견주면**: 원국 985ms → 106ms(그때 값은 콜드 스타트를 포함한 수치였다),
  첫 글자 1.8초 → 1.26초, **전체 5.8초 → 8.1초.** 전체가 길어진 것은 느려진 것이 아니라
  **분량이 두 배가 되었기 때문이다**(TASK-56). 자수당으로 보면 오히려 빨라졌다.

  **`maxDuration = 30` 에 여유가 있다** — 최대 관측치가 9.9초다. 유형이 늘어난 것 때문에
  타임아웃이 가까워지지는 않았다.
- **`/` 첫 화면 실측 (2026-08-19 · TASK-87 · 사진 다섯 장이 붙은 뒤).**
  `node scripts/measure-load.mjs http://localhost:3100/` 로 잰다 — **프로덕션 빌드**
  (`npm run build && npx next start -p 3100`) · 390px · 1.6Mbps · RTT 150ms.
  조건이 코드에 있으므로 다시 잴 때 같은 조건이 나온다. **dev 서버로 재지 말 것.**

  | 조건 | FCP | LCP |
  | --- | --- | --- |
  | 첫 방문 · DPR 2 (2026-08-20 · 등장 애니메이션 + 앞 세 장 preload) | 0.66~0.70초 | **0.72초** |
  | 첫 방문 · DPR 2 (2026-08-19 · 첫 장만 preload) | 0.59~0.63초 | 0.72초 |
  | 첫 방문 · DPR 3 (2026-08-19) | 0.66초 | 0.69초 |
  | 재방문(캐시 살림) | 0.3~0.6초 | 0.3~0.6초 |

  - **LCP 요소는 "가장 큰 카드" 의 사진이다.** 제목이 아니다 — 사진이 카드 오른쪽 면을
    채우므로 화면에서 가장 큰 요소다. **그런데 어느 카드인지는 고정이 아니다** —
    카드 높이가 제목·설명 줄 수에 따라 달라서, 지금은 제목이 두 줄인
    `diet-method`(셋째)가 가장 크다. 예전에는 첫 카드였다.
  - **그래서 `priority` 는 앞의 셋에 준다** (TASK-104 재실측 · 2026-08-20).
    첫 장에만 주던 때는 **LCP 1.48초**였다 — preload 된 첫 장이 0.57초에 오는데
    정작 LCP 요소인 셋째 장이 1.43초에 왔기 때문이다. 셋에 주면 **0.65~0.72초**다.
    - **다섯 장 전부에 줘도 같았다**(0.65초 · 같은 날). 옛 경고("preload 가 서로를
      밀어내 첫 장이 늦어진다")는 **지금 레이아웃에서는 재현되지 않는다** — 사진이
      8~33KB 로 작아서다. 그래도 셋으로 두는 이유는 390px 에서 **접히는 자리까지가
      셋**이라, 스크롤하지 않을 수도 있는 장을 미리 받지 않기 위해서다.
    - **카드 높이가 바뀌면 LCP 요소도 바뀐다.** 설명 줄 수·제목 줄 수를 건드리는
      변경(TASK-99 의 글 폭, TASK-103 의 제목)은 **이 값을 다시 재야 한다.**
  - **저장소의 사진 크기는 사용자에게 가지 않는다.** `diet-food.jpg` 는 디스크에서 326KB
    지만 화면에 가는 것은 `next/image` 가 384px webp 로 다시 인코딩한 **8KB** 다. 다섯 장
    합계가 78KB(DPR 2) · 115KB(DPR 3)이고 **가장 무거운 자산은 여전히 웹폰트 112KB** 다
    (2.4초에 도착). 사진을 더 줄이는 것은 지금 의미가 없다 — 줄일 것은 저장소 용량뿐이다.
  - 최적화된 이미지는 `Cache-Control: public, max-age=14400, must-revalidate` 로 나간다.
    아래 `public/` 원본의 `max-age=0` 과 **다른 경로다** — 그쪽은 폰트·QR 처럼 최적화를
    거치지 않는 자산에 적용된다.
  - **재기 전에 CSS 청크가 200 인지 확인할 것.** 같은 저장소에서 다른 세션이
    `npm run build` 를 돌리면 이미 실행 중인 `next start` 가 사라진 청크를 가리켜
    **스타일 없는 화면**이 되고 숫자가 통째로 무의미해진다. 두 번 속았다 — 한 번은
    "priority 를 줘도 30ms 밖에 안 좋아진다" 는 틀린 결론까지 냈다. 서버를 다시 띄울
    때는 `netstat -ano | grep :3100` 으로 **PID 를 찾아 죽여야 한다**(`EADDRINUSE` 로
    조용히 실패하고 낡은 서버가 계속 응답한다).
- **`/reading/[type]` 첫 화면 실측 (2026-08-20 · TASK-92 · 히어로 사진이 붙은 뒤).**
  같은 스크립트·같은 조건이다 (`node scripts/measure-load.mjs http://localhost:3100/reading/diet
  --runs 3` · 390px · DPR 2 · 1.6Mbps · RTT 150ms · 3회 중앙값).

  | 조건 | FCP | LCP |
  | --- | --- | --- |
  | `priority` 있음 | 0.67초 | **0.69초** |
  | `priority` 없음 | 0.59초 | **1.40초** |
  | 사진을 열 맨 위로 (TASK-97 · `priority` 있음) | 0.62초 | **0.63초** |

  - **사진을 맨 위로 올려도 LCP 가 나빠지지 않는다** (TASK-97). 슬롯 크기가 그대로
    200px 이라 받는 바이트가 같고, 오히려 그리는 자리가 위로 올라가 조금 빨라졌다.
    **슬롯을 키우는 변경이라면 이야기가 다르다** — 그때는 원본 480 유지 판단까지 함께
    다시 재야 한다 (위 "유형 사진" 절).

  - **LCP 요소는 히어로 사진**(`/cards/<type>.jpg`)이다. 제목이 아니다 — 열 폭을 채우므로
    화면에서 가장 큰 요소이고 `/` 첫 카드와 같은 이유다. **710ms 차이라 노이즈가 아니다.**
  - `priority` 를 빼면 FCP 는 0.08초 빨라진다(preload 가 없으니 첫 페인트를 덜 붙잡는다).
    **그 0.08초로 LCP 0.71초를 사는 거래는 하지 않는다.**
  - **사용자가 받는 사진은 13KB 다.** 요청은 `w=828` 로 나가지만 최적화기가 원본 폭(480)
    에서 멈춘다 — 그래서 원본을 키우면 **저장소만이 아니라 전송량도** 오른다 (위 "유형
    사진" 절의 480 유지 판단).
  - 여전히 **가장 무거운 자산은 웹폰트 112KB** 이고 2.35초에 도착한다 (아래 항목).
- **웹폰트는 첫 화면을 늦추지 않는다** (TASK-75 · 같은 날 실측). 390px · 1.6Mbps · RTT
  150ms 로 조여서 `/reading/diet` 를 재면 **FCP 1.29초**, 폰트는 1.22초에 요청돼
  **3.19초에 도착한다.** 즉 글자는 시스템 폰트로 먼저 그려지고 제목만 나중에 바뀐다 —
  `font-display: swap` 이 의도대로 동작한다. **다만 느린 회선에서 약 1.9초 동안 제목이
  폴백으로 보이다가 바뀐다.** 그게 거슬린다는 말이 나오면 그때 다시 볼 것.
  - **`public/` 자산은 `Cache-Control: public, max-age=0, must-revalidate` 로 나간다**
    (Next 기본값). 재방문마다 조건부 요청 한 번(304)이 붙는다 — 파일 이름에 해시가 없어서
    길게 캐시하면 갱신이 안 먹으므로 지금은 그대로 둔다. **바꾸려면 파일명에 해시를 넣는
    것이 먼저다.**
- 환경변수는 Vercel Dashboard > Settings > Environment Variables 에 등록한다
  (`GEMINI_API_KEY` 는 Production/Preview/Development 모두 필요).
- **Vercel CLI 는 배포 계정과 다른 계정으로 로그인되어 있다.** CLI 로 환경변수를 읽거나
  고칠 수 없으니 대시보드에서 처리한다. `vercel link --yes` 를 쓰지 말 것 — 기존 프로젝트를
  찾는 게 아니라 **같은 이름의 빈 프로젝트를 새로 만든다** (실제로 한 번 만들어졌다).
  `vercel link`/`vercel env pull` 은 `.env.local` 을 덮어쓰고 `.gitignore` 에 `.env*` 를
  덧붙인다. 후자는 나중 줄이라 `!.env.example` 예외를 무력화하므로 특히 주의.
- 프로덕션 모델이 코드 기본값과 다를 수 있다. 확인은 `POST /api/saju` 응답의 `model` 필드로 한다.
- `/api/saju` 는 Node 런타임 고정이다 (`lunar-javascript` 가 CommonJS). Edge 로 바꾸지 말 것.
- 함수 타임아웃은 `maxDuration = 30`. Hobby 플랜 상한을 넘기지 않도록 주의한다.
