@AGENTS.md

# diet-saju

생년월일시로 **사주 원국(사주팔자)** 을 계산하고, Gemini(무료 등급)로 해석문을 생성해 보여주는 웹 서비스.
Next.js App Router 단일 앱이고 Vercel 에 배포된다.

> `@AGENTS.md` 는 `next dev` 가 자동 관리하는 블록이다. 직접 수정하지 말 것.

## 규칙이 사는 곳

이 파일은 **전체 구조와 경계**만 담는다. 세부 규칙은 작업하는 디렉터리의 파일을 읽는다.

| 무엇을 고치나 | 읽을 파일 |
| --- | --- |
| 화면·색·부품·모션·사진 (`components/` · `app/`) | [components/CLAUDE.md](components/CLAUDE.md) |
| 판정·프롬프트·만세력·섹션 계약 (`lib/`) | [lib/CLAUDE.md](lib/CLAUDE.md) |
| 인스타그램 카드뉴스 (`docs/cardnews*`) | [docs/cardnews/CLAUDE.md](docs/cardnews/CLAUDE.md) |
| 만세력 검증·판정 근거표·표본 기준선 | [docs/saju-validation.md](docs/saju-validation.md) |
| 팔레트 원본/파생과 대비 근거 | [docs/design-tokens.md](docs/design-tokens.md) |
| 남은 할 일 | [docs/TASK.md](docs/TASK.md) |

**앱의 표현 규칙과 카드뉴스 규칙을 서로 옮기지 않는다.** 수치·식품 이름 금지는 카드뉴스에 걸리지
않고, 반대로 표지의 구어체를 앱으로 가져오지 않는다.

## 명령어

```bash
npm run dev        # 개발 서버 (3000 사용 중이면 3001)
npm run build      # 프로덕션 빌드
npm run typecheck  # tsc --noEmit
npm run lint       # eslint

npm test              # Vitest 전체 (935건 · 약 16초)
npm run test:watch    # 감시 모드
npm run validate:saju # 사주 계산 테스트만
```

테스트는 `lib/**/*.test.ts` 에 코드와 나란히 둔다. `vitest.config.mts` 가 `server-only` 를 빈 모듈로
별칭 처리한다 — 없으면 `pillars.ts` import 가 예외를 던진다. 설정 파일이 `.mts` 인 이유는 `.ts` 로
두면 Vite 가 CJS 로 읽어 경고가 나기 때문이다.

## 아키텍처

요청 흐름은 한 방향이다.

```
app/page.tsx                  유형 선택 (서버 컴포넌트 · 정적, 입력 없음)
  └─ app/reading/[type]/page.tsx   입력 + 결과 (세그먼트 = ReadingType id)
        │
components/SajuForm.tsx  (client · readingType 은 prop)
        │  ├─ components/BirthInputProvider.tsx  입력값 (app/layout.tsx 에 얹힘)
        │  ├─ lib/form/birth-input.ts   입력값 모양·요약 (클라이언트 안전)
        │  ├─ ResultView.tsx → ReadingSections.tsx  섹션별 렌더
        │  └─ lib/reading/sections.ts   섹션 계약 + 파서 (클라이언트 안전)
        │  POST /api/saju  →  NDJSON 스트림 응답
        ▼
app/api/saju/route.ts    (server, nodejs runtime)
        ├─ lib/rate-limit.ts   IP 기준 분당 제한
        ├─ lib/saju/schema.ts  zod 재검증 (클라이언트 검증은 신뢰하지 않음)
        ├─ lib/saju/pillars.ts 만세력 계산  ← 사실(fact)은 코드가 만든다
        │    ├─ time-correction.ts  진태양시·서머타임·표준자오선 보정
        │    ├─ ganji.ts            간지 테이블·십신·왕상휴수사·시주 규칙
        │    ├─ analysis.ts         오행 강약·신강신약·대운·세운
        │    ├─ constitution.ts     체질 경향 판정 규칙표
        │    ├─ yearly.ts           올해 세운 판정 규칙표
        │    └─ decade.ts           대운 10년 판정 규칙표
        ├─ lib/prompt.ts       프롬프트 조립
        └─ lib/gemini.ts       Gemini 호출  ← 해석(interpretation)만 LLM 이 한다
```

### 반드시 지킬 경계

1. **사주 계산과 판정을 LLM 에게 맡기지 않는다.** 간지·오행·십신은 `lib/saju/pillars.ts` 가, 체질은
   `constitution.ts` 가 정하고 LLM 은 서술만 한다. 프롬프트에도 `계산 완료 · 수정 금지` 로 명시돼 있다.
2. **`lib/` 의 서버 모듈은 맨 위에 `import "server-only"` 를 유지한다.** 클라이언트에서 실수로
   import 하면 빌드가 깨지도록 만든 안전장치다.
3. **API 키는 `lib/env.ts` 와 `lib/gemini.ts` 밖에서 읽지 않는다.**
4. **클라이언트에서 Gemini 를 직접 호출하지 않는다.** 항상 `/api/saju` 경유.

### 스트리밍 응답 규약

`/api/saju` 는 NDJSON 스트림을 돌려준다 (한 줄에 이벤트 하나, `SajuStreamEvent`).
순서는 `chart` → `delta*` → `done`.

**상태 코드를 쓸 수 있는 구간은 스트림을 열기 전까지다.**

| 시점 | 실패 | 응답 |
| --- | --- | --- |
| 스트림 전 | 레이트 리밋 / 입력 검증 / 사주 계산 / 서버 설정 | JSON + 429·400·500 |
| 스트림 후 | Gemini 호출 실패 (쿼터·안전정책 등) | 200 + `error` 이벤트 |

원국은 코드가 100ms 안에 계산하는데 Gemini 첫 조각은 1초쯤 뒤에 온다. **원국을 먼저 보내고 Gemini
실패는 이벤트로 알린다** — 원국은 LLM 실패와 무관하게 유효한 결과다.
그래서 **관측도 상태 코드만 세면 안 된다.**

### 두 단계 플로우 — 유형 먼저, 입력 나중

`/` 에서 유형을 고르고 `/reading/[type]` 에서 정보를 넣는다.
**`/` 에는 생년월일 입력이 없고 `/reading/*` 에는 유형 선택 컨트롤이 없다.**

- **단계는 클라이언트 상태가 아니라 라우트로 나눈다.** 세그먼트는 기존 `ReadingType` id 를 그대로
  쓴다 — 한글 슬러그를 만들면 API 계약과 URL 이 두 벌이 된다.
- `generateStaticParams` 로 `READING_TYPES` 를 프리렌더하고 그 밖의 값은 `notFound()`.
- **`/` 는 통째로 정적이다** (클라이언트 컴포넌트 없음 · ISR 5분). 이 성질을 깨는 변경을 하지 말 것 —
  깨려면 무엇을 얻는지 먼저 답해야 한다.

#### 입력값은 메모리에만 둔다

`BirthInputProvider` 가 `app/layout.tsx` 에 얹혀 있다. 레이아웃은 이동에서 언마운트되지 않으므로
`/` ↔ `/reading/*` 에서 값이 남는다. **`app/reading/layout.tsx` 로 내리면 안 된다** — `/` 를 거쳐 가면
값이 날아간다.

- **`sessionStorage`·`localStorage`·쿼리스트링을 쓰지 않는다.** 셋 다 생년월일이 프로세스 밖으로
  나간다(디스크 · 방문 기록 · Vercel 액세스 로그). 그 순간 `app/privacy/page.tsx` 의 "저장하지
  않습니다" 를 **같은 커밋에서 고쳐야 한다.**
- 지금 상태는 새로고침·새 탭·탭 닫기로 사라진다. **이 조건을 벗어나면 처리방침을 고쳐야 한다.**
- `lib/form/birth-input.test.ts` 가 소스에서 저장소·URL 사용을 막는다 (주석을 걷어낸 코드를 본다).
- **입력값과 함께 "완료된 풀이" 도 들고 다닌다** (`lib/form/reading-cache.ts`). 아까운 것은 토큰이
  아니라 **요청 수**다(무료 등급의 병목은 500 RPD). 규칙 넷:
  - **키는 입력 스냅샷 전체 + 유형이다.** `Object.keys` 로 훑으므로 필드가 늘면 자동으로 들어간다 —
    **손으로 나열하지 말 것.**
  - **입력이 바뀌면 통째로 비운다** (그래서 상한·LRU 가 필요 없다).
  - **완료된 것만 담는다** — `done` 까지 왔고 `error` 이벤트가 없었고 중단되지 않은 경우.
    중간까지 받은 글을 담으면 다음에 완결된 풀이인 척 나온다. 테스트가 소스에서 확인한다.
  - **캐시 적중에는 자동 스크롤을 걸지 않는다.**
- **캐시도 메모리에만 있다.** **서버 캐시는 다른 이야기다** — 담기는 것이 생년월일 파생 키 + 풀이
  전문이라 처리방침 3항을 같은 커밋에서 고쳐야 한다.
- **조회수는 캐시 적중이어도 센다** (그 유형 페이지를 본 것은 맞다).

## 관측 (`lib/observability.ts`)

요청마다 구조화된 JSON 한 줄을 stdout 에 남긴다 (Vercel Runtime Logs).

- **상태 코드로 집계하지 말 것.** `SajuOutcome` 으로 센다 — `ok` / `rate_limited` / `invalid_input` /
  `chart_failed` / `config_error` / **`reading_failed`**(200 속 실패).
- **개인정보를 싣지 않는다.** `METRIC_FIELDS` 허용 목록에 있는 키만 직렬화된다. 성별은 값이 아니라
  **지정 여부**만 남긴다. 필드를 늘릴 때는 `app/privacy/page.tsx` **6항의 목록도 같은 커밋에서 고친다.**
- 계측이 요청을 깨뜨리지 않는다 — `emit` 은 어떤 예외도 밖으로 내보내지 않는다.
- **싱크는 stdout 하나뿐이다.** 외부 수집기도 방문자 분석 도구도 붙이지 않는다. 나중에 붙이면
  제3자가 늘어나므로 처리방침 4항도 함께 고칠 것.
- 참고로 **Vercel Web Analytics 의 custom events 는 Hobby 플랜에서 쓸 수 없다.**

## 조회수와 좋아요 (`lib/counters.ts`)

유형마다 열람 수와 좋아요 수를 센다. 로그인은 없다. 두 라우트의 앞단은 `lib/counter-request.ts`.

- **저장소는 Upstash Redis 다.** 인메모리 `Map` 이면 인스턴스마다 다른 수가 찍힌다.
  **레이트 리밋을 영속화할 일이 생기면 새로 붙이지 말고 이걸 쓴다.**
- **환경변수 이름이 두 벌이다** (`KV_REST_API_*` · `UPSTASH_REDIS_REST_*`). `lib/env.ts` 가 둘 다 받는다 —
  한쪽만 읽으면 대시보드에서는 연결됐는데 앱은 "설정 없음" 이 된다.
- **키는 `views:<type>` · `likes:<type>` 뿐이다.** 결과별로 세지 않는다 (결과에 id 가 필요해지고 그건
  결과를 서버에 두는 것이다). **생년월일에서 파생된 값은 해시도 키에 넣지 않는다.**
- **누가 눌렀는지 서버에 남기지 않는다.** 중복은 브라우저 `localStorage` 플래그로만 막는다.
  **취소를 허용하므로 서버가 하한 0 을 강제한다.**
- **조회수는 브라우저가 센다** (`ViewBeacon` · `useRef` 로 StrictMode 이중 실행 방지).
  렌더 경로에서 저장소를 건드리면 모든 방문이 함수 실행이 된다. 덤으로 크롤러는 세어지지 않는다.
- **`readCounters(초)` 는 GET 경로 형태를 쓴다** — Next 의 Data Cache 는 GET 만 캐시하므로 POST 로
  보내면 `/` 가 동적이 된다. `/admin` 은 반대로 `no-store` 다.
- **비콘에 풀이 생성용 레이트 리밋을 걸지 않는다** (버킷 `counter:` · 분당 60).
- **저장소가 죽어도 화면은 산다.** 숫자를 못 읽으면 그 자리를 숨긴다 — **0 을 대신 보여주지 않는다.**
- 처리방침 4·5·6항이 이 절을 서술한다. **세는 항목을 늘리면 세 곳과 시행일을 같은 커밋에서 고친다.**

### 결과 영구 링크(`/r/[id]`)는 도입하지 않았다

결과별 OG 카드를 만들려면 결과를 서버에 두어야 하고, 그건 "저장하지 않습니다" 를 무효화한다.
다시 볼 시점은 **결과를 서버에 캐싱할 때**다 (그때 저장 계층과 보관 기간 문구를 함께 손댄다).

## 보안 규칙 (env / 키)

- `NEXT_PUBLIC_` 접두사는 **브라우저로 노출된다.** 키·시크릿에 절대 붙이지 않는다.
- 커밋 대상은 `.env.example` **하나뿐**이다.
- 에러 응답·로그에 키 원문을 넣지 않는다. 클라이언트로 내보내는 에러는 일반화된 문장만 쓴다 —
  스택·원문 메시지·설정 상세는 서버 로그에만.
- **사용자 생년월일은 개인정보다. 저장하지 않고 로그에도 남기지 않는다.**
  `/api/*` 는 `Cache-Control: no-store` (`next.config.ts`).
- 키가 노출되면: Google AI Studio 에서 폐기 → 재발급 → Vercel 환경변수 갱신 → 재배포.

**env 계층이 둘인 이유**: `getSecrets()`(키, 없으면 `EnvError`)와 `getRuntimeConfig()`(모델명·레이트
리밋, 기본값 있음)를 분리한다. 앞단 로직이 "키 없음" 때문에 먼저 터지면 입력 검증 결과가 가려진다.
검증은 import 시점이 아니라 **최초 호출 시점(lazy)** 이다 — 키 없이도 `next build` 는 성공해야 한다.

## 법적 고지 (코드와 반드시 일치)

`app/privacy/page.tsx` 와 `app/disclaimer/page.tsx` 는 **실제 코드 동작을 서술한 문서**다.
데이터 흐름이나 표현 범위를 바꾸면 **같은 커밋에서** 문서도 고친다. 지금 약속하는 내용:

- 생년월일·시각·성별·이름을 **저장하지 않는다** (DB 없음, 로그에도 없음)
- IP 는 레이트 리밋 목적으로 **메모리에 최대 1분**만 둔다
- 입력이 **Google 로 전송**되며 무료 등급이라 제품 개선에 쓰일 수 있다
- 추적 쿠키를 쓰지 않고 **외부 방문자 분석 도구를 전혀 쓰지 않는다**
- 6항에 방문 통계와 운영 지표의 수집 항목이 전부 나열돼 있다
- **체질 판정이 코드로 결정된다**는 것과 **한의학 사상체질·건강검진과 무관**하다는 것
- 만세력 학파 차이(경도 보정·자시 기준)로 다른 서비스와 결과가 다를 수 있다는 것
- 유형별 약속 — 원인을 진단하지 않는다(`gain-cause`) · 식이 처방이 아니다(`diet-food`) ·
  지병·부상·임신 여부를 모른다(`exercise`) · 시기를 정해 드리는 것이 아니다

## 작업 방식

할 일은 [docs/TASK.md](docs/TASK.md) 에서 `[TASK-NN]` 단위로 관리한다. 착수 전 해당 태스크를 읽고,
끝나면 체크박스와 상태를 갱신한다. **TASK.md 에는 태스크만 둔다.**

보드는 **위에서 아래로 우선순위 순**이다 (번호 순서와 우선순위는 다르다). 사용자 확인 없이 순서를
건너뛰지 않는다.

### 문서에 무엇을 쓰고 무엇을 쓰지 않나

**규칙만 적는다.** 실측 수치·버린 대안·되돌린 이유·과거 태스크 서술을 문서에 남기지 않는다 —
그것 때문에 이 파일이 아홉 날에 23배로 불었고 사소한 태스크마다 읽히는 고정 비용이 됐다.
히스토리가 필요하면 `git log` 와 `git show <커밋>:CLAUDE.md` 를 본다.

- **문서 갱신은 태스크 크기에 비례한다.** 코드 변경이 작고 규칙이 안 바뀌었으면 **문서를 고치지 않는다.**
- 문서를 고치는 경우는 셋뿐이다 — **규칙이 새로 생겼거나 · 규칙이 바뀌었거나 · 파일 위치가 바뀌었을 때.**
- 한 규칙은 한 줄로 적는다. 왜 그런지는 필요한 만큼만, 근거표는 `docs/` 로 보낸다.
- **법적 고지·처리방침과 어긋나는 변경은 예외다** — 그때는 반드시 같은 커밋에서 문서를 고친다.

### 실측이 필요한 작업과 아닌 작업

**실측은 아래를 건드릴 때만 한다.** 문안 한 줄 교체나 색 토큰 교체에 폭 3종 스크린샷을 요구하지 않는다.

| 실측 | 무엇으로 |
| --- | --- |
| 레이아웃 폭·슬롯 크기·겹침 | `node scripts/screenshot.mjs` (390px 기본) |
| 첫 화면 속도 (`priority`·자산 크기) | `node scripts/measure-load.mjs` (**프로덕션 빌드로**) |
| 프롬프트·섹션 계약·모델 설정 | `node scripts/sample-readings.mjs` (CI 에 넣지 않는다) |
| 사주 계산 규칙 | `npm run validate:saju` |

**수치를 적을 때는 모델명·측정일·조건을 함께 적는다.** 그 셋이 없는 수치는 다음 사람이 믿을 수 없다.

### 검증 게이트

- 작업 중에는 **`npm run lint` + `npm test`** (합쳐 20초 남짓).
- **`npm run build` 와 `npm run typecheck` 는 커밋 직전 한 번.** 중간마다 돌리지 않는다.
- 같은 넷을 `.github/workflows/ci.yml` 이 푸시·PR 마다 돌린다.

### 소스를 읽는 테스트가 여덟 개 있다

`birth-input.test.ts` · `prompt.test.ts` · `tokens.test.ts` · `motion.test.ts` · `sections.test.ts` ·
`line-breaks.test.ts` · `verdict-photo.test.ts` · `counters.test.ts` 는 **소스 파일을 읽어 문자열과
구조를 검사한다.** 그래서 문안·클래스 수정이 무관해 보이는 테스트를 깨울 수 있다 — 놀라지 말고
그 테스트가 지키려는 규칙을 확인한다. **검사를 완화하는 쪽으로 풀지 말 것.**

### 모델 배정 범례

| 표기 | 모델 | 쓰는 경우 |
| --- | --- | --- |
| **(O)** | Opus 5 | 도메인 정확도, 아키텍처, 애매한 리팩터, 프롬프트 설계 |
| **(S)** | Sonnet 5 | 스펙이 정해진 기능 구현, API 연동, 테스트 작성 — 대부분의 작업 |
| **(H)** | Haiku 4.5 | 문안 수정, 스타일 정리, 반복 치환, 문서 갱신 |

### 상태 표기

`⬜ 대기` · `🟡 진행중` · `✅ 완료` · `⏸ 보류` · `❌ 취소`

계정·플랜이 있어야 착수할 수 있는 태스크는 제목 끝에 `🔒 <무엇이> 필요` 를 붙인다.

### 새 태스크 형식

```markdown
### [TASK-NN] 제목 · **(O|S|H)** · ⬜ 대기

- [ ] 체크 가능한 작업 단위

→ 완료 기준: 무엇이 되면 끝인가 (검증 가능한 문장).
```

### 그 밖의 규칙

- **완료·보류된 태스크는 TASK.md 에서 지운다.** 번호는 재사용하지 않고 **다음 번호는 TASK.md
  맨 아래에만** 적는다.
- 새 UI 문자열은 한국어로 쓴다.
- 커밋 메시지는 한국어 요약 + 태스크 번호. 예: `결과 공유 OG 이미지 추가 (TASK-10)`

## 확정된 결정사항

- **유형은 일곱이고 그중 다섯이 공개다.**
  - 공개: `종합 체질 풀이`(`diet`) · `내가 살이 찌는 이유`(`gain-cause`) ·
    `나에게 맞는 다이어트 방법`(`diet-method`) · `나에게 맞는 다이어트 식단`(`diet-food`) ·
    `나에게 맞는 운동`(`exercise`)
  - 내부: `종합 사주 풀이`(`general`) · `몸이 바뀌는 10년`(`decade`)
- **다이어트 계열은 묻는 것이 서로 다르다.** `diet` 몸이 어떤 쪽인가 · `gain-cause` 왜 붙는가 ·
  `diet-method` 무엇을 어떻게 하는가 · `diet-food` 무엇을 먹는가 · `exercise` 어떤 운동을 하는가.
  `READING_TYPES` 순서가 UI 순서다.
- **id 를 바꾸지 않는다** — 세그먼트가 곧 URL 이라 이미 공유된 링크와 공유 카드가 죽는다.
- **`yearly` 유형은 없앴고 `lib/saju/yearly.ts` 는 남긴다.** `/reading/yearly` 는 404 가 아니라
  `/` 로 리다이렉트한다 (`next.config.ts`) — 목적지가 `/reading/diet` 가 아닌 이유는 운세를 보러 온
  사람에게 체질 풀이를 들이미는 것은 다른 것을 준 것이기 때문이다.
- **내부로 내린 유형을 `READING_TYPES` 에서 빼지 않는다** — 빼면 `Record` 가 몫을 전부 지우라고 하고
  되살릴 때 섹션 계약과 프롬프트를 다시 써야 한다. 필요한 것은 노출 제어뿐이다.
- **시간축 유형(`decade`)을 다시 공개로 올리지 않는다** — 나머지 넷은 몸을 다이어트 축으로 묻는데
  그것만 시간축을 묻는다.
- **브랜드 컬러는 그린이고 유형별로 테마를 갈지 않는다.**
- **신살(神殺)과 합·충은 계산하지 않는다** (근거는 `lib/CLAUDE.md`).
- **궁합·직업 적성은 도입하지 않는다** — 궁합은 입력이 2인분이 되어 처리방침 1항까지 손대야 하고,
  직업 적성은 십신→직업 매핑표를 새로 만들어야 한다.
- **관측은 서버 로그까지만 한다.** 제3자 분석 스크립트를 넣지 않는다 (유형별 누계는 우리가 직접 센다).
- 새 기능은 **일곱 유형 모두**에 적용되는지 확인한다 (`Record<ReadingType, …>` 가 잡아 준다).

## 미확정 결정사항

- **수익화 여부**: 광고나 유료 결제가 들어가면 레이트 리밋 영속화 우선순위가 올라가고 법적 고지에
  결제·광고 내용을 추가해야 한다. Gemini 유료 등급으로 올리면 "무료 등급이라 학습에 쓰일 수 있다"
  고지도 걷어낸다.
  → **결제는 키가 아니라 Google 프로젝트 단위로 붙는다.** 곁가지(이미지 생성 등)에 결제를 붙일 때
  **서비스 키와 같은 프로젝트에서 키를 발급하면 안 된다** — 풀이 생성 키까지 유료가 되어 위 고지가
  사실과 어긋난다. 곁가지는 별도 프로젝트의 별도 키로 하고 Vercel 환경변수로 올리지 않는다.

## 배포 (Vercel)

- 프로덕션: https://diet-saju.vercel.app · `main` 푸시 → 자동 배포 · 프리뷰는 PR 단위.
- 함수 리전은 `icn1`(서울). `X-Vercel-Id` 에서 **확인할 것은 두 번째 구간**이다 (앞은 요청을 받은
  엣지 지점이라 다를 수 있다).
- `/api/saju` 는 **Node 런타임 고정**이다 (`lunar-javascript` 가 CommonJS). **Edge 로 바꾸지 말 것.**
- 함수 타임아웃은 `maxDuration = 30` (Hobby 상한). 관측 최대치는 10초 안쪽이라 여유가 있다.
- 프로덕션 모델이 코드 기본값과 다를 수 있다. 확인은 응답의 `model` 필드로 한다.
- 환경변수는 Vercel Dashboard > Settings > Environment Variables (`GEMINI_API_KEY` 는 세 환경 모두).
- **Vercel CLI 는 배포 계정과 다른 계정으로 로그인되어 있다.** 환경변수는 대시보드에서 처리한다.
  **`vercel link --yes` 를 쓰지 말 것** — 기존 프로젝트를 찾는 게 아니라 같은 이름의 빈 프로젝트를
  새로 만든다. `vercel link`/`vercel env pull` 은 `.env.local` 을 덮어쓰고 `.gitignore` 에 `.env*` 를
  덧붙여 `!.env.example` 예외를 무력화한다.
- `public/` 자산은 `Cache-Control: public, max-age=0, must-revalidate` 로 나간다 (파일 이름에 해시가
  없어서 길게 캐시하면 갱신이 안 먹는다). **바꾸려면 파일명에 해시를 넣는 것이 먼저다.**
- 속도를 다시 잴 때는 **CSS 청크가 200 인지 먼저 확인한다** — 다른 세션이 `npm run build` 를 돌리면
  실행 중인 `next start` 가 사라진 청크를 가리켜 스타일 없는 화면이 되고 숫자가 무의미해진다.
  서버를 다시 띄울 때는 `netstat -ano | grep :3100` 으로 PID 를 찾아 죽인다.
