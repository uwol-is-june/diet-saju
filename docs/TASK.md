# diet-saju 태스크 보드

> 범례·상태 표기·작성 형식은 `CLAUDE.md` 의 "작업 방식" 참고.

---

## Phase 0 — 기반

### [TASK-02] 키 발급 + Vercel 배포 연결 · **(H)** · 🟡 진행중

- [ ] Google AI Studio 에서 `GEMINI_API_KEY` 발급
- [ ] `.env.local` 에 키 입력 후 로컬에서 `/api/saju` 200 응답 확인
- [ ] GitHub 저장소 → Vercel 프로젝트 임포트 (Framework Preset: **Next.js**)
- [ ] 환경변수 등록: `GEMINI_API_KEY`, `GEMINI_MODEL`, `RATE_LIMIT_PER_MINUTE`
      (Production / Preview / Development 모두)
- [ ] 배포 후 `/api/saju` 200 응답 및 `Cache-Control: no-store` 헤더 확인
- [ ] 리전을 `icn1`(서울)로 지정해 지연 확인

→ 완료 기준: 프로덕션 URL 에서 풀이가 끝까지 나오고, 브라우저 번들·네트워크 탭에 키가 없다.

---

## Phase 1 — 사주 계산 정확도

### [TASK-03] 만세력 계산 정확도 개선 · **(O)** · ⬜ 대기

- [ ] 진태양시 보정 (동경 135° ↔ 서울 127°, 약 −32분) 적용 여부 옵션화
- [ ] 야자시/조자시 처리 규칙 결정 및 구현 (23:00~00:59 일주 경계)
- [ ] 서머타임 구간 보정 (1948~1951, 1955~1960, 1987~1988)
- [ ] 윤달 입력 처리 및 `lunarDate` 윤달 표기 검증
- [ ] 검증 케이스 20건 이상 공개 만세력과 교차 대조 → `docs/saju-validation.md`

→ 완료 기준: 검증 케이스 전건 일치. 불일치는 사유가 문서에 기록되어 있다.

### [TASK-04] 해석 근거 데이터 확장 · **(O)** · ⬜ 대기

- [ ] 지지 십신 전체(`getXxxShiShenZhi`) 포함
- [ ] 오행 강약 가중치 (월지 계절 반영, 단순 개수 → 가중 점수)
- [ ] 신강/신약 판정
- [ ] 대운(大運) / 세운(歲運) 산출
- [ ] 확장 필드를 `SajuChart` 타입과 프롬프트에 반영

→ 완료 기준: 근거 없는 서술이 줄었음을 샘플 5건으로 확인.

### [TASK-05] 사주 계산 테스트 · **(S)** · ⬜ 대기

- [ ] Vitest 도입 (`npm run test`)
- [ ] `calculateSajuChart` 단위 테스트: 양력/음력, 윤달, 시각 미상, 입춘 전후, 1900/2100 경계
- [ ] `lib/saju/hanja.ts` 변환 전수 테스트 (간체/정체 모두)

→ 완료 기준: TASK-03 검증 케이스가 테스트로 고정되어 있다.

---

## Phase 2 — LLM 품질과 비용

### [TASK-06] 프롬프트 고도화 + 구조화 출력 · **(O)** · ⬜ 대기

- [ ] 마크다운 자유 서술 → `responseMimeType: "application/json"` + `responseSchema`
- [ ] 섹션별 렌더 컴포넌트 (요약 카드 / 상세 아코디언)
- [ ] 유형별(`general` / `diet`) 프롬프트 분리 정리
- [ ] 프롬프트 인젝션 테스트: 이름 칸에 지시문을 넣어도 무시되는지 확인
- [ ] 동일 입력 3회 실행 일관성 확인, `temperature` 조정

→ 완료 기준: 응답이 항상 같은 스키마로 오고, 파싱 실패 시 폴백이 동작한다.

### [TASK-07] 스트리밍 응답 · **(S)** · ⬜ 대기

- [ ] `generateContentStream` 전환, route 에서 `ReadableStream` 반환
- [ ] 클라이언트 점진적 렌더 + 중단(abort) 처리
- [ ] 스트리밍 중 에러 시 부분 결과 처리 방침 결정

→ 완료 기준: 첫 글자가 3초 내에 보인다.

### [TASK-08] 결과 캐싱 · **(S)** · ⬜ 대기

- [ ] 캐시 키 = 해시(생년월일시 + 성별 + 양음력 + readingType + 모델 + 프롬프트 버전)
- [ ] 저장소 선정 (Vercel KV / Upstash Redis)
- [ ] 생년월일 원문을 키에 쓰지 않는다 (해시만 저장)
- [ ] TTL 및 프롬프트 버전 변경 시 무효화 전략
- [ ] TASK-12 개인정보 처리방침 문구 갱신

→ 완료 기준: 같은 입력 재요청이 LLM 을 호출하지 않는다.

---

## Phase 3 — 서비스화

### [TASK-09] 레이트 리밋 영속화 · **(S)** · ⬜ 대기

- [ ] Upstash Redis 기반 슬라이딩 윈도우로 교체 (현재 인메모리 = 인스턴스별 분리)
- [ ] IP + (선택) 세션 기준 이중 한도
- [ ] 429 응답에 `Retry-After` 유지

→ 완료 기준: 인스턴스가 여러 개여도 분당 한도가 전역으로 지켜진다.

### [TASK-10] 결과 공유 · **(S)** · ⬜ 대기

- [ ] 결과 영구 링크 (`/r/[id]`) — 생년월일 노출 금지 전제
- [ ] 동적 OG 이미지 (`next/og`) — 사주팔자 4기둥 + 오행 배지
- [ ] 이미지로 저장 / 링크 복사 버튼

→ 완료 기준: 카카오톡·인스타에 붙였을 때 카드가 제대로 뜬다.

### [TASK-11] UI/UX 다듬기 · **(H)** · ⬜ 대기

- [ ] 로딩 상태를 스켈레톤/진행 문구로 개선
- [ ] 모바일 폼 레이아웃 점검 (생년월일 입력, iOS date picker)
- [ ] 접근성: 라벨-입력 연결, 포커스 링, 대비비, `aria-live` 결과 알림
- [ ] 다크 모드 대응 여부 결정

### [TASK-12] 법적 고지 · **(H)** · ⬜ 대기

- [ ] 면책 문구 정식화: "오락·참고 목적, 의학적/법률적 조언 아님"
- [ ] 개인정보 처리방침: 입력값 미저장 원칙 명문화
- [ ] 푸터 링크 + 첫 방문 안내

### [TASK-13] 관측과 에러 추적 · **(S)** · ⬜ 대기

- [ ] Vercel Analytics 연결
- [ ] 서버 에러 집계(Sentry 등) — 생년월일·API 키 스크러빙 설정 필수
- [ ] Gemini 실패율 / 429 발생률 / 평균 응답 시간 지표화

---

## Phase 4 — 확장

### [TASK-14] 체질·다이어트 리딩 심화 · **(O)** · ⬜ 대기

TASK-03 / TASK-04 완료 후 착수.

- [ ] 오행 균형 → 체질 경향 매핑 규칙표를 코드로 작성 (LLM 즉흥 판단 금지)
- [ ] 식습관/운동 제안을 규칙표에서 뽑아 프롬프트에 근거로 제공
- [ ] 의학적 주장으로 읽히지 않는 표현 가이드 확정 (TASK-12 문구와 정합)

→ 완료 기준: 같은 사주에 항상 같은 체질 판정이 나온다 (판정은 결정론적, 서술만 LLM).

### [TASK-15] 리딩 타입 추가 · **(S)** · ⬜ 대기

- [ ] 올해 운세 / 궁합 / 직업 적성 중 우선순위 결정
- [ ] `readingType` 확장이 프롬프트·UI·캐시 키에 일관 반영되는지 확인
