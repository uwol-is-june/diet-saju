# diet-saju 태스크 보드

> 범례·상태 표기·작성 형식은 `CLAUDE.md` 의 "작업 방식" 참고.

---

## Phase 2 — LLM 품질과 비용

### [TASK-06] 프롬프트 고도화 + 구조화 출력 · **(O)** · ⬜ 대기

**주의**: 스트리밍(TASK-07)과 충돌한다. JSON 구조화 출력은 완성 전까지 파싱이 불가능해
점진 렌더가 깨진다. 섹션 단위로 나눠 보내는 프로토콜을 먼저 설계할지, 구조화를 포기할지
결정해야 한다.

- [ ] 스트리밍 유지 여부 결정 (섹션별 스트리밍 vs 완성 후 일괄)
- [ ] 마크다운 자유 서술 → `responseMimeType: "application/json"` + `responseSchema`
- [ ] 섹션별 렌더 컴포넌트 (요약 카드 / 상세 아코디언)
- [ ] 유형별(`general` / `diet`) 프롬프트 분리 정리
- [ ] 프롬프트 인젝션 테스트: 이름 칸에 지시문을 넣어도 무시되는지 확인
- [ ] 동일 입력 3회 실행 일관성 확인, `temperature` 조정

→ 완료 기준: 응답이 항상 같은 스키마로 오고, 파싱 실패 시 폴백이 동작한다.

### [TASK-17] 무료 등급 쿼터 대응 · **(S)** · 🟡 진행중

일일 한도는 `gemini-3.5-flash-lite` 로 옮겨 500 RPD 를 확보했다 (기존 20 RPD). 남은 것은
소진에 대비한 운영 장치다. 한도표와 근거는 CLAUDE.md "무료 등급 일일 한도" 참고.

- [x] 후보 모델 실제 한도 확인 — Lite 계열만 500 RPD, 나머지 Flash 는 20 RPD
- [x] 모델 교체 — 기본값을 `gemini-3.5-flash-lite` 로 (품질 3건 대조 후 결정)
- [x] 일일 소진 문구 분리 — `quotaId` 의 `PerDay` 로 판별해 "내일 다시" 안내
- [ ] 폴백 체인: 주 모델 429(PerDay) → 다른 모델로 자동 재시도 (쿼터가 모델별로 분리되므로
      일일 용량이 합산된다)
- [ ] 서버에서 일일 사용량 카운트 → 소진 임박 시 조기 안내
- [ ] `RATE_LIMIT_PER_MINUTE` 재검토 (현재 5, 모델 RPM 은 15)

→ 완료 기준: 하루 몇 건까지 감당되는지 수치로 알고, 소진 시 문구가 정확하다.

### [TASK-08] 결과 캐싱 · **(S)** · ⬜ 대기

일일 쿼터(TASK-17)를 아끼는 가장 값싼 수단이다.

- [ ] 캐시 키 = 해시(생년월일시 + 성별 + 양음력 + readingType + 모델 + 프롬프트 버전 + 세운 기준연도)
- [ ] 저장소 선정 (Vercel KV / Upstash Redis)
- [ ] 생년월일 원문을 키에 쓰지 않는다 (해시만 저장)
- [ ] TTL 및 프롬프트 버전 변경 시 무효화 전략
- [ ] TASK-12 개인정보 처리방침 문구 갱신

→ 완료 기준: 같은 입력 재요청이 LLM 을 호출하지 않는다.

---

## Phase 3 — 서비스화

### [TASK-09] 레이트 리밋 영속화 · **(S)** · ⬜ 대기

- [ ] Upstash Redis 기반 슬라이딩 윈도우로 교체 (현재 인메모리 = 인스턴스별 분리)
- [ ] IP + (선택) 세션 기준 이중 한도
- [ ] 429 응답에 `Retry-After` 유지

→ 완료 기준: 인스턴스가 여러 개여도 분당 한도가 전역으로 지켜진다.

### [TASK-10] 결과 공유 · **(S)** · ⬜ 대기

- [ ] 결과 영구 링크 (`/r/[id]`) — 생년월일 노출 금지 전제
- [ ] 동적 OG 이미지 (`next/og`) — 사주팔자 4기둥 + 오행 배지
- [ ] 이미지로 저장 / 링크 복사 버튼

→ 완료 기준: 카카오톡·인스타에 붙였을 때 카드가 제대로 뜬다.

### [TASK-16] 컬러 시스템 구축 · **(S)** · ⬜ 대기

추출 팔레트는 [design-tokens.md](design-tokens.md) 참고. 착수 전 CLAUDE.md
"미확정 결정사항"의 서비스 정체성을 먼저 확정한다 (그린 = 다이어트 쪽으로 톤 이동).

- [ ] 시맨틱 토큰 층 설계: `--color-brand` / `-hover` / `-subtle`, `surface`, `text-*`, `border`, `danger`, `warning`, `link`
- [ ] 원시 팔레트(green500 등)를 시맨틱 토큰에 매핑, 컴포넌트는 시맨틱 토큰만 참조
- [ ] `app/globals.css` `@theme` 교체 (현재 보라 `#7c3aed` 계열 → 신규 토큰)
- [ ] 기존 하드코딩 색상 전수 치환 (`components/`, `app/`)
- [ ] 대비비 검증: `green500 #50D88F` 위 흰 텍스트는 AA 미달 → 버튼 배경/라벨 조합 확정
- [ ] 오행(목화토금수) 배지 색을 팔레트 안에서 정의 — TASK-10 OG 이미지와 공유
- [ ] 다크 모드 토큰 정의 여부 결정 (TASK-11 항목과 중복 → 여기서 결론 내고 TASK-11 에서 삭제)

→ 완료 기준: 컴포넌트에 raw hex 가 없고, 팔레트 변경이 `@theme` 한 곳 수정으로 끝난다.

### [TASK-11] UI/UX 다듬기 · **(H)** · ⬜ 대기

- [ ] 로딩 상태를 스켈레톤/진행 문구로 개선
- [ ] 모바일 폼 레이아웃 점검 (생년월일 입력, iOS date picker)
- [ ] 접근성: 라벨-입력 연결, 포커스 링, 대비비, `aria-live` 결과 알림
- [ ] 다크 모드 대응 여부 결정

### [TASK-12] 법적 고지 · **(H)** · ⬜ 대기

- [ ] 면책 문구 정식화: "오락·참고 목적, 의학적/법률적 조언 아님"
- [ ] 개인정보 처리방침: 입력값 미저장 원칙 명문화
- [ ] **Gemini 무료 등급은 입력 내용이 Google 제품 개선에 사용될 수 있다.**
      생년월일이 Google 로 전송되는 사실을 고지할지, 유료 등급으로 전환할지 결정
- [ ] 푸터 링크 + 첫 방문 안내

### [TASK-13] 관측과 에러 추적 · **(S)** · ⬜ 대기

- [ ] Vercel Analytics 연결
- [ ] 서버 에러 집계(Sentry 등) — 생년월일·API 키 스크러빙 설정 필수
- [ ] Gemini 실패율 / 429 발생률 / 평균 응답 시간 지표화

---

## Phase 4 — 확장

### [TASK-14] 체질·다이어트 리딩 심화 · **(O)** · ⬜ 대기

TASK-03 / TASK-04 완료 후 착수.

- [ ] 오행 균형 → 체질 경향 매핑 규칙표를 코드로 작성 (LLM 즉흥 판단 금지)
- [ ] 식습관/운동 제안을 규칙표에서 뽑아 프롬프트에 근거로 제공
- [ ] 의학적 주장으로 읽히지 않는 표현 가이드 확정 (TASK-12 문구와 정합)

→ 완료 기준: 같은 사주에 항상 같은 체질 판정이 나온다 (판정은 결정론적, 서술만 LLM).

### [TASK-15] 리딩 타입 추가 · **(S)** · ⬜ 대기

- [ ] 올해 운세 / 궁합 / 직업 적성 중 우선순위 결정
- [ ] `readingType` 확장이 프롬프트·UI·캐시 키에 일관 반영되는지 확인

### [TASK-18] 프로덕션 모델 교체 · **(H)** · ⬜ 대기

코드 기본값은 이미 `gemini-3.5-flash-lite` 다. 프로덕션은 Vercel 환경변수 `GEMINI_MODEL` 이
`gemini-3.5-flash` 로 덮어쓰고 있어 **하루 20건 · 응답 24초**로 돌고 있다.
링크를 남에게 공유하기 전에는 반드시 처리한다 (타임아웃 30초까지 여유가 5.6초뿐이다).

CLI 계정(`seojunincar-8503`)과 실제 배포 프로젝트의 계정이 달라 코드에서 처리할 수 없다.
대시보드 작업이다.

- [ ] Vercel > diet-saju > Settings > Environment Variables 에서
      `GEMINI_MODEL` 을 `gemini-3.5-flash-lite` 로 수정 (Production/Preview/Development)
- [ ] Redeploy (환경변수는 재배포해야 반영된다)
- [ ] `POST /api/saju` 응답의 `model` 필드가 `gemini-3.5-flash-lite` 인지 확인
- [ ] 응답 시간이 10초 이내로 떨어졌는지 확인
- [ ] 잘못 생성된 빈 Vercel 프로젝트 삭제 — `seojunincar-8503s-projects` 스코프의
      `diet-saju`(Production URL 이 `--`, 배포 이력 0건). 실제 서비스 프로젝트와 다른 계정이다

→ 완료 기준: 프로덕션이 lite 로 돌고 응답이 10초 이내다. 일일 한도 500건.
