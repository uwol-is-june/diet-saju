# diet-saju 태스크 보드

> 이 파일에는 **태스크만 둔다.** 범례·상태 표기·작성 형식·규칙은 `CLAUDE.md` 의 "작업 방식",
> 보류한 일과 그 근거는 `CLAUDE.md` 의 "보류한 작업", 끝낸 일의 결론이 어디 있는지는
> `CLAUDE.md` 의 "끝낸 태스크의 결론은 어디에 있나" 에 있다.
>
> **위에서 아래로 우선순위 순이다.** 순서를 바꿀 때는 이유를 태스크 본문에 적는다.
> 번호 순서와 우선순위는 다르다 — 지금 틀린 값이 나가고 있는 문제는 번호가 뒤여도 위로 올린다.

## 지금 남은 태스크

### [TASK-78] 배포 뒤 프로덕션 실측을 다시 잰다 · **(S)** · ⬜ 대기

`CLAUDE.md` 배포 절의 수치는 **2026-08-13 값이고 낡았다.** 그 뒤로 분량이 두 배가 되고
(TASK-56) 유형이 다섯으로 늘고(TASK-45 · 48 · 63) 셸·웹폰트가 들어갔는데(TASK-74 · 75)
**그 뒤로 프로덕션에서 잰 적이 없다.** 지금 적혀 있는 값은 원국 985ms · 첫 글자 1.8초 ·
전체 5.8초다.

> **배포가 먼저다.** `task-72-77` 브랜치가 `main` 에 들어가야 잴 대상이 생긴다.
> 그 전에는 착수할 수 없다.

- [ ] `POST /api/saju` 로 유형 다섯을 각각 재고 **원국 · 첫 글자 · 전체** 세 구간을 적는다
- [ ] `X-Vercel-Id` 의 **두 번째 구간**이 `icn1` 인지 확인한다 (앞은 엣지 지점이라 다를 수 있다)
- [ ] 응답의 `model` 필드로 프로덕션 모델이 코드 기본값과 같은지 확인한다
- [ ] **웹폰트가 첫 화면을 얼마나 늦추는지 본다** (TASK-75 에서 새로 들어왔다).
      `head-800.woff2` 는 112KB · `font-display: swap` 이라 글이 먼저 떠야 한다 —
      실제로 그런지, 제목이 늦게 바뀌는 것이 눈에 거슬리는지 확인한다
- [ ] `maxDuration = 30` 에 걸리는 유형이 있는지 본다 (로컬은 유형별 6~10초)
- [ ] `CLAUDE.md` 배포 절의 수치와 **측정일**을 갱신한다.
      "위 수치는 낡았다 — 배포 뒤 한 번 다시 잴 것" 문단도 이번 측정으로 다시 쓴다

**주의**

- **로컬 값을 프로덕션 값으로 적지 말 것.** 콜드 스타트와 네트워크 왕복이 빠져 있다.
- **수치를 옮겨 적을 때는 모델명과 측정일을 함께 적는다** — 그것이 없어서 세 곳이
  어긋난 적이 있다 (`thinkingLevel` 사례).
- 표본 하나가 133초까지 간 적이 있는데 로컬 네트워크 지연이었다. **한 번 튄 값으로
  결론을 내지 말고** 몇 번 재서 분포를 본다.

→ 완료 기준: 공개 유형 다섯의 프로덕션 실측이 `CLAUDE.md` 배포 절에 **모델명·측정일과 함께**
적혀 있고, 함수 리전이 `icn1` 이며, `maxDuration` 에 걸리는 유형이 없다는 것이 확인됐다.

---

새 태스크는 `CLAUDE.md` 의 "새 태스크 추가 형식" 을 따라 우선순위 순으로 넣는다.
번호는 재사용하지 않는다 — **다음 신규는 TASK-79** 이다.
