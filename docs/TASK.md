# diet-saju 태스크 보드

> 범례·상태 표기·작성 형식은 `CLAUDE.md` 의 "작업 방식" 참고.
> **위에서 아래로 우선순위 순이다.** 순서를 바꿀 때는 이유를 태스크 본문에 적는다.

---

**진행 중인 태스크가 없다** (2026-08-13 기준).

보류한 작업과 그 이유는 `CLAUDE.md` 의 "보류한 작업" 절에 있다. 다시 꺼낼 때 조사를
되풀이하지 않도록 판단 근거를 함께 남겨 뒀으니 착수 전에 그쪽을 먼저 읽을 것.

새 태스크는 아래 형식으로 이 아래에 추가한다. **번호는 재사용하지 않는다 — 다음 신규는 TASK-21.**

```markdown
## 1. [TASK-NN] 제목 · **(O|S|H)** · ⬜ 대기

**이 순서인 이유**: 왜 지금인가 (선행 관계를 여기 적는다).

- [ ] 체크 가능한 작업 단위
- [ ] ...

→ 완료 기준: 무엇이 되면 끝인가 (검증 가능한 문장).
```
