# 만세력 계산 검증 (TASK-03 · TASK-04 · TASK-05 · TASK-14 · TASK-53 · TASK-54)

`npm run validate:saju` 로 재현할 수 있다 (`npm test` 에도 포함된다).
사주 계산 테스트는 개별 대조 11,000건 이상을 포함해 전건 통과한다.
(건수는 늘어나므로 여기 적지 않는다 — 실행해서 확인할 것)

테스트는 코드와 나란히 둔다.

| 파일 | 범위 |
| --- | --- |
| `lib/saju/ganji.test.ts` | 십신 10×10, 시주 오서둔 10×12, 왕상휴수사 60건, 지장간 전체·통근, 한자 파싱, 60갑자 |
| `lib/saju/time-correction.test.ts` | 자오선 전환 2회, 서머타임 12개 연도, 보정량, 균시차, 왕복 변환 |
| `lib/saju/analysis.test.ts` | 대운 방향·대운수·구간, 세운, 신강신약, 오행 점수 |
| `lib/saju/pillars.test.ts` | 일·년·월·시주 전수 대조, 절기 공표값, 자시 학파, 윤달, 경계, 결정론 |

**3-6 절만 성격이 다르다.** 그쪽은 계산이 아니라 **풀이가 읽히는가**를 재고, 테스트가
아니라 `scripts/sample-readings.mjs` 로 실제 API 를 두드려 잰다 (쿼터를 쓰므로 CI 에 넣지
않는다).

같은 라이브러리를 두 번 부르는 자기참조 검증은 의미가 없으므로, **명리학 규칙을 테스트 안에서
독립 구현해** 대조하고 절기는 천문 공표값과 비교했다. 새 규칙을 추가할 때도 같은 방식으로
문헌값을 테스트에 박아 둔다.

> TASK-05 전까지는 `scripts/validate-saju.ts` 일회성 스크립트였다. Vitest 로 이식하면서
> 삭제했다 — 검증 로직이 두 곳에 있으면 한쪽이 반드시 낡는다.

---

## 1. 라이브러리에서 발견한 문제 3가지

`lunar-javascript` 를 그대로 쓰면 한국 사주 관행과 어긋난다. 실측으로 확인한 내용이다.

### 1-1. 절기를 베이징 시간(UTC+8)으로 계산한다

```
2024 입춘 — 라이브러리: 2024-02-04 16:27
             공표(KST):  2024-02-04 17:27   ← 정확히 1시간 차이
```

한국 벽시계 시각을 그대로 넣으면 **월주(月柱) 경계가 1시간 어긋난다.** 절입 시각 전후 1시간
안에 태어난 사람의 월주가 틀리게 나온다.

→ **조치**: 년주·월주는 출생 시각을 **베이징 벽시계로 변환해서** 넣는다. 절기 비교가 같은
좌표계에서 이뤄지므로 경계 판정이 맞는다. (`lib/saju/pillars.ts` 의 `instant.beijingClock`)

### 1-2. `sect` 설정이 일주에만 적용되어 학파가 섞인다

`EightChar` 에는 야자시 학파를 정하는 `sect` 가 있고 기본값은 2(자정 기준)다.
그런데 시주 천간은 라이브러리 내부(`lunar.js` 의 `_computeTime`)에서 **항상 23시 기준 일간**으로
계산되며 `sect` 를 보지 않는다.

```
2024-01-01 23:30  일주=甲子(자정 기준)  시주=丙子(乙일 자시 = 23시 기준)
                  → 甲일인데 시주가 乙일 기준. 두 학파가 한 사주에 섞였다.
```

→ **조치**: 시주를 우리가 직접 계산한다. 오서둔(五鼠遁)으로 `시간 = f(일간, 시지)` 를 적용해
어느 학파를 골라도 일주와 시주가 항상 정합한다. (`lib/saju/ganji.ts` 의 `hourGanIndex`)

### 1-3. 진태양시·서머타임·표준자오선 변경 개념이 없다

→ **조치**: `lib/saju/time-correction.ts` 에서 보정한 지방시를 일주·시주 계산에 넣는다.

---

## 2. 시간 보정 설계

### 2-1. 서머타임과 표준자오선 이력은 tz 데이터베이스에서 가져온다

연도별 서머타임 날짜를 손으로 옮기는 대신 **Node 내장 IANA tz 데이터**(`Asia/Seoul`)를 쓴다.
실측으로 문헌과 전부 일치함을 확인했다.

| 시점 | tz 오프셋 | 문헌 |
| --- | --- | --- |
| 1905-06 | UTC+8:28 | 서울 지방 평균시 |
| 1910-06 | UTC+8:30 | 대한제국 동경 127.5° |
| 1930-06 | UTC+9:00 | 일제 동경 135° |
| 1948~51 여름 | UTC+10:00 | 서머타임 (135° + 1h) |
| 1954-03-15 → 04-15 | +9:00 → +8:30 | **1954-03-21 자오선 변경** |
| 1955~60 여름 | UTC+9:30 | 서머타임 (127.5° + 1h) |
| 1961-07 → 09 | +8:30 → +9:00 | **1961-08-10 자오선 복귀** |
| 1987~88 여름 | UTC+10:00 | 서머타임 부활 |
| 1989 이후 여름 | UTC+9:00 | 서머타임 폐지 |

전 구간(1900~2100, 402개 표본)에서 `실제 오프셋 − 표준자오선 오프셋` 이 **0분 또는 60분**
뿐임을 확인했다. 자오선 이력 표(`MERIDIAN_HISTORY`)와 tz 가 어긋나면 이 검사가 깨진다.

하드코딩 대신 tz 를 쓰는 이유: 정확하고, tzdata 갱신을 자동으로 따라가고, 검증 가능하다.

### 2-2. 보정 방식 3가지

| 모드 | 내용 | 서울 보정량(1990년 기준) |
| --- | --- | --- |
| `standard` | 보정 없음 | 0분 |
| `longitude` **(기본)** | 경도 보정만 (지방 평균태양시) | −32분 |
| `true` | 경도 + 균시차 | −32분 ± 최대 17분 |

기본값을 `longitude` 로 둔 이유: 한국 만세력 다수 관행이 경도 보정만 적용한다.
균시차까지 넣는 엄밀한 진태양시는 옵션으로 뒀다.

보정량은 시기에 따라 달라진다 — 검증기가 확인한 값:

| 조건 | 보정량 |
| --- | --- |
| 1990-05-17 (동경 135°) | −32분 |
| 1958-03-10 (동경 127.5°) | **−2분** ← 당시 표준시가 이미 한반도 중앙 기준 |
| 1988-07-15 (135° + 서머타임) | **−92분** |

### 2-3. 시각 미상일 때는 보정하지 않는다

출생시각을 모르면 정오를 임시로 쓰므로 보정에 의미가 없다. `solarTimeMode` 를 `standard` 로
강제하고 시주를 제외한다. 임의 시각을 채워 넣지 않는다.

### 2-3-1. 시각 입력은 **분 단위**로 받는다 — 12지시 선택을 넣지 않은 이유 (TASK-23)

UI 는 시(0~23)·분(0~59) 드롭다운 두 개다. 분을 5·10분 단위로 줄이지 않았고,
"자시~해시" 12지시만 고르는 방식도 넣지 않았다. 둘 다 **보정 때문에** 안 된다.

시지(時支) 경계는 시계시의 정시에 있지 않다. 경도 보정이 −32분이면 자시(23:00~01:00)의
끝은 시계시 **01:32** 로 밀린다. 그래서 같은 "1시대" 안에서도 앞뒤가 시진이 갈린다.

실측 (1990-05-17 · 서울 · `solarTimeMode: "longitude"` · 보정 −32분):

| 시계시 | 보정 후 | 시주 | 시지 |
| --- | --- | --- | --- |
| 01:30 | 00:58 | 경자 | 자(子) |
| 01:31 | 00:59 | 경자 | 자(子) |
| **01:32** | 01:00 | **신축** | **축(丑)** |
| 01:35 | 01:03 | 신축 | 축(丑) |

- **5분 단위로 줄이면** 경계가 선택지 사이(30 과 35)에 끼어 01:31~01:34 출생자를 표현할 수
  없다. 어느 쪽으로 반올림해도 절반은 시주가 틀린다. 1분 단위여야 한다.
- **12지시만 고르게 하면** 그 시진의 대표 시각을 우리가 정해 채워야 한다. 그것은 2-3 절의
  "임의 시각을 채워 넣지 않는다" 와 정면으로 충돌하고, 더구나 사용자가 고른 시진과 보정 후
  시진이 어긋날 수 있다 (사용자는 시계시로 "축시" 라고 생각하는데 보정하면 자시가 된다).
  모르면 채우지 말고 **시주를 제외**한다.

선택지 표와 이 규칙은 `lib/form/birth-time.ts` 에 있고 `birth-time.test.ts` 가 고정한다.

### 2-4. 자시(子時) 학파

23:00~23:59 출생자의 일주를 어느 날로 볼지에 대한 학파 차이다. 둘 다 지원한다.

| 옵션 | 일주 경계 | 예: 2024-01-01 23:30 |
| --- | --- | --- |
| `yajasi` **(기본)** | 자정 | 일주 갑자 + 시주 **갑자** |
| `jasi` | 23:00 | 일주 을축 + 시주 **병자** |

기본값을 `yajasi` 로 둔 이유: 한국 만세력 다수 관행이 야자시·조자시를 구분한다.
어느 쪽을 골라도 오서둔으로 시주를 다시 계산하므로 **일주와 시주가 항상 같은 학파**다.

---

## 3. 검증 항목과 결과

### A. 일주 — 율리우스일 기반 60갑자 연속성 (7,236건 · 불일치 0)

일주는 절기와 무관하게 60일 주기로 끊기지 않고 흐른다. 이 성질로 독립 검증했다.

앵커 두 개가 서로 정합함을 먼저 확인했다.

```
1900-01-01 = 갑술일 (60갑자 10번)
2000-01-01 = 무오일 (60갑자 54번)
두 날짜 간격 36,524일 → (10 + 36524) mod 60 = 54 ✅
```

1900~2100 각 달 1·11·21일에 대해 `(10 + 경과일수) mod 60` 과 대조했다.

### B. 년주 — (연도 − 4) mod 60, 입춘 기준 (402건 · 불일치 0)

입춘을 확실히 지난 3/15 는 당해 간지, 입춘 전인 1/15 는 전년 간지여야 한다.
1900~2100 전 연도에서 확인했다.

### C. 월주 — 오호둔 (2,412건 · 불일치 0)

`인월 천간 = (년간 % 5) × 2 + 2` 에서 월지만큼 순행하는 규칙과 대조했다.
갑기년→병인월, 을경년→무인월, 병신년→경인월, 정임년→임인월, 무계년→갑인월.

### D. 시주 — 오서둔 (720건 · 불일치 0)

60일 연속 × 12시진 전수. `시간 = (일간 % 5) × 2 + 시지` 규칙과 대조했다.
시진 경계(자시가 23:00 시작, 01:00 종료)도 확인했다.

### E. 시간 보정 (문헌 대조 · 전건 일치)

2-1 의 표가 그 결과다. 자오선 전환 4개 시점, 서머타임 시행 12개 연도,
비시행 6개 연도, 보정량 4개 케이스, 균시차 범위를 확인했다.

### F. 절기 — 한국천문연구원 공표값 대조 (6건 · 전건 2분 이내)

라이브러리 출력(베이징 시간)을 절대 시각으로 바꾼 뒤, 공표값을 **그 시기 한국 표준시로
해석해** 비교했다. 1958년은 표준시가 UTC+8:30 이었으므로 +9 로 고정 환산하면 30분 어긋난다.

| 연도 | 절기 | 계산(KST) | 공표(KST) | 차 |
| --- | --- | --- | --- | --- |
| 2024 | 입춘 | 17:27 | 17:27 | 0분 |
| 2024 | 우수 | 13:13 | 13:13 | 0분 |
| 2000 | 입춘 | 21:40 | 21:40 | 0분 |
| 1990 | 입춘 | 11:14 | 11:14 | 0분 |
| 1988 | 입춘 | 23:42 | 23:43 | 1분 |
| 1958 | 입춘 | 16:19 | 16:20 | 1분 ← UTC+8:30 시기 |

> 최초 작성 시 2000년 입춘 공표값을 20:40 으로 잘못 적어 검증이 실패했고, 출처 확인 결과
> **21:40 이 맞고 라이브러리가 옳았다.** 검증기의 기대값도 틀릴 수 있다는 사례로 남긴다.

### G. 경계 케이스 (전건 통과)

- 자시 학파 두 방식의 일주가 하루 차이로 갈리는지
- 두 학파 모두 시주 천간이 자기 일간과 오서둔으로 정합한지 (1-2 재발 방지)
- 윤달: 음력 2020-04-15 → 양력 2020-05-07 / 음력 **윤**4-15 → 양력 2020-06-06
- 윤달 표기에 "윤" 이 붙고 평달에는 붙지 않는지
- 시각 미상: 시주 `null`, 보정 비활성, 오행 합 6 (3기둥 × 2)
- 시각 있음: 오행 합 8 (4기둥 × 2)
- 지원 범위 끝단(1900-01-01, 2100-12-31) 이 예외 없이 계산되는지
- 보정으로 날짜가 넘어가는 케이스: 1990-05-17 00:10 → 05-16 23:38 → **전날 일주**
- 60갑자 인덱스 왕복 일치

---

## 3-2. 파생 근거 검증 (TASK-04)

해석 근거를 늘리면서 **고전 규칙으로 확정되는 것**과 **우리가 정한 관례**를 나눠 담았다.
관례를 사실처럼 내보내면 안 되기 때문이다.

| 항목 | 성격 | 근거 |
| --- | --- | --- |
| 지지 십신 | 고전 | 지장간 본기(정기) 천간을 일간과 비교 |
| 지장간 표 | 고전 | 12지지 × 여기·중기·정기 (판본 차이는 아래 표에 명시) |
| 왕상휴수사 | 고전 | 당령자 왕 / 왕이 생하는 것 상 / 왕을 생하는 것 휴 / 왕을 극하는 것 수 / 왕이 극하는 것 사 |
| 신강·신약 3기준 | 고전 | 득령(월지 본기)·득지(일지 통근)·득세(나머지, 통근 포함) |
| 대운 순행·역행 | 고전 | 양남·음녀 순행 / 음남·양녀 역행 |
| 대운 간지 | 고전 | 월주에서 순행 또는 역행 |
| 세운 간지 | 고전 | (연도 − 4) mod 60 |
| **오행 점수 배수** | **우리 관례** | 왕 1.5 / 상 1.2 / 휴 1.0 / 수 0.8 / 사 0.6 |
| **신강·신약 4단계** | **우리 관례** | 3기준 충족 개수 → 신강 / 약간 신강 / 약간 신약 / 신약 |
| **대운수 반올림** | **우리 관례** | 일수 ÷ 3 반올림, 최소 1세 (나머지를 개월로 환산하지 않음) |

관례에 해당하는 값은 `lib/saju/analysis.ts` 의 상수(`SEASONAL_MULTIPLIER` 등)로 모아 뒀다.
바꾸려면 그 상수만 고치면 된다.

프롬프트에는 점수를 **숫자로 인용하지 말라**고 명시했다. 우리가 정한 배수에서 나온 값이라
절대적 수치가 아니기 때문이다. 샘플 5건 모두에서 숫자 인용이 없었다.

#### 지장간 통근 (TASK-32)

`lib/saju/ganji.ts` 의 `JI_JANGGAN` — 12지지 × **여기(餘氣)·중기(中氣)·정기(正氣)**.
마지막 항목이 정기(=본기)이고 `jiBongi` 가 그것을 읽는다. 표를 두 벌로 두지 않기 위해서다.

| 지지 | 여기 | 중기 | 정기 | | 지지 | 여기 | 중기 | 정기 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 자(子) | 임 | — | 계 | | 오(午) | 병 | 기 | 정 |
| 축(丑) | 계 | 신 | 기 | | 미(未) | 정 | 을 | 기 |
| 인(寅) | 무 | 병 | 갑 | | 신(申) | 무 | 임 | 경 |
| 묘(卯) | 갑 | — | 을 | | 유(酉) | 경 | — | 신 |
| 진(辰) | 을 | 계 | 무 | | 술(戌) | 신 | 정 | 무 |
| 사(巳) | 무 | 경 | 병 | | 해(亥) | 무 | 갑 | 임 |

**판본 차이가 있는 자리와 채택한 쪽** — 아래 셋은 유파마다 빠지거나 다르게 적힌다.

| 자리 | 판본 차이 | 채택 |
| --- | --- | --- |
| 오(午)의 중기 | 기(己)를 넣지 않는 판본이 있다 | **넣는다** |
| 해(亥)의 여기 | 무(戊)를 넣지 않는 판본이 있다 | **넣는다** |
| 신(申)의 여기 | 무(戊) / 기(己) 로 갈린다 | **무(戊)** |

**일수 가중치(여기 7일·중기 3일 …)를 쓰지 않는다.** 일수 배분은 유파마다 다르지만
"어느 천간이 들었는가" 는 이견이 거의 없다. **있나 없나만** 보면 우리 관례를 늘리지 않고
고전 규칙 안에 머문다. 가중치를 넣는 순간 `SEASONAL_MULTIPLIER` 같은 관례가 하나 더 생긴다.

**통근을 쓰는 곳은 신강·신약 판정뿐이다.** 화면과 프롬프트의 지지 십신(`jiSipsin`)은
그대로 본기 기준이다. 표시까지 바꾸면 근거 표시가 흔들린다.

##### 득령은 왜 본기 그대로인가

세 기준 중 **득지·득세만** 지장간까지 연다. 월령(月令)은 "그 달을 지배하는 기운" 이라
숨어 있는 글자가 아니고, 여기까지 열면 세 기준이 전부 통근 검사가 되어 판정이 신강 쪽으로
쏠린다. 격자 표본 194,400건(년·월·일·시 간지를 훑음)으로 실측한 분포:

| 기준 | 신강 | 약간 신강 | 약간 신약 | 신약 | 신강 계열 |
| --- | --- | --- | --- | --- | --- |
| 본기만 (변경 전) | 5.6% | 26.2% | 43.0% | 25.2% | **31.8%** |
| **득지·득세만 통근 (현재)** | 15.6% | 43.1% | 33.4% | 8.0% | **58.6%** |
| 3기준 모두 통근 | 30.2% | 43.2% | 22.6% | 4.0% | 73.4% |

`strength.verdict` 는 `constitution.ts` 의 대사 기조(발산형/축적형) 입력이므로 이 분포가
그대로 다이어트 접근 순서의 분포가 된다. 세 기준을 모두 열면 73%가 발산형이 되어 2분법이
사실상 죽는다.

##### 실측 사례

`1999-12-09 22:12` (기묘 · 병자 · **을미** · 정해, 일간 을목)

- 일지 미(未)의 지장간은 정·**을**·기다. 본기 기토만 보면 편재라 득지가 떨어져
  **약간 신약**이 나왔고, 다른 만세력 앱은 신강으로 냈다.
- 중기 을목이 일간과 같은 글자이므로 통근이 선다 → 득지 ○ → **약간 신강**.
- 같은 사주의 오행 점수는 이미 일간 편 66%(목 2.4 + 수 3.0 vs 화 1.2 + 토 1.6 + 금 0)를
  가리키고 관성(금)이 0개였다. 세력 계산과 3기준 판정이 서로 반대를 말하던 상태가 풀렸다.
- 일지 십신 표시는 **편재 그대로**다. `pillars.test.ts` 가 이 둘을 함께 고정한다.

**돕는 글자 수(`supportingChars`)는 지지를 전부 통근으로 센다.** 3기준이 아니라 세력을 세는
값이라 "뿌리가 있는가" 로 보는 것이 맞다. 그래서 득령이 아니오인 월지가 이 수에는 잡힐 수
있다 — 당령과 통근은 다른 층이다. `strength.rooted` 가 어느 자리에 뿌리가 섰는지 함께 내고,
프롬프트와 결과 화면이 그 줄을 보여준다.

### H. 지지 십신 (전건 통과)

- 지장간 본기 12건을 문헌값으로 다시 적어 대조
- 지지 십신 120건(일간 10 × 지지 12)이 본기 천간의 십신과 일치
- 자기 천간의 십신은 항상 비견
- 십신 10종이 모두 산출되는지 (누락 분기 없음)

### I. 왕상휴수사 (60건 · 불일치 0)

문헌 표를 그대로 적어 대조했다. 각 계절에서 왕·상·휴·수·사가 정확히 하나씩 나오는지도 확인.

| 계절 | 왕 | 상 | 휴 | 수 | 사 |
| --- | --- | --- | --- | --- | --- |
| 봄(인묘진) | 목 | 화 | 수 | 금 | 토 |
| 여름(사오미) | 화 | 토 | 목 | 수 | 금 |
| 가을(신유술) | 금 | 수 | 토 | 화 | 목 |
| 겨울(해자축) | 수 | 목 | 금 | 토 | 화 |

### J. 대운 (전건 통과)

- 순행/역행 4개 조합 (양남·음녀·음남·양녀)
- 대운수: 0.5일→1세(최소값), 9일→3세, 28일→9세
- 1990년(경오·양간) 남자 순행 / 여자 역행 실측 확인
- 대운 간지가 월주에서 한 칸씩 순행·역행하는지 (8구간 전수)
- 대운 구간이 10년 단위로 빈틈없이 이어지는지
- 성별 미지정이면 `daeun === null` (순행·역행을 정할 수 없으므로 내지 않는다)
- 대운수 근거 일수가 0~32일 범위인지 60건 (절기 간격이 약 15일이므로)

절기 조회는 라이브러리 테이블이 기준일 주변 1년여만 담고 있어, 연말·연초 출생자는
한 테이블로 앞뒤 절기를 모두 찾지 못한다. **앞뒤 45일씩 옮긴 기준일에서도 모아 합친다.**
또한 테이블 키가 간체(`惊蛰`, `芒种`)이고 인접 연도 항목은 로마자(`LI_CHUN`)로 중복
제공되므로 두 표기를 모두 받는다.

### K. 세운 (전건 통과)

- 1984년 = 갑자년 앵커
- 간지·나이(세는나이)가 규칙과 일치
- 세운에 붙은 대운이 나이 구간과 정합

### L. 신강/신약 (108건 · 불일치 0)

- 판정과 3기준 충족 개수가 항상 정합
- 돕는 글자 수가 전체 글자 수를 넘지 않음
- 시각 미상이면 판정 글자 수 5 (시주 제외)
- 오행 점수가 `개수 × 계절 배수`와 일치

### M. 지장간 통근 (TASK-32 · 전건 통과)

지장간 표를 **테스트 안에 독립으로 다시 적어** 대조한다. 소스를 불러다 쓰면 자기참조가 된다.

- 12지지의 지장간이 문헌 표와 일치 (`ganji.test.ts`)
- 정기(마지막 글자)가 `jiBongi` 와 같음 — 표를 두 벌로 두지 않는다는 불변식
- 사왕지(자·묘·유)는 두 글자, 나머지는 세 글자 / 한 지지 안에 같은 천간이 두 번 들지 않음
- 정기의 오행이 지지 자체의 오행과 일치 (12건)
- **본기가 이미 돕는 지지는 반드시 통근** (10 × 12 전수) — 판정이 약해지는 방향으로
  움직이지 않는다는 불변식
- 통근하지 않는 조합이 남아 있음 — 판정이 늘 참이 되어 버리지 않는지
- 득지는 통근, 득령은 본기 (일간 10 × 지지 12 전수 · `analysis.test.ts`)
- 돕는 글자 수가 통근 기준 독립 계산과 일치 (일간 10종)
- 실측 사례 `1999-12-09 22:12` 이 원국 계산부터 대사 기조까지 이어짐 (`pillars.test.ts`)

### 해석 품질 표본 점검 (샘플 5건)

TASK-04 완료 기준이다. 5건 × 8항목 = **38/40 통과**.

미달 2건은 모두 `diet` 유형에서 나왔고, 검증 정규식이 diet 섹션 지시보다 엄격했던 탓이다
(계절 언급을 계절 이름으로만 탐지, 득령·득지·득세를 diet 지시에 넣지 않았음).
이후 diet 지시를 general 과 같은 수준으로 맞췄다.

---

## 3-3. 체질 판정 (TASK-14)

`lib/saju/constitution.ts` — `diet` 유형의 체질 판정을 **코드가** 정한다. LLM 은 서술만 한다.
원국을 코드가 계산하는 것과 같은 이유다: 판정을 매번 즉흥으로 내면 같은 사주에 다른 답이 나온다.

판정 축은 다섯이고, 전부 이미 검증된 근거(오행 점수·왕상휴수사·신강신약·십신)에서 파생된다.

| 축 | 값 | 근거 | 성격 |
| --- | --- | --- | --- |
| 오행 균형 | 과다 / 적정 / 부족 | 오행 점수가 평균의 몇 배인가 | **우리 관례**(임계값) |
| 한열(조후) | 한·서늘·중화·따뜻·열 | 계절의 한난 + 원국 화·수 세력 | 고전(순서) + **우리 관례**(눈금) |
| 대사 기조 | 발산형 / 축적형 | 신강 계열 / 신약 계열 | **우리 관례**(2분법) |
| 살이 붙는 패턴 | 근육·식욕·불규칙·스트레스·정체형 | 십신 우세 그룹 | 고전(십신 5분류) + **우리 관례**(대응표) |
| 다이어트 접근 순서 | 활동량·식사량 조절·리듬 고정·회복 우선 | 대사 기조 × 걸리는 지점 | **전부 우리 관례**(2×2 대응표) |
| 움직임 종류 (TASK-40) | 유산소·근력·이완 중심 / 저강도 지속 | 접근 순서 (1:1) | **전부 우리 관례**(파생표) |
| 식사 순서 (TASK-40) | 순서·시각 규칙 5가지 | 살이 붙는 패턴 (1:1) | **전부 우리 관례**(파생표) |

| 관례 값 | 상수 | 현재 값 |
| --- | --- | --- |
| 과다 임계 | `EXCESS_RATIO` | 평균 점수의 1.5배 이상 |
| 부족 임계 | `DEFICIENT_RATIO` | 평균 점수의 0.5배 이하 |
| 한열 눈금 | `SEASON_TILT` + 화·수 비교 | 각각 −1·0·+1, 합 −2~+2 를 5단계로 |
| 십신 동점 | `SIPSIN_GROUPS` 순서 | 비겁→식상→재성→관성→인성 중 앞선 쪽 |
| 걸리는 지점 | `SITE_OF_PATTERN` | 근육·정체형 → 움직임 / 식욕·불규칙·스트레스형 → 먹는 것 |
| 접근 순서 | `APPROACH_TABLE` | 아래 2×2 표 |
| 움직임 종류 | `MOVEMENT_PLAN` | 활동량→유산소 / 식사량→근력 / 회복→이완 / 리듬→저강도 지속 |
| 식사 순서 | `MEAL_PLAN` | 패턴 5가지에 순서·시각 규칙 하나씩 |

절대 개수가 아니라 **평균 대비 비율**로 자르는 이유: 시각 미상이면 글자가 6개뿐이라 같은
"2개" 의 무게가 다르다. 비율로 두면 기둥 수와 무관하게 같은 뜻이 된다.

조후를 넣은 이유: 오행 개수만으로는 "여름에 태어난 화 많은 사주" 와 "겨울에 태어난 화 많은
사주" 가 같아진다. 계절의 한난을 먼저 보고 원국 세력으로 보정하는 것이 고전 조후의 순서다.

#### 다이어트 접근 순서 — 2×2 대응표 (TASK-24)

"무엇을 먼저 고정하는가" 를 정하는 축이다. **전부 우리 관례다** — 고전에 대응물이 없다.
`METABOLISM_NOTE` 가 이미 순서를 가리키고 있었던 것을 독립된 축으로 끌어올렸다
(발산형 = "쓰는 양을 늘리는 순서", 축적형 = "수면과 끼니를 먼저 고정하고 강도는 나중에").

|  | 걸리는 지점 = **움직임** | 걸리는 지점 = **먹는 것** |
| --- | --- | --- |
| **발산형** (신강 계열) | 활동량 우선 | 식사량 조절 우선 |
| **축적형** (신약 계열) | 회복 우선 | 리듬 고정 우선 |

걸리는 지점은 살이 붙는 패턴에서 나온다 — 근육형(비겁)·정체형(인성)은 활동량이 줄면 붙는
결이고, 식욕형(식상)·불규칙형(재성)·스트레스형(관성)은 먹는 양·시각·상황에서 붙는 결이다.

**동점이 없다.** 두 입력이 각각 이미 결정론적이므로(대사 기조는 신강신약의 4단계를 2분한 것,
패턴은 십신 우세 그룹이고 그쪽 동점은 고전 십신 순서로 이미 고정) 2×2 의 어느 칸에 떨어지는지가
유일하게 정해진다. 축을 더 넣을 때는 동점 처리를 반드시 함께 고정할 것.

**한열을 이 축에 넣지 않았다.** 한열은 "무엇을 먼저 고정하는가" 와 다른 층이다 — 같은 순서를
어느 온도·시간대에 실행하느냐를 정한다. 섞으면 값이 20가지가 되면서 각 칸의 근거를 설명할 수
없게 된다. 한열은 `THERMAL_GUIDE` 의 식습관·움직임 항목으로 실행 조건에 붙는다.
테스트가 "같은 대사 기조·같은 패턴이면 한열이 달라도 방식이 같다" 를 확인한다.

#### 실행 방법 — 움직임 종류와 식사 순서 (TASK-40)

`나에게 맞는 다이어트 방법`(`diet-method`) 유형이 쓰는 축이다. **경계를 (B) 일부 개방으로
넓혔다** — 방법은 구체적으로 쓰고 수치는 계속 막는다. 금지의 근거 넷 중 셋(입력이
생년월일시뿐이라 근거가 없다 · 코드가 못 정하면 LLM 이 즉흥으로 낸다 · 안전)은 **수치에만**
걸리고, 방법 자체는 판정에서 코드가 고를 수 있는 값이다.

| 층 | 정하는 것 | 근거 축 | 상수 |
| --- | --- | --- | --- |
| 무엇을 먼저 고정하는가 | 접근 순서 | 대사 기조 × 걸리는 지점 | `APPROACH_TABLE` |
| **어떤 종류로 움직이는가** | 움직임 종류·강도의 결 | ← 접근 순서 (1:1) | `MOVEMENT_PLAN` |
| **어떤 순서로 먹는가** | 식사 순서·시각 규칙 | ← 살이 붙는 패턴 (1:1) | `MEAL_PLAN` |
| 언제·어떤 온도로 실행하는가 | 시간대·조리·음식 온도 | 한열 | `THERMAL_GUIDE` |

**새 동점이 없다.** 두 표 모두 이미 결정론적인 축에서 1:1 로 파생되므로 판정이 새로 필요
없다. 테스트가 60개 원국을 훑어 "같은 접근 순서면 항상 같은 움직임 종류" 를 확인한다.

**시간대와 온도를 이 표에 넣지 않았다.** `APPROACH_TABLE` 이 한열을 뺀 것과 같은 이유이며,
테스트가 `MOVEMENT_PLAN`·`MEAL_PLAN` 문구에 온도·성미 어휘(온성·냉성·따뜻하게 …)가
들어오지 않는지 확인한다. 들어오면 층이 무너진다.

**식품 이름도 이 표에 없다.** 무엇을 먹을지는 `ELEMENT_FOOD` 가 정하고 이 표는 순서와
시각의 규칙만 정한다. 테스트가 `MEAL_PLAN` 에 `ELEMENT_FOOD` 의 재료 이름이 섞이지 않는지
본다 — 섞이면 목록 밖 식품이 이 경로로 새어 나간다.

검사는 두 갈래다. 막는 것(처방 어휘 · 문구에 숫자)과 **여는 것**(종류 네 가지가 전부 쓰이는지,
`how`·`caution` 이 실제로 구체적인지). 후자가 없으면 나중에 규칙을 조이면서 조용히 다시
닫아 버려도 아무도 모른다. `app/disclaimer/page.tsx` 가 이 경계를 그대로 서술한다.

#### 재료 범주 (TASK-27)

"무엇을 먹으면 좋은가" 도 **코드가 고른다.** 식품 이름을 LLM 이 고르게 하면 같은 사주에 다른
목록이 나오고 그때그때 효능이 붙는다. 체질 판정과 같은 원칙이다.

| | 근거 | 성격 |
| --- | --- | --- |
| 오행 → 오미 (목=신맛·화=쓴맛·토=단맛·금=매운맛·수=짠맛) | 고전 배속 | **고전** |
| 오행 → 오색 (목=청·화=적·토=황·금=백·수=흑) | 고전 배속 | **고전** |
| 그 배속에서 고른 **구체 재료 목록** (`ELEMENT_FOOD`) | 한국에서 흔히 구하는 것 | **우리 관례** |
| 부족은 곁들이기 / 과다는 더 늘리지 않기 (`FOOD_HOW`) | 개인 변수를 모르므로 | **우리 관례** |

**섹션을 새로 만들지 않았다.** 재료는 기존 `잘 맞는 식습관` 절 안에서 말한다 (diet 는
TASK-24 로 이미 8절이고 더 늘리면 생성 시간이 는다). 구현도 새 구조를 만들지 않고
**기존 `focus` 배열에 얹었다** — `focus` 는 이미 과다·부족 오행마다 한 줄씩 갖고 있어
결정론과 `even` 처리(항목 없음)를 그대로 물려받는다.

**한열이 재료를 고르지 않는다.** 재료는 오행이, 조리·온도는 한열이 정한다. 식품의 온·냉
성질표는 출처가 한의학인데 면책 고지가 "한의학과 무관" 을 약속하므로, 성미(性味) 용어를
쓰지 않고 조리·온도는 기존 `THERMAL_GUIDE` 의 생활어 문구에 맡겼다. 표를 하나만 두어
두 곳이 어긋날 여지도 없앴다. 테스트가 성미 용어(온성·냉성 등)를 막는다.

`even`(과다·부족이 하나도 없음)은 표본 408건 중 5건(1.2%)이다. 이 경우 재료도 없다 —
없는 근거로 식품을 지어내지 않고, 프롬프트가 한열·대사 기조 위주로 쓰라고 지시한다.

### M. 체질 판정 (표본 120건 · `lib/saju/constitution.test.ts`)

60갑자를 서로 다른 보폭으로 훑어 만든 60개 원국 × (시각 있음/미상) 로 검사한다.
난수를 쓰지 않으므로 실패하면 그대로 재현된다.

- **결정론** — 같은 입력 두 번 → 완전히 같은 판정 (완료 기준)
- 십신 동점이면 고전 순서에서 앞선 그룹 (동점 처리를 고정하지 않으면 판정이 흔들린다)
- 오행 3단계가 임계 상수와 항상 일치 / 개수 0인 오행은 항상 부족
- 전부 과다이거나 전부 부족일 수 없음 (평균 기준의 성질)
- 한열 눈금 점수가 −2~+2 이고 5단계와 짝이 맞음, 조후 케이스 10건 표 대조
- **십신을 세는 글자가 신강신약 판정 글자와 같음** (시주 있으면 7자, 없으면 5자 · 일간 제외)
  — 두 판정이 다른 글자를 보면 프롬프트 안에서 근거가 어긋난다
- 패턴 5종이 표본에서 모두 출현 (한쪽으로 몰리면 매핑이 죽은 것)
- **접근 순서 4종이 표본에서 모두 출현** + 2×2 표를 그대로 따름 (표를 테스트에 박아 대조)
- 접근 순서도 결정론 · 한열이 방식을 바꾸지 않음 (판정에서 뺀 이유를 검사로 고정)
- **재료 범주**가 오미 배속과 맞고, 과다·부족마다 `focus` 에 실리며, 목록에 상표·보조식품·
  숫자·성미 용어가 없음. `even` 이면 재료도 비어 있음

### N. 표현 가이드 (`constitution.test.ts` · `prompt.test.ts`)

면책 고지가 "진단이나 치료 방법이 아니다" 라고 약속하므로, **사용자에게 인용될 수 있는
문구 전부**를 금지 어휘로 훑는다: 치료·완치·처방·진단·질병·질환·증상·효능·효과·약효·
해독·독소·면역·항암·염증·혈압·혈당·콜레스테롤·보장·반드시 등.

감량 "방법" 을 말하는 절이 생기면서(TASK-24) **처방으로 읽히는 것**을 따로 막았다:
단식·칼로리·kcal·kg·그램·저탄·고지·키토·원푸드·목표 체중·기간 표현·보조제·영양제.
접근 순서 문구에는 **숫자가 하나도 들어가지 않는지**도 검사한다 — "2주에 3kg" 류가 섞이는
즉시 처방이 되기 때문이다. 프롬프트에서도 유형 규칙과 섹션 지침 **두 곳**에서 막는다
(한쪽만 있으면 그 절만 참조하는 모델이 선을 못 본다).

식품(TASK-27)은 한 겹 더 있다. 목록이 코드에 닫혀 있어도 **본문에 다른 식품이 등장하는 것은
프롬프트만 막을 수 있으므로**, "재료 범주 안에 있는 것만 쓰라 / 목록 밖 식품 이름을 만들지
말라" 를 지시하고 실제 생성으로 확인했다.

실측 3건 (1990-05-17 · 1978-11-03 · 2001-02-19 · `gemini-3.5-flash-lite`):

- 8절이 순서대로 전부 도착
- **목록 밖 식품 이름 0건** (닭가슴살·단백질·현미·견과류 등 34개 흔한 식품으로 훑음)
- 금지 어휘·처방 어휘 0건
- 과다 계열은 "굳이 더 늘리지 않아도", 부족 계열은 "한 가지씩 곁들이는" 으로 나옴

> 검출기 주의: "견과" 로 훑으면 **"비견과 겁재"** 에 걸린다. 식품 이름 검사에 명리 용어와
> 겹치는 부분 문자열을 쓰지 말 것 (실제로 한 번 오탐이 났다).

오행-신체 배속(목=간·담, 화=심·소장 …)은 고전 배속이지만 **판정 결과에도 프롬프트에도
내보내지 않는다.** 장기 이름이 나가면 진단처럼 읽힌다. 배속의 출처를 밝히는 주석
(`BODY_AXIS.classical`)으로만 두고, 대신 생활어 축(`axis`: "소화와 먹는 리듬" 등)을 쓴다.
테스트가 판정 결과와 프롬프트 양쪽에서 장부 이름이 없음을 확인한다.

한열 눈금 점수(`thermalScore`)도 오행 점수와 같은 이유로 프롬프트에 숫자로 내보내지 않는다.
십신 글자 수는 원국에서 센 사실이므로 그대로 쓴다.

`lib/prompt.test.ts` 는 판정이 실제로 프롬프트에 실리는지도 본다 — 실리지 않으면 LLM 이
즉흥으로 판정하게 되고, 그러면 이 태스크가 무의미해진다. `general` 유형은 체질 블록을
받지 않으며, 두 유형의 원국 사실 블록이 동일함도 확인한다.

---

## 3-4. 올해 세운 판정 (TASK-15 · TASK-39)

`lib/saju/yearly.ts` — 코드가 정한다. 축은 둘이지만 **밖으로 나가는 것은 하나뿐이다.**

| 축 | 값 | 근거 | 성격 | 프롬프트로 나가나 |
| --- | --- | --- | --- | --- |
| 작용 | 보완 / 가중 / 중립 | 세운 오행이 원국의 부족을 채우는가, 과다에 더하는가 | 고전(견주는 방식) + **우리 관례**(3단계) | **예** (`diet`) |
| 주제 | 경쟁과 독립 / 표현과 생산 / 활동과 결실 / 책임과 압박 / 학습과 정비 | 세운 천간의 십신 그룹 | 고전(십신 5분류) + **우리 관례**(대응표) | 아니오 |

세운 두 글자의 오행이 같으면(예: 병오 = 화·화) **한 번만 센다** — 한 해의 성격은 하나다.
채우는 개수와 더하는 개수를 비교해 같으면(둘 다 0 포함) 중립이다.

### 왜 작용만 내보내는가 (TASK-39 결정 ①)

`yearly` 유형이 없어지면서 이 판정은 `diet` 의 "올해의 몸 흐름" 섹션으로 옮겨갔다.
두 축의 성격이 다르다.

- **작용은 원래 몸 쪽 값이다.** `analyzeYearly` 가 `constitution.deficient`/`excess` 를 받아
  계산한다 — 즉 체질 판정에서 나온 값이고, 운세 유형에만 흘려보내던 쪽이 이상했다.
  옮기는 데 새로 정할 것이 없다.
- **주제는 생활 영역 어휘다.** `책임과 압박` 을 몸 이야기로 옮기려면 십신→몸 대응표를
  새로 만들어야 하고, 그 표에는 근거가 없다. 게다가 "압박이 늘어 몸이 상한다" 류의 추론은
  면책 고지가 막는 의학적 주장에 닿는다. **넘기지 않으면 그 문제가 생기지 않는다.**

계산은 남겨 뒀다. 십신 그룹 판정 자체는 고전이고, 생활 영역을 다루는 유형이 다시 생기면
그대로 쓸 수 있다. 되살릴 때 필요한 것은 표가 아니라 **어느 맥락에 쓸지의 판단**이다.

### O. 올해 세운 (60년 × `lib/saju/yearly.test.ts`)

2000~2059 전 구간을 훑는다. 난수를 쓰지 않으므로 실패하면 그대로 재현된다.

- **결정론** — 같은 사주·같은 연도 두 번 → 완전히 같은 판정
- 세운 간지가 고전 규칙 `(연도 − 4) mod 60` 과 일치
- 작용 판정이 `fills`/`piles` 개수 비교와 항상 일치
- `fills`·`piles` 가 원국의 부족·과다 목록 안에서만 나옴
- 실측 케이스 3건: 2008 무자 → 보완(수 부족을 채움) / 2026 병오 → 가중(화 과다에 더함) /
  2029 기유 → 중립
- 다섯 주제가 60년 안에서 모두 출현, 세 작용이 60개 원국에서 모두 출현
- 성별 미지정(대운 null)에서도 판정이 나옴
- 문구 8개를 금지 어휘로 훑음: 반드시·틀림없이·확실히·보장·대박·횡재·사고·이혼·질병·
  죽음·합격·당첨·투자 등. 면책 고지가 "미래 예측으로 받아들이지 마세요" 를 약속한다.
  **유형이 없어져도 이 검사는 남긴다** — 작용 문구는 여전히 사용자에게 나간다

프롬프트 쪽 검사는 `lib/prompt.test.ts` 에 있다 — 작용 판정이 실제로 실리는지,
**주제 축이 새어 나가지 않는지**(`themeLabel`·`themeNote` 부재 확인), 판정 블록이
`general` 로 새지 않는지, **월별 운세를 쓰지 말라는 지시가 두 곳(판정 블록 ·
`TYPE_RULES.diet`)에 남아 있는지**를 본다.

---

## 3-4-1. 대운(10년) 판정 (TASK-45)

`lib/saju/decade.ts` — 코드가 정한다. **우리 관례가 하나만 늘었다:**
**"대운을 세운과 같은 3단계(보완·가중·중립)로 본다".**

| 축 | 값 | 근거 | 성격 | 프롬프트로 나가나 |
| --- | --- | --- | --- | --- |
| 작용 | 보완 / 가중 / 중립 | 대운 오행이 원국의 부족을 채우는가, 과다에 더하는가 | 고전(견주는 방식) + **우리 관례**(3단계를 대운에도 적용) | **예** (`decade`) |
| 직전과 견준 결 | 완화 / 심화 / 전환 / 유지 | 두 구간의 작용을 견줌 | **위 관례에서 파생** (새 축이 아니다) | **예** (`decade`) |

- **판정 규칙을 `yearly.ts` 에서 그대로 가져왔다.** 두 글자의 오행이 같으면 한 번만 세고,
  채우는 개수와 더하는 개수를 비교해 같으면(둘 다 0 포함) 중립이다. **새로 정한 임계값이
  없다** — 대운 세기 가중이나 천간/지지 비중 같은 값을 만들면 관례가 불어난다. 만들지 말 것.
- **직전 구간 비교도 새 관례가 아니다.** 같은 규칙을 한 번 더 적용한 것이고, 두 판정이
  같으면 `유지` 라 **동점 처리가 따로 필요 없다.** 늘어난 것은 적용 횟수뿐이다.
- **다음 구간은 판정하지 않는다.** 아직 오지 않은 시간을 말하면 예측이 되고,
  `app/disclaimer/page.tsx` 가 "시기를 정해 드리는 것이 아니다" 를 밖에서 약속한다.
- 문구 표는 **세운 문구와 공유하지 않는다.** 층이 다르기 때문이다 — 세운은 한 해를 어떻게
  보낼지, 대운은 10년 동안 몸의 기본값이 어디로 기울어 있는지다. 같은 문장을 쓰면
  `diet` 의 "올해의 몸 흐름" 과 `decade` 가 같은 말을 한다. `decade.test.ts` 가 대조한다.

### 성별 미지정이면 이 유형은 성립하지 않는다

대운 순행/역행이 성별로 정해지므로 `chart.daeun` 이 null 이고, 그러면 `chart.decade` 도
null 이다. **임의로 순행을 정하지 않는다** — 그러면 같은 사주에 다른 판정이 나간다.

그래서 이 유형만 성별을 **요구**한다(`READING_TYPE_NEEDS_GENDER`). 막는 자리는 둘이다:
폼이 이유와 함께 제출을 막고, `sajuInputSchema` 의 refine 이 서버에서 다시 막는다
(클라이언트 검증은 UX 용이고 신뢰 대상이 아니다).

**홈에서 카드를 막지 않는 이유:** 성별은 `BirthInputProvider` 의 메모리에 있어 클라이언트만
안다. 홈에서 카드를 흐리게 하려면 `/` 에 클라이언트 컴포넌트가 들어가고, 그러면 **`/` 를
통째로 정적으로 두는 성질이 깨진다.** 카드는 누구에게나 보이고 폼이 이유를 말한다.

### Q. 대운 판정 (`lib/saju/decade.test.ts`)

| 검사 | 결과 |
| --- | --- |
| 같은 사주·성별·나이 → 같은 판정 | 통과 |
| 성별이 다르면 다른 구간을 지난다 | 통과 (순행/역행이 갈린다) |
| 성별 미지정이면 `decade === null` | 통과 |
| 부족을 채우면 보완 / 과다에 더하면 가중 / 그 밖은 중립 | 통과 |
| 첫 대운을 지나는 중이면 직전 구간 없음 | 통과 |
| 1960~2005년생 46건에서 세 작용이 모두 나온다 | 통과 |
| 문구에 사건 예고·시기 특정 어휘 없음 | 통과 (`yearly` 목록 + `몇 살`·`언제부터`·`내년`) |
| 세운 문구를 그대로 쓰지 않는다 | 통과 |

### 실제 풀이 표본 (2026-08-18 · 6표본 중 5건 · 1건은 설계대로 400)

`node scripts/sample-readings.mjs --types decade`. **시각 미상 + 성별 미지정 표본은 400 으로
막혔다** — 이 유형의 설계가 그대로 동작한 것이라 실패가 아니다.

| 자수 | 절당 문장 | hedge/1000자 | 명리로 연 절 | 장면 비율 | 금지 어휘 | 수치·월별 | 제목 준수 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1,948 | 7.56 | 0.81 | 0.28 | 0.14 | **0건** | **0건** | 5/5 |

TASK-55~57 로 고친 지시문 위에서 태어난 유형이라 다른 공개 유형과 같은 자리에서 나왔다.
**처음 회차는 명리로 여는 절이 0.68 이었다** — `직전 10년과 달라진 것`·`몸에서 먼저 드러나는
자리` 두 절이 간지·계절 이름으로 열고 있었고, 그 절들에 여는 자리 규칙을 한 줄씩 더해
0.28 로 내려갔다. **전역 규칙만으로는 안 잡히고 절 지침에 다시 적어야 한다**는 TASK-56 의
관측이 새 유형에서도 그대로였다.

> **라벨 인용 지표는 이 유형을 재지 못한다.** 스크립트가 세는 라벨이 체질 판정 축들이라
> 대운 작용·견준 결은 목록에 없다. 지표는 TASK-53 에서 고정했고 **고치고 나서 고르지 않는다**는
> 원칙이 있으므로 손대지 않았다 — 대신 `prompt.test.ts` 가 판정이 프롬프트에 실리는지 본다.

### 현재 대운을 찾는 규칙은 한 곳에 있다

`findCurrentDaeun` / `findCurrentDaeunIndex`. 예전에는 같은 `find` 가 `DaeunTimeline` ·
`ResultView` · `card-model` 셋에 흩어져 있었다. 나이 구간 판정이 어긋나면 **화면·공유 카드·
풀이가 서로 다른 대운을 가리킨다.**

---

## 3-5. 출생지 경도 (TASK-37)

`longitude` 는 API 가 원래 받고 있었는데 UI 가 없어 **전국이 서울(126.9784°) 고정**이었다.
그 상태에서 무엇이 틀렸는지 실측하고, 목록 단위를 정한 근거를 남긴다.

### 무엇이 흔들리는가

**년주·월주는 영향이 없다.** 절기 판정은 절대 시각(UTC)으로 하므로 출생지가 바꿀 수 없다
(`beijingClock` 이 `utcMs` 에서 나온다). 3개 날짜 × 3개 시각 × 7개 지역 = **84건 검사, 차이 0**.
흔들리는 것은 보정된 지방시를 쓰는 **일주·시주**뿐이다.

| 출생지 | 보정량 | 시주가 갈리는 비율 | 일주가 갈리는 비율 |
| --- | --- | --- | --- |
| 서울 (기본값) | −32분 | — | — |
| 인천 | −33분 | 0.8% | 0.07% |
| 대전 | −30분 | 1.7% | 0.14% |
| 대구 | −26분 | 5.0% | 0.42% |
| 부산 | −24분 | **6.7%** | 0.56% |
| 울릉도 | −16분 | 13.3% | 1.11% |

하루 1440분을 1분 단위로 훑어 서울 기본값과 대조한 값이다. 비율은 시주 쪽이 크지만
**일주가 갈리는 쪽이 더 아프다** — 일간이 바뀌면 십신·신강신약·체질 판정이 통째로 달라진다.

### 목록 단위 — 시/군 + 광역시는 구만 통째 (168개)

광역 안에서 동서 끝이 벌어지는 폭을 재고 정했다.

| 구역 | 경도 폭 | 시각차 |
| --- | --- | --- |
| 서울 (구 전체) | 0.32° | 1.3분 |
| 부산 (구만) | 0.26° | 1.0분 |
| **인천 (군 포함)** | **1.25°** | **5.0분** |
| 인천 (구만) | 0.25° | 1.0분 |
| 경기도 (김포~가평) | 0.80° | 3.2분 |
| 경상북도 (울릉 제외) | 1.24° | 5.0분 |
| 강원도 (철원~강릉) | 1.59° | 6.4분 |

- **구까지 나누지 않는다.** 광역시의 구를 구분해 얻는 것은 0.8%p 뿐이다.
- **시/도 17개만으로도 안 된다.** 강원을 한 덩어리로 두면 5.8% 가 남아, 고치려던 문제
  (부산 6.7%)와 거의 같은 크기다.
- **단, 광역시의 군(郡)은 뗀다.** 인천은 옹진군이 백령도(124.7°)까지 품어 군을 합치면
  폭이 1.25°(5.0분)로 벌어진다. 군을 떼면 0.25°로 다른 광역시와 같아진다.
  같은 규칙으로 기장군·달성군·강화군·옹진군·울주군이 따로 선다.
- 제주는 광역이지만 섬 동서 폭이 0.75°(3분)라 **제주시·서귀포시로 나눈다.**

결과적으로 잔차가 어디서나 **1% 안쪽**이다.

### 표의 출처

`lib/form/birthplaces.ts` 는 **자동 생성 파일**이다 (`node scripts/build-birthplaces.mjs`).
결과물을 커밋하므로 배포에 스크립트가 필요 없다 — 아이콘과 같은 방식이다.

| | |
| --- | --- |
| 자료 | 통계청(KOSTAT) **센서스용 행정구역경계 2013** |
| 경유 | `github.com/southkorea/southkorea-maps` `kostat/2013/json/skorea_municipalities_geo_simple.json` (시군구 251개) |
| 라이선스 | KOSTAT 부분은 "Free to share or remix" |
| 경도 산출 | 행정구역 경계의 **면적가중 중심점**(신발끈 공식), 소수 둘째 자리 |

**시청 좌표를 손으로 옮기지 않았다** — 그것이 곧 눈대중이고, 경계에서 계산하면 168개
전부를 출처 하나로 설명할 수 있다. 소수 둘째 자리에서 끊는 이유는 0.01° = 0.04분이고
보정이 분 단위로 반올림되기 때문이다. 그 아래는 없는 정확도를 있는 척하는 것이다.
시/도 표시 이름은 스크립트가 들고 있으며(좌표가 아니라 이름이다), 자료의 코드 집합과
어긋나면 생성이 실패한다.

### P. 출생지 (`lib/form/birth-input.test.ts` · `lib/saju/pillars.test.ts`)

- 시/도 17개, 모든 시/도에 항목 1개 이상, 시/도+시/군 조합 유일(`고성군` 이 강원·경남에 둘)
- 경도가 스키마 범위(124~132) 안 — 벗어나면 고를 수 없는 항목이 된다
- 대표 6개 지점이 문헌값과 0.2° 안
- 광역시에 `구` 로 끝나는 항목이 없고 군은 남아 있음 / 도 지역 통합시의 구가 모시로 합쳐짐
- **고르지 않으면 보정량이 −32분(서울)로 그대로** — 기존 결과가 조용히 바뀌지 않는다
- 부산을 고르면 −24분이 되고, 01:28 출생이 서울은 자시·부산은 축시로 갈린다
- 년주·월주는 경도와 무관 (3날짜 × 3시각)
- 시각 미상이면 경도를 줘도 보정하지 않음 (`mode === "standard"`)
- 로그 허용 목록에 `longitude`·`birthplace` 가 없음 (`observability.test.ts`)

### 개인정보

- **지역 이름은 서버로 보내지 않는다.** 계산에 필요한 것은 경도뿐이라 요청에는 숫자만
  실린다. 결과 화면의 지역 표시는 폼이 들고 있는 값으로 그린다.
- **쓰이지 않을 값은 아예 보내지 않는다.** 시각 미상이거나 보정 없음을 고르면 서버가
  경도를 버리므로 요청에서 뺀다. 폼도 같은 조건으로 선택을 잠근다.
- `app/privacy/page.tsx` 1항(처리하는 정보)·3항(보관 기간)에 태어난 지역을 넣었고,
  **4항에 보정량으로 대략의 지역이 짐작될 수 있다**고 밝혔다 — 프롬프트가 이미
  `correctionMinutes` 를 보내므로 지역 이름을 빼도 이 사실은 그대로다.

---

## 3-6. 풀이 서술 품질 — 착수 전 기준선 (TASK-53 · TASK-54)

여기까지의 절이 **판정이 맞는가**를 재는 반면 이 절은 **읽히는가**를 잰다. TASK-55~58 이
"풀이가 밋밋하다" 를 고치는데, 기준선 없이 고치면 좋아졌는지를 느낌으로 판단하게 되고
그건 사후 합리화다. 그래서 **고치기 전에** 표본과 지표를 고정했다.

측정 도구는 `scripts/sample-readings.mjs` 다. 로컬 dev 서버의 `POST /api/saju` 를 두드려
**실제 경로(스트리밍 · 섹션 파싱 · 문장 분리)를 그대로 지난** 결과를 잰다.

> **이 표는 보조다.** "공감되는가" 는 셀 수 없다. 지표만 좋아지고 읽기가 나빠지면
> **지표를 버린다.** 판단은 산출물 마크다운을 직접 읽고 한다.

### 표본 (합성 생일 6건)

실제 사람의 생년월일을 쓰지 않는다. 판정 공간을 훑도록 전수 계산에서 고른 6건이며,
`gainPattern` 5/5 · `metabolism` 2/2 · `thermal` 5/5 · `dietApproach` 4/4 ·
`movementKind` 4/4 · `yearly.effect` 3/3 을 덮는다. 여기에 무작위로는 안 걸리는 두
경로를 따로 넣었다 — **`even`**(과다·부족이 하나도 없는 원국, 전수에서 약 2%)과
**시각 미상 + 성별 미지정**(시주 없음 · `daeun === null`).

스크립트의 `expect` 가 매 회차 판정을 대조한다. `constitution.ts` 를 고쳐 표본이 다른
칸으로 밀리면 전후 비교의 전제가 깨지므로 **조용히 지나가지 않고 실패로 잡힌다.**

### 기준선 (2026-08-18 · `gemini-3.5-flash-lite` · 6표본 × 3유형 × 2회차 = 36건)

| 유형 | 자수 | 절당 문장 | hedge/1000자 | 명리로 연 절 | 장면 비율 | 절 사이 일치도 | 라벨 인용 | 제목 준수 | 첫 글자 | 전체 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `diet` | 865 | 4.06 | 2.29 | **0.98** | 0.073 | 0.025 | 35/36 | 12/12 | 1,109ms | 3,817ms |
| `gain-cause` | 1,075 | 3.97 | **4.70** | 0.95 | 0.105 | 0.038 | 34/36 | 12/12 | 1,293ms | 7,070ms |
| `diet-method` | 1,141 | 3.72 | 0.36 | 0.42 | 0.106 | **0.045** | 24/24 | 12/12 | 1,138ms | 4,409ms |

- **금지 어휘 5건** — `단식` 1 · `칼로리` 2 · `처방` 2. **전부 부정문이다**
  (`칼로리나 감량 기간에 얽매이지 않고` · `무리한 단식이나 극단적인 제한 대신` ·
  `영양학적 처방이 아니라`). 위반이 아니라 모델이 금지된 것을 **거부하려고 이름을 부른**
  경우다. 스크립트가 부정문을 걸러내지 않는 이유는 아래 "오탐" 참고.
- **수치·월별 0건** — 본문에 숫자 처방도 `3월에는` 류도 나오지 않았다.
- **회차 간 단어 일치도(Jaccard) 0.29** — `lib/gemini.ts` 의 temperature 실측(0.343)과
  같은 방법이다. 표본이 달라 직접 비교는 아니지만 자릿수가 같다.
- **판정 라벨 미인용 3건은 전부 `대사 기조`** 였다 (36건 중). 다른 축은 100% 인용됐다.

### 이 표가 말하는 것

1. **형식은 이미 지켜지고 있다.** 섹션 제목 준수 36/36, 수치 0건, 라벨 인용 93/96.
   즉 **밋밋함은 계약 위반이 아니라 서술의 문제**다 — TASK-53~58 의 전제("축이 부족한 게
   아니라 전달 층이 문제")가 숫자로 확인됐다.
2. **`diet` 는 절의 98%를 명리 용어로 연다.** 사실상 모든 절이
   `임수 일간이 겨울에 태어나…` 로 시작한다는 뜻이고, 이것이 TASK-56 이 겨냥한 지점이다.
   `diet-method` 가 0.42 로 낮은 것은 그 유형의 지침이 이미 실행을 먼저 말하기 때문이다.
3. **장면은 7~11%뿐이다.** 장면을 명시적으로 요구하는 절은 `gain-trigger` 하나인데,
   그 유형(`gain-cause`)조차 0.105 에 그친다.
4. **hedge 는 유형마다 13배 차이가 난다** (`gain-cause` 4.70 vs `diet-method` 0.36).
   원인을 묻는 유형일수록 무르게 쓴다 — TASK-55 가 (i)판정 단정과 (ii)인과 금지를
   갈라야 하는 이유가 여기 있다.
5. **절 사이 일치도는 셋 다 낮다** (0.025~0.045). `diet-method` 가 가장 높긴 하지만
   절대값이 작아 **"같은 값을 네 이름으로 되풀이한다" 는 TASK-58 의 가설을 이 지표만으로는
   뒷받침하지 못한다.** 낱말이 겹치지 않아도 같은 얘기일 수 있으므로 TASK-58 은 이 값과
   함께 사람이 읽은 판단을 근거로 삼아야 한다.

### 지표를 고르며 걷어낸 오탐

본문을 훑는 검사는 문구 표를 훑는 검사보다 오탐이 나기 쉽다. **기준선을 확정하기 전에**
아래 둘을 걷어냈다 — 남겨 두면 TASK-55~58 이 없는 회귀를 쫓게 된다.

| 걸린 것 | 실제 | 처리 |
| --- | --- | --- |
| `진단`·`처방` | `구체적인 진단과 처방은 전문가와 상의하세요` — **우리 `TYPE_RULES` 가 요구한 면책 문장**이다 | 전문가 권유가 같은 문장에 있으면 그 두 낱말만 면제 |
| `효과` | `무리한 절식은 오히려 역효과를 낳는다` | `역효과` 를 제외 (`견과`/`비견과 겁재` 오탐과 같은 계열) |
| `한 주` | `한 주 동안 일정이 몰리면` — 처방이 아니라 **장면**이다 (지표 ④ 가 장면 어휘로 센다) | 본문 검사에서 `주`·`달` 을 단위에서 뺌 |

마지막 줄은 `constitution.ts` 문구 표 검사와 **일부러 다르게** 두었다. 그쪽은 실행
지시문이라 `주 3회` 계열이 실제 위험이지만, 본문에서는 같은 말이 상황 묘사다.
**문맥이 다르면 목록도 다르다.**

**부정문은 걸러내지 않는다.** 위 금지 어휘 5건이 전부 부정문이었지만, 걸러내려면
"아니라 / 보다 / 말고" 를 문맥으로 판단해야 하고 넓게 잡으면 진짜 위반이 함께 빠져나간다.
사람이 보라고 남겨 둔다 — 이 지표는 게이트가 아니라 눈에 띄게 하는 장치다.

### 추론 예산 재실측 (TASK-54 · 2026-08-18 · `gemini-3.5-flash-lite`)

`thinkingLevel: MINIMAL` 고정의 근거가 **다른 모델(`gemini-3.5-flash`)에서 잰 값**이었고,
숫자가 세 곳에서 어긋나 있었다. 현재 기본 모델로 다시 쟀다.

| level | 첫 글자 | 전체 | `thoughtsTokenCount` |
| --- | --- | --- | --- |
| MINIMAL | 1.1초 | 3.8~7.1초 | **없음** |
| LOW | 1.1초 | 5.3초 | **없음** |
| MEDIUM | — | 5.0초 | **없음** |
| HIGH | **8.8~11.2초** | 10.7~13.3초 | 1,600~3,930 |

**lite 에서 `MINIMAL`·`LOW`·`MEDIUM` 은 같은 설정이다.** 추론 토큰이 하나도 생기지 않고
응답에 필드 자체가 오지 않는다. 그래서 원래 태스크가 잡았던 "MINIMAL vs LOW" 대조는
성립하지 않고, 실제 선택지는 **MINIMAL 과 HIGH 둘뿐**이다.

#### HIGH 를 택하지 않은 이유 셋 (표본 18건)

1. **첫 글자가 8~10배 늦다.** 원국을 먼저 보내고 스트리밍하는 설계 전체가 첫 글자
   1초를 위한 것인데(`app/api/saju/route.ts` 의 "왜 원국을 먼저 보내는가"), HIGH 는
   그 1초를 9초로 만든다. 전체 시간(13초)이 `maxDuration = 30` 안이라는 것과는 별개의 손실이다.
2. **추론 내용이 본문으로 샜다.** 18건 중 1건에서 모델의 영어 사고 과정이 그대로 나왔다 —
   `5. **Final Polish:** Check markdown format and section headers precisely.` 같은 메모가
   `## 한눈에 보기` 앞에 붙었다. 섹션 계약이 깨져 폴백으로 떨어지고, 그 건은 판정 라벨을
   하나도 인용하지 못했다.
3. **본문이 잘린다.** 아래 참고.

서술도 좋아지지 않았다. 자수(803 vs 865) · 장면 비율(0.073 vs 0.073) · 절 사이 일치도가
전부 MINIMAL 과 같은 자리였고, **판정 라벨 인용은 오히려 내려갔다**(94% → 83%).
**밋밋함은 추론 예산에서 오지 않는다** — TASK-55~58 이 프롬프트 쪽을 고치는 것이 맞다.

#### 출력 토큰 예산은 추론과 공유한다

TASK-56 이 분량을 늘리려면 알아야 하는 값이다.

- **한국어는 출력 토큰 1개당 약 1.7자다** (1,340자/784토큰 · 1,273자/755토큰). 그래서
  `maxOutputTokens: 4096` 은 약 **6,900자**를 담는다. 지금 풀이가 900~1,100자이므로
  **3,000자대로 늘려도 상한을 올릴 필요가 없다** — 단 추론이 꺼져 있을 때만이다.
- 추론을 켜면 같은 예산을 나눠 쓴다. `HIGH` + `maxOutputTokens: 1200` 으로 재 봤더니
  `thoughtsTokenCount` 1,148 + `candidatesTokenCount` 48 로 예산이 추론에 다 들어갔고
  **본문은 문장 중간에서 끊긴 85자**가 나왔다.
- **그때도 스트림은 `error` 가 아니라 `done` 으로 끝난다.** 잘린 글이 성공으로 보고되므로
  관측에도 안 잡힌다. 추론을 켜는 변경을 한다면 상한을 함께 올리는 것이 필수다.

#### 프로덕션에서 재지 않은 이유

원래 태스크는 프로덕션 실측을 요구했다. 그 요구는 **`thinkingLevel` 을 올렸을 때도
`maxDuration = 30` 안에 들어오는지**를 확인하기 위한 것이었는데, 결론이 "올리지 않는다"
이므로 프로덕션에 나가는 설정이 그대로다. 지금 값(전체 5.8초 · `CLAUDE.md` 배포 절)이
계속 유효하다. **설정을 바꾸는 결론이 나왔다면 프로덕션에서 다시 쟀어야 한다.**

### hedge 경계를 다시 그은 뒤 (TASK-55 · 2026-08-18 · 6표본 × 1회차)

기준선이 뒤집은 것은 **hedge 가 유형마다 13배 차이 난다**(`gain-cause` 4.70 vs
`diet-method` 0.36)는 관측이었다. 원인은 프롬프트가 **성질이 다른 두 문장을 한 줄로
묶어 놨던 것**이다 — (i) 코드가 정한 판정 라벨과 (ii) 몸에서 실제로 일어나는 인과.
`app/disclaimer/page.tsx` 가 막는 것은 (ii)뿐이고 (i)은 오히려 "언제나 같은 판정" 이라고
자랑하는 내용인데, 프롬프트의 `성향과 경향으로 서술합니다` 한 줄이 둘을 같이 물렀다.

| 유형 | hedge/1000자 (기준선 → 이후) | 자수 | 라벨 인용 | 제목 준수 |
| --- | --- | --- | --- | --- |
| `diet` | 2.29 → **1.06** | 865 → 708 | 18/18 | 6/6 |
| `gain-cause` | 4.70 → **0.65** | 1,075 → 797 | 18/18 | 6/6 |
| `diet-method` | 0.36 → 0.68 | 1,141 → 968 | 12/12 | 6/6 |

- **가장 크게 물러 있던 유형이 가장 크게 내려갔다** (`gain-cause` 7배). 유형 간 13배 격차도
  사라졌다 — 경계가 유형마다 다르게 그어져 있던 것이 격차의 원인이었다는 뜻이다.
- `diet-method` 만 0.36 → 0.68 로 올랐지만 **절대값이 작아 문장 한두 개 차이**다
  (표본 6건 합계 4건). 이 유형은 원래 hedge 가 거의 없었다.
- **판정 라벨 인용이 48/48 로 올랐다** (기준선 93/96). 단정을 허용하니 라벨을 피해 가지
  않는다 — 미인용 3건이 전부 `대사 기조` 였던 것도 그 축을 무르게 쓰려다 생략한 결과였다.
- **자수는 셋 다 줄었다.** hedge 어미를 걷어낸 만큼이며, 분량은 TASK-56 이 다룬다.

#### 지시문이 만든 금지 어휘 오탐 하나를 되돌렸다

첫 회차에서 금지 어휘가 5 → 9건으로 늘었는데 **7건이 `진단`·`질환`** 이었고, 전부
`gain-misread` 절의 마지막 문장이었다. 원인은 새로 쓴 지시문이 "몸에서 일어나는 일을
**진단**한 것이 아니라는 것을 밝혀라" 라고 요구한 것 — **`TYPE_RULES` 가 의학 용어로
막고 있는 낱말을 지시문이 직접 요구**하고 있었다. `확인한 것이 아니라` 로 바꾸고 그 낱말을
부정문으로도 쓰지 말라고 덧붙이니 6 → 1건이 됐다.

**부정문이라도 지시문이 시켜서 나오는 것은 오탐이 아니라 우리 잘못이다.** 3-6 절 "오탐"
표의 셋은 모델이 알아서 쓴 부정문이라 면제했지만, 이건 프롬프트를 고치는 쪽이 맞다.

### 절 구성을 다시 나눈 뒤 (TASK-56 · 2026-08-18 · 6표본 × 1회차)

기준선이 짚은 둘을 함께 고쳤다 — **절의 98%가 명리 용어로 열린다**(`diet`)는 것과
**장면이 7~11%뿐**이라는 것. 원인은 `각 절은 3~5문장 / 전체 2,000자` 라는 **균등 배분**이
근거 인용 + 용어 풀이 + 근거→해석 연결을 다 넣으라 요구해 **장면 쓸 자리가 물리적으로
없었던 것**이다.

| 유형 | 자수 (기준선 → 이후) | 절당 문장 | 명리로 연 절 | 장면 비율 | hedge/1000자 | 라벨 인용 |
| --- | --- | --- | --- | --- | --- | --- |
| `diet` | 865 → **1,543** | 4.06 → 6.9 | 0.98 → **0.15** | 0.073 → **0.254** | 2.29 → 1.39 | 14/15 |
| `gain-cause` | 1,075 → **1,981** | 3.97 → 8.05 | 0.95 → **0.20** | 0.105 → **0.214** | 4.70 → 0.94 | 12/12 |
| `diet-method` | 1,141 → **1,663** | 3.72 → 6.0 | 0.42 → **0.17** | 0.106 → **0.263** | 0.36 → 0.89 | 8/8 |

- **절당 문장 수가 유형마다 갈렸다** (6.0 / 6.9 / 8.05). 기준선에서 셋이 3.7~4.1 로 붙어
  있던 것이 균등 배분의 자국이었다. 절 단위로 봐도 `diet` 의 중심 절(체질)이 다른 절의
  두 배 안팎이다.
- **분량은 지시(3,000자)의 절반에서 멎었다.** 모델이 상한을 목표로 삼지 않는다.
  1,500~2,000자면 baseline 의 두 배이고 읽기에도 과하지 않아 **지시값은 3,000 그대로 둔다** —
  내리면 다시 1,000자대로 내려앉는다.
- **글이 잘리지 않는다.** 가장 긴 표본이 2,411자(약 1,420토큰)로 `maxOutputTokens` 4096 의
  35%다. 모든 표본이 종결 부호로 끝났다.
- 전체 응답 시간은 로컬 6.3~10.4초(기준선 3.8~7.1초). `maxDuration = 30` 대비 여유가 크다.
  **프로덕션 실측은 배포 뒤에 한 번 확인할 것** — 기준선의 로컬:프로덕션 비가 약 1.5배였다.

#### 두 번 재야 했던 이유 — 첫 회차가 만든 회귀 셋

분량만 늘리고 끝냈다면 아래 셋을 놓쳤다. **분량을 늘리면 늘어난 자리를 무엇이 채우는지를
따로 봐야 한다.**

| 회귀 | 첫 회차 | 원인 | 고친 것 |
| --- | --- | --- | --- |
| hedge 가 `diet` 에서 **기준선보다 높아짐** (2.77) | 요약 절이 `약간 서늘한 쪽으로 기운 편입니다` | TASK-55 규칙이 "판정 문장" 이라고만 말해 긴 글에서 느슨해짐 | 요약 절을 예문과 함께 명시 |
| 라벨 인용 48/48 → **43/48** | `대사 기조`·`접근 순서` 를 풀어 씀 | 자리가 생기자 라벨을 설명으로 대체 | "라벨은 낱말 그대로" 를 시스템 지시로 승격 |
| `diet-method` 의 명리 여는 절 0.42 → **0.64** | `이 사주는 …` 로 여는 문장 | **우리 집 문체가 지표에 걸린다** — `사주` 가 명리 용어 목록에 있다 | 지표를 고치지 않고 **프롬프트를 고쳤다** |

마지막 줄이 판단이 갈리는 지점이었다. `이 사주는` 을 목록에서 빼면 숫자는 바로 좋아지지만,
**고치고 나서 지표를 고르면 유리한 것만 고르게 된다**(스크립트 주석의 원칙). 게다가
`이 사주는 …` 으로 여는 문장은 실제로 읽는 사람이 자기 이야기로 받지 못하는 문장이라,
지표가 가리킨 것이 오탐이 아니었다. 여는 자리 규칙에 **나쁜 예 두 개와 좋은 예 하나**를
붙이자 0.64 → 0.17 로 내려갔다.

#### 무료 등급 분당 토큰 한도

**이번 실측 범위(회당 18요청 · 직렬 12.5초 간격 · 요청당 약 1,400 출력 토큰)에서는 걸리지
않았다.** 429 를 받은 적이 없어 `quotaId` 로 확인할 기회도 없었다. 확인처는 여전히
`CLAUDE.md` 가 적어 둔 두 곳(AI Studio 대시보드 · 429 응답의 `quotaId`)뿐이고,
**대시보드는 계정 접근이 필요하므로 남겨 둔다.**

### 말투를 상담체로 바꾼 뒤 — 최종 (TASK-57 · 2026-08-18 · 6표본 × 3유형 × 2회차 = 36건)

`--label after --repeat 2`. **기준선과 같은 조건**이라 위 표들과 직접 비교된다.

| 유형 | 자수 | 절당 문장 | hedge/1000자 | 명리로 연 절 | 장면 비율 | 절 사이 일치도 | 라벨 인용 | 제목 준수 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `diet` | 865 → **1,651** | 4.06 → 8.48 | 2.29 → 2.04 | 0.98 → **0.33** | 0.073 → **0.231** | 0.025 → 0.053 | 34/36 | 12/12 |
| `gain-cause` | 1,075 → **1,689** | 3.97 → 7.25 | 4.70 → **1.08** | 0.95 → **0.25** | 0.105 → **0.205** | 0.038 → 0.047 | 35/36 | 12/12 |
| `diet-method` | 1,141 → **1,574** | 3.72 → 5.35 | 0.36 → 0.72 | 0.42 → **0.10** | 0.106 → **0.290** | 0.045 → 0.050 | 24/24 | **11/12** |

- **회차 간 단어 일치도 0.238** (기준선 0.29). 장면이 늘면서 회차마다 다른 장면이 나오므로
  내려간 것이다. 같은 입력에 같은 문장이 필요하면 온도가 아니라 캐싱이라는 결론은 그대로다.
- **제목 준수 1건 실패** — `## 어떤 종류로 움직il까`. 한글 낱말 가운데에 로마자가 섞인
  모델 글리치이고 프롬프트 문제가 아니다. **계약이 설계대로 버텼다** — 그 절만 `id: null` 로
  담기고 나머지가 잡혀 `recognized: true` 라 원문 폴백으로 떨어지지 않았다.
- **금지 어휘 11건 · 수치 1건.** 아래에서 갈라 본다.

#### 이 태스크가 실제로 바꾼 것 (지표에 안 잡히는 것 포함)

| ① 판정 문구 | "그대로 베끼지 말고 다시 쓰세요" → **"결을 살려 쓰고 앞뒤에 장면과 근거를 붙여라"**. `constitution.ts` 의 동사 중심 문구(`그릇을 작게 쓰고 천천히 씹기`)가 본문에 살아서 나온다 |
| ② 말투 예시 셋 | **표본 36건 중 예시 문장이 그대로 나온 건 0건.** "복사하지 말고 결만 따르라" 가 작동했다 |
| ③ 괄호 풀이 tic | `일간(태어난 날의 천간)` 류가 **36건에서 2건**. 절마다 반복되던 패턴이 사라졌다 |
| ④ 면책 문장 | `TYPE_RULES` 셋에서 빼고 `ResultView` 고정 문구로 옮겼다. 본문이 사람 말로 끝난다 |
| ⑤ 존댓말 | **`-습니다`체로 확정** (그 전에는 결정이 아니라 우연이었다) |
| ⑥ 되짚기 | 36건 중 2건에 한 번씩. **상한(절마다 하나)을 넘긴 건 0건**이고 요약 절에는 없었다 |

#### 금지 어휘 11건을 갈라 보면

**위반은 하나였고 고쳤다.**

| 갈래 | 건수 | 판단 |
| --- | --- | --- |
| 부정문 (`칼로리를 계산하지 않고`·`치료제처럼 대하지 않고`·`단식 방식을 가까이하지 않고`) | 5 | 기준선과 같은 갈래. **부정문은 걸러내지 않는다**는 원칙대로 사람이 보고 넘긴다 |
| `진단` — `gain-misread` 마지막 문장 | 4 | **지시문이 끌어온 것.** "진단한 것이 아니다" 를 요구하니 그 낱말이 나간다 |
| `장부` — **오탐** | 1 | `굳어진 긴장부터` 에 걸렸다. `견과`/`비견과 겁재` 와 같은 부분 문자열 충돌이다 |
| **`한 컵` — 진짜 위반** | 1 | `미지근한 물을 한 컵 먼저 마시고`. 숫자를 안 썼을 뿐 **섭취량**이다 |

- **`한 컵`** — 금지 목록이 `몇 개·몇 그램·몇 잔` 처럼 **숫자를 전제한 표현만** 적고 있었다.
  `eating` 과 `diet-method` 규칙에 "한글로 센 양도 같다" 를 더했다. 재측정에서 **0건.**
- **`진단`** — TASK-55 에서 한 번 고쳤는데도 남았다. "그 낱말을 쓰지 마라" 로는 부족해서
  **쓸 문장의 결을 직접 줬다** ("여기까지는 사주에서 읽은 결이고, 몸에서 실제로 무슨 일이
  일어나는지는 저희가 알 수 없습니다"). 재측정에서 4 → 1건, 그마저 다른 절이었다.
- **`장부` 오탐은 스크립트를 고치지 않고 여기 적어 둔다.** 스크립트 주석이 "지표는 고정하고
  나중에 바꾸지 않는다 — 고치고 나서 고르면 유리한 것만 고르게 된다" 를 못 박아 뒀고,
  이 검사는 게이트가 아니라 사람 눈에 띄게 하는 장치다. **다음에 이 낱말이 뜨면 문맥부터 볼 것.**

#### 반복은 남아 있다 — 그러나 원인이 파생 축이 아니었다 (TASK-58 재측정)

TASK-58 은 "파생 절이 같은 값을 네 이름으로 말한다" 를 의심했고, **분량과 장면이 들어가면
반복이 덜 보일 수 있으니 다시 재라**고 적어 두었다. 잰 결과 절 사이 단어 일치도는
`diet-method` 0.045 → 0.050 으로 **사실상 그대로**이고, 표본 12건을 통독한 판단은 이렇다.

- **반복이 줄어든 표본이 있다.** 파생 절이 각자 다른 장면을 갖게 되면서 `무엇을 먼저 고정할까`
  (순서) · `어떤 종류로 움직일까`(동작) · `어떤 순서로 먹을까`(밥상)가 서로 다른 그림이 됐다.
- **그런데 판정이 "리듬 고정 우선 × 저강도 지속" 으로 겹치는 표본에서는 여전히 겹쳐 읽힌다.**
  두 축이 모두 "같은 시각에 반복" 으로 수렴하기 때문이다.
- **원인 하나는 파생 축이 아니라 TASK-56 이 만든 층 누수였다.** `movement` 지침은 원래
  "시간대와 온도는 아래 실행 절이 맡는다" 고 정해 두었는데, 내가 붙인 장면 예시가
  `(출근 전, 점심 뒤, 주말 아침처럼)` 이라 **시각을 직접 요구하고 있었다.** 그래서 움직임 절이
  시간대를 말하고 실행 절이 같은 말을 되풀이했다. 장면을 **시각이 아니라 상황으로** 쓰라고
  고쳤다 (앉아 있던 시간이 길어졌을 때, 계단 앞에 섰을 때).
- **남은 반복은 계약 문제였고, 그 결정은 TASK-48 과 함께 내렸다** (아래).

### 반복의 정체는 파생 축이 아니라 마지막 절이었다 (TASK-58 종결 · 2026-08-18)

`diet-method` 12건을 다시 읽으니 되풀이가 나오는 자리가 하나로 좁혀졌다 —
**`이번 달 실천 3가지` 가 위 세 절을 한 줄씩 요약하고 있었다.**

```
첫째, 첫 끼니 시각을 고정합니다.        ← "무엇을 먼저 고정할까" 그대로
둘째, 그릇 크기를 줄이고 …곁들입니다.   ← "어떤 순서로 먹을까" 그대로
셋째, 숨이 조금 차되 말은 이어지는 …    ← "어떤 종류로 움직여 볼까" 그대로
```

12건 중 예외가 없었다. **파생 축(`movementKind`·`mealSequence`)이 아니라 이 절이 새 정보 0
이었던 것**이고, 그래서 세 절을 접는 대신 **이 절 하나를 다시 설계했다.**

| | 전 | 후 |
| --- | --- | --- |
| 제목 | 이번 달 실천 3가지 | **이번 달 먼저 할 하나** |
| id | `monthly-actions` | `first-step` |
| 요구 | "구체적이고 작게 시작할 수 있는 것" | **하나만 고르고**, 자리 잡았는지 **무엇으로 아는지**, 그다음 무엇으로 넘어가는지 |

- **`movement` 를 접지 않은 이유는 TASK-48 이다.** 그쪽이 `movement` 를 두 유형에 남기고
  지침만 가르는 설계이고, 방법 유형에서 움직임을 빼면 "무엇을 어떻게" 가 반쪽이 된다
  (`diet` 에 `gain-pattern` 을 남긴 것과 같은 판단).
- **셀 수 있는 지표는 움직이지 않았다** — 절 사이 단어 일치도 0.050 → 0.049(기준선 0.045).
  태스크가 착수 전에 이미 적어 둔 대로다: *"파생 절은 같은 판정을 다른 어휘로 돌려 말하므로
  Jaccard 가 낮게 나온다. 판단 근거는 지표가 아니라 사람이 읽은 결과여야 한다."*
  **지표가 못 보는 종류의 반복이었고, 고친 뒤에도 못 본다.**

#### 곁가지로 잡은 계약 파손 하나

같은 회차에서 **제목 준수가 9/12** 로 떨어졌는데 새 제목이 아니라
`## 어떤 종류로 **움직일까**` 가 원인이었다 — 모델이 `움직il까`(로마자 혼입) 2건,
`움직임이 들어갈까`(의역) 1건을 냈다. `after` 회차에서 1/36 으로 보였던 글리치가 이 유형에
집중돼 있었다. 제목을 **`어떤 종류로 움직여 볼까`** 로 바꾸니 **12/12** 로 돌아왔다.

**계약 제목은 모델이 재현하기 쉬운 낱말로 고를 것.** 형식을 강제할 수단이 부탁뿐이라
(`sections.ts` 의 "왜 JSON 구조화 출력을 쓰지 않는가") 어려운 낱말은 그대로 파손률이 된다.

### 산출물

풀이 마크다운은 **커밋하지 않는다.** LLM 출력이라 덩어리가 크고 회차마다 다르다.
스크립트가 OS 임시 디렉터리에 쓰고, 저장소에 남기는 것은 위 표와 판단뿐이다.

---

## 4. 남은 한계

고칠 계획이 없거나, 다른 태스크로 넘긴 것들.

1. **절기 시각 오차 1~2분** — 라이브러리가 근사 알고리즘을 쓴다. 절입 시각 2분 안에 태어난
   경우 월주가 갈릴 수 있다. 정밀 천문 계산으로 바꾸려면 별도 작업이 필요하다.
2. **한국 음력 vs 중국 음력** — 라이브러리는 중국 기준으로 음력을 계산한다. 한국천문연구원
   음력과 드물게 하루 차이가 난다(삭 시각이 자정 근처일 때). 음력 입력자에게만 영향.
3. **해외 출생 미지원** — 국내 출생지는 TASK-37 에서 열었다(아래 3-4 절). 다만 스키마가
   경도를 124~132°(한반도)로 제한하고 `wallClockToUtc` 가 `Asia/Seoul` 고정이라,
   해외 출생자가 현지 시계시를 넣으면 한국 시각으로 취급돼 결과가 크게 틀린다.
   경도 하나로 안 되고 시간대 이력이 필요하다.
4. **균시차 근사** — NOAA 근사식으로 오차 약 0.1분. 실용상 무해하다.
5. **테스트로 고정되지 않음** — 지금은 일회성 스크립트다. Vitest 로 옮기는 것은 TASK-05.
6. **세운은 절입 경계를 따지지 않는다** — 연 단위 표시가 목적이라 해당 연도 간지를 그대로
   쓴다. 입춘 이전 며칠은 엄밀히 전년도 간지다. 일별 운세를 낼 때 다시 봐야 한다.
7. **세는나이 기준** — 대운·세운 나이는 세는나이(출생년=1세)다. 한국 만세력 다수 관행이지만
   만 나이를 쓰는 유파도 있다.
8. **지장간을 오행 점수에는 반영하지 않는다** — 신강·신약의 득지·득세는 지장간 전체를
   보지만(3-2 절), **오행 점수와 지지 십신은 여전히 본기만** 본다. 점수에 넣으려면 여기·중기·
   정기의 일수 배분을 정해야 하는데 그 배분이 학파마다 다르다. 통근은 "있나 없나" 만 보므로
   그 문제를 피해 간다.
9. **체질 판정의 임계값에 외부 근거가 없다** — 1.5배·0.5배는 표본을 보고 정한 우리 관례이지
   문헌값이나 임상 근거가 아니다. 판정이 **재현 가능**하다는 것과 **맞다**는 것은 다르다.
   이 서비스가 보증하는 것은 앞쪽뿐이며, 면책 고지에도 그렇게 적었다.
10. **합·충·형·파 미반영** — 지지 사이의 관계(삼합·육합·충 등)는 오행 세력을 바꾸는 유파가
   많으나, 판정 기준이 학파마다 갈려 체질 판정에 넣지 않았다. 오행 점수는 글자 수와
   계절 기세만 본다. 올해 운세도 같다 — **세운과 원국 사이의 충(沖)·합(合)을 보지 않는다.**
   세운 오행이 과부족에 닿는지만 본다.
11. **월별 운세 없음** — 올해 운세는 연 단위다. 월운을 내려면 절입 경계로 월지를 잡아야 하고
   (한계 6번의 세운 문제와 같은 뿌리), 프롬프트에서 월 특정을 금지해 두었다.
12. **올해 운세의 기준 연도는 서버 시각의 연도다** — 입춘 이전 며칠은 엄밀히 전년도 간지이지만
   (한계 6번) 연 단위 표시가 목적이라 그대로 쓴다. 1~2월 초 이용자에게는 한 해 앞선 판정이
   나갈 수 있다.
